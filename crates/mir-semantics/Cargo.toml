[package]
name = "mir-semantics"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
mir-ast = { path = "../mir-ast" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
