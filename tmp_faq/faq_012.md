# FAQ 12

## この文書について

この文書は、2026-04-23 時点の current reading に基づいて、

- 現状でどこまで何が終わっているか
- もう二大問題を完全に解決して、言語側の実装まで終わっていると読めるか
- その段階へ持っていく全体像に対して今どこにいるか
- そこへ持っていくために、まだ何の判断や情報が要るか
- それらに答えると repo がどこまで自走できるか

を、`faq_011.md` の次段として整理する FAQ である。

規範判断の正本は `specs/`、長期の repository memory は `plan/`、
薄い snapshot は `progress.md` と `tasks.md` にある。
この FAQ 自体は **current explanation** であり、新しい規範判断を作る文書ではない。

今回の FAQ では、特に user の clarification に従い、
**二大問題** を次の意味で固定して読む。

1. **Problem 1**
   - 型システム / 定理証明 / モデル検査
   - しかも、単なる current layer の sample 実装ではなく、
     theory construction、checker/runtime 実装、
     semantics と綺麗に対応した public syntax 実装まで含める
2. **Problem 2**
   - `memory_order` family を高水準 relation / order / handoff discipline として再解釈し、Mir 側へ無理なく取り込むこと
   - しかも、単なる relation list や sample 実装ではなく、
     theory construction、runtime/verification 実装、
     semantics と綺麗に対応した public syntax 実装まで含める

この厳しめの定義で判定すると、
結論は `faq_011.md` より一段前進しているが、なお
**「完全に終わった」とは言えない**
になる。

今回の FAQ は、特に次を踏まえている。

- `docs/reports/0904-clean-near-end-alpha-release-completion.md`
  - clean near-end active suite と repo-local alpha-ready current layer の closeout
- `progress.md`
  - 2026-04-23 00:07 JST 時点の lane-based snapshot
- `tasks.md`
  - 2026-04-22 23:53 JST 時点の current task map
- `plan/01-status-at-a-glance.md`
  - Problem 1 / Problem 2 の current state
- `plan/11-roadmap-near-term.md`
  - near-term residual package
- `plan/12-open-problems-and-risks.md`
  - current risk register
- `plan/17-research-phases-and-autonomy-gates.md`
  - autonomy gate の current reading
- `specs/12-decision-register.md`
  - D-197 / D-198 を含む current principal judgment
- 2026-04-23 08:20 JST の fresh anchor
  - `python3 scripts/current_l2_guided_samples.py smoke-all --format json`
  - `python3 scripts/current_l2_guided_samples.py closeout --format json`

---

## 1. 先に短く答えるとどうか

短く言うと、次の読みが現在もっとも正確である。

1. **かなり進んでいる。**
   current repo は docs-only skeleton ではない。
   clean near-end active suite、finite-index first strong typing layer、
   order/handoff relation family、mutex/weak-memory の model-check second line、
   Lean foundations、generated clean theorem stubs まで
   repo-local alpha-ready current layer に actualize 済みである。
2. **しかし、二大問題を completely solved と呼ぶのはまだ正確ではない。**
   user が明示したように、
   theory construction、implementation、semantics と綺麗に対応した public syntax 実装まで含めて言うなら、
   current repo が close しているのは
   - current principal line
   - repo-local runnable floor
   - helper-local / sample-local verification floor
   - mixed gate と true user-spec gate の切り分け
   までであり、
   final public theory / public syntax / public contract までは終わっていない。
3. **言語側の実装まで終わっているともまだ言えない。**
   現在あるのは、
   - clean sample suite とそれを支える runtime/checker-adjacent code
   - CLI / smoke / closeout / Lean sync
   - first strong typing finite-index fragment
   - order/handoff high-level relation family
   - model-check second-line sample corpus
   である。
   しかし、
   - final parser grammar
   - final public parser / checker / runtime / verifier API
   - semantics と clean に対応した final public syntax
   - concrete theorem/model-check production binding
   - packaging / installed binary
   はまだない。
4. **全体像の中での現在地は、「foundation がまだ全然できていない初期」ではない。**
   むしろ、
   - repo-local alpha current layer は reached
   - 二大問題の current first line は source-backed かつ runnable
   - ただし final public completion は still mixed gate / true user-spec gate
   という段階である。
5. **「何を決めればそこまで行けるか」には 2 種類ある。**
   - repo がかなり自走して詰められる mixed gate
   - user の目的・配布形態・最終 target がないと決められない true user-spec gate

---

## 2. `faq_011.md` 以後に genuinely progressed したこと

`faq_011.md` の後で進んだのは、単なる wording refresh ではない。
大きい変化は次である。

### 2.1 active sample suite が clean near-end へ置き換わった

以前の `p..` prototype chain / `samples/not_implemented/` / old Lean stub corpus を
active principal として読む段階は終わった。

current repo は、

- `samples/clean-near-end/`
  - active canonical sample suite
- `samples/old/2026-04-22-pre-clean-near-end/`
  - historical archive
- `samples/lean/clean-near-end/`
  - active clean suite 由来の generated theorem stub
- `samples/lean/old/2026-04-22-pre-clean-near-end/`
  - historical Lean archive

という形に整理されている。

これは重要である。
なぜなら、現在地の説明が
「old prototype をたくさん持っている near-end」
から
**「clean active suite を principal に持つ repo-local alpha current layer」**
へ変わったからである。

### 2.2 Problem 1 の current principal が finite-index first layer で concretized された

`faq_011.md` までの読みでは、
Problem 1 はかなり進んでいたが、
まだ old bundle / prototype chain 由来の説明が濃く残っていた。

現在は、

- finite decidable index fragment
- user-defined authority preorder
- user-defined security label lattice
- capture set
- region preorder
- simple cost bound

を first strong typing layer の principal として、
clean sample suite と runtime code で source-backed に actualize している。

つまり、
**Problem 1 は「first executable slice がある」段階から、
「clean active suite を principal に持つ repo-local alpha current layer がある」段階へ上がった**
と読める。

### 2.3 Problem 2 の current principal が order/handoff relation family と model-check split で concretized された

Problem 2 についても、現在は

- `publication_order`
- `witness_order`
- `finalization_order`
- `scoped_happens_before`

を principal に置く clean order/handoff suite が active であり、
同時に

- `01_peterson_sc_pass`
- `02_peterson_relaxed_counterexample`
- `03_broken_mutex_counterexample`

を **model-check second line** に分離して持っている。

これは、
`memory_order` 再解釈の current principal line が

- high-level relation family は first line
- mutex / weak-memory / visibility counterexample は second line

という split で repo-local に actualize されたことを意味する。

### 2.4 docs / plan / progress / tasks / source trace も clean current layer に同期された

current repo では、
status explanation が old prototype bundle 前提のまま残っている状態ではない。

少なくとも、

- `README.md`
- `Documentation.md`
- `progress.md`
- `tasks.md`
- `plan/01`
- `plan/11`
- `plan/12`
- `plan/17`
- `specs/00`
- `specs/12`
- `docs/reports/0904-*`

が、clean near-end active path に同期している。

したがって今回の FAQ 12 は、
`faq_011` の abstract continuation ではなく、
**clean near-end alpha closeout 後の explanation refresh**
として書くのが正確である。

---

## 3. 現在、どこまで何が終わっているか

### 3.1 repo 全体として既に終わっていること

repo 全体として current source-backed に終わっているのは、少なくとも次である。

- active clean sample suite 16 本
- old sample / old Lean corpus の archive separation
- finite-index first strong typing layer
- user-defined authority / label / capture / region / cost theory
- order/handoff relation family の first line
- mutex / weak-memory family の model-check second line
- Lean foundations
- active clean suite 由来の generated Lean stub corpus
- smoke / closeout / matrix / family-run で再確認できる helper surface

2026-04-23 08:20 JST の fresh anchor では、
`python3 scripts/current_l2_guided_samples.py smoke-all --format json`
と
`python3 scripts/current_l2_guided_samples.py closeout --format json`
が通っており、次を再確認できた。

- active sample root は `samples/clean-near-end/`
- archive root は `samples/old/2026-04-22-pre-clean-near-end/`
- family 構成は
  - typing 5
  - order-handoff 6
  - model-check 3
  - modal 2
- typing / order-handoff / model-check / modal の representative outcomes は
  0904 closeout report と整合した

### 3.2 Problem 1 の current status

ここでの Problem 1 は、
**型システム / 定理証明 / モデル検査を、
theory construction から implementation、public syntax まで含めて完成させること**
である。

#### 3.2.1 Problem 1 で current repo が既に持っているもの

少なくとも次は持っている。

- theory side
  - first strong typing line を full dependent core ではなく
    finite decidable index fragment に置く principal judgment
  - conceptual spine:
    `Σ ; Ψ ; Γ ; Δ ⊢ e : A @ μ ! ε ⇝ C ; O`
  - IFC / authority / capture / region / cost を same first layer 上の target とする reading
  - theorem/model-check を first-line type solver へ collapse しない split
- implementation side
  - `crates/mir-runtime/src/clean_near_end.rs`
    による finite-index first layer
  - constraint kinds:
    `label_flow`,
    `authority_ge`,
    `capture_subset`,
    `region_outlives`,
    `cost_leq`,
    `effect_row_subset`,
    `requires_witness`,
    `requires_publication`
  - typing clean samples 5 本
  - theorem obligation export / Lean stub generation
  - model-check second-line sample corpus
- verification side
  - authorized / unauthorized / label-flow / capture / cost negative pair
  - Lean foundations
  - generated clean theorem stubs
  - repo-local smoke / closeout evidence

#### 3.2.2 ただし、Problem 1 はまだ complete ではない

user の厳しめの定義では、まだ complete ではない。
理由は次である。

- theory construction が final ではない
  - finite-index first layer は決まった
  - しかし final public typed source principal は未決
  - full dependent core を採るかは still later
- theorem side が final public contract ではない
  - Lean foundations はある
  - generated stubs もある
  - しかし final public theorem result object、
    proof object public schema、
    consumer-facing theorem payload contract はまだない
- model-check side が final public language / tool binding ではない
  - second-line corpus はある
  - しかし first settled property language、
    concrete production tool binding、
    final public checker artifact はまだない
- syntax side が final public syntax ではない
  - clean sample suite はある
  - しかし final parser grammar、
    final elaboration policy、
    semantics と clean に対応した public typed syntax はまだ deferred

したがって、Problem 1 について一番正確な言い方は次である。

**Problem 1 の current first line は repo-local alpha current layer として実装・検証済み。**
しかし、
**Problem 1 を theory / implementation / public syntax まで含めて完全解決したとはまだ言えない。**

### 3.3 Problem 2 の current status

ここでの Problem 2 は、
**`memory_order` family をそのまま source principal に出さずに、
高水準の order / handoff / publication / witness discipline として再解釈し、
theory construction から implementation、public syntax まで含めて完成させること**
である。

#### 3.3.1 Problem 2 で current repo が既に持っているもの

少なくとも次は持っている。

- theory side
  - low-level `memory_order_*` exact source wording を principal にしない judgment
  - high-level relation family を principal にする judgment
  - `atomic_cut` を local finalization nucleus に留める judgment
  - mutex / weak-memory family は type checker first line ではなく model-check second line とする split
- implementation side
  - clean order-handoff samples 6 本
  - clean model-check samples 3 本
  - relation family:
    `program_order`,
    `dependency_order`,
    `publication_order`,
    `observation_order`,
    `witness_order`,
    `finalization_order`,
    `scoped_happens_before`
  - missing witness / handoff-before-publication rejection
  - stage-block / delegated RNG / auditable witness current line
- verification side
  - valid handoff path
  - negative pair
  - Peterson SC pass
  - relaxed memory counterexample
  - broken mutex counterexample

#### 3.3.2 ただし、Problem 2 もまだ complete ではない

Problem 2 も、user の定義ではまだ complete ではない。
理由は次である。

- theory construction が final ではない
  - current principal は high-level relation family
  - しかし low-level `memory_order` family を最終的に public syntax にどう位置づけるかは未決
  - fairness / replay / shared-space final catalog も未決
- implementation が final public language line ではない
  - clean suite と runtime はある
  - しかし final source wording、
    final emitted-artifact schema、
    final witness/provider public contract はまだない
- syntax side が final public syntax ではない
  - current clean samples は repo-local alpha surface としては十分
  - しかし semantics と clean に対応した final public handoff syntax、
    final parser grammar、
    final lowering contract はまだない

したがって、Problem 2 について一番正確な言い方は次である。

**Problem 2 の current first line は repo-local alpha current layer として実装・検証済み。**
しかし、
**Problem 2 を theory / implementation / public syntax まで含めて完全解決したとはまだ言えない。**

### 3.4 実際に実行できるコード例と、その sample で何が検査されるか

ここでは、「本当に動くのか」「どこまで checker / model-check / Lean が見ているのか」を誤読しないために、clean near-end active suite からいくつか actual code example を抜く。

sample 単位の実行には、現在は次の helper command を使える。

```bash
cargo run -q -p mir-runtime --bin mir-clean-near-end -- run-sample <sample-id> --format json
```

また、2026-04-23 の fresh 実行結果では、少なくとも次の sample を再確認している。

#### 3.4.1 typing: user-defined authority と label lattice による declassification success

```mir
module CleanNearEnd.AuthorizedDeclassification

use CleanNearEnd.IndexTheories

principal Alice : FingerprintAuthority.Releaser

resource secret_key : PrivateKey
  label SecurityLabel.KeyMaterial
  capture SecretKeyStore
  lifetime Session

resource public_board : Board
  label SecurityLabel.Public
  lifetime Session

transition release_fingerprint(actor = Alice) {
  stage derive:
    fp_secret <- perform derive_fingerprint(secret_key)
      ensure label(fp_secret) = SecurityLabel.KeyMaterial

  stage declassify:
    fp_public <- declassify fp_secret
      from SecurityLabel.KeyMaterial
      to SecurityLabel.Public
      using FingerprintReleasePolicy
      requires authority(Alice) >= FingerprintAuthority.Releaser

  stage publish:
    receipt <- perform publish_fingerprint(fp_public)
      to public_board
      after declassify(fp_public)
      requires witness(fp_public)
      ensure label(fp_public) = SecurityLabel.Public
}
```

実行 command:

```bash
cargo run -q -p mir-runtime --bin mir-clean-near-end -- run-sample 01_authorized_declassification --format json
```

fresh result の核心は次である。

- `static_verdict = "valid"`
- `terminal_outcome = "success"`
- `constraints_solved`:
  - `authority(Alice) >= FingerprintAuthority.Releaser`
  - `declassify SecurityLabel.KeyMaterial -> SecurityLabel.Public permitted by FingerprintReleasePolicy`
  - `label(fp_public) = SecurityLabel.Public`

この sample が示しているのは、

- `SecurityLabel` や `FingerprintAuthority` が builtin ではなく **user-defined finite theory** であること
- first strong typing layer が authority preorder と label lattice を実際に読んでいること
- declassification が magical predicate ではなく policy + finite constraint として扱われていること

である。

#### 3.4.2 typing: capture / lifetime は actual static rejection まで入っている

```mir
module CleanNearEnd.CaptureEscapeRejected

use CleanNearEnd.IndexTheories

resource token : SessionToken
  capture EphemeralToken
  lifetime Step

fn make_public_closure(token : SessionToken @ capture(EphemeralToken))
  -> Fn[Unit -> Unit] @ capture(RoomHistory) {
  return fn () {
    use token
  }
}

transition bad_capture_escape {
  stage closure:
    f <- make_public_closure(token)
      requires {EphemeralToken} <= {RoomHistory}
      requires Region.Session <= Region.Step
}
```

実行 command:

```bash
cargo run -q -p mir-runtime --bin mir-clean-near-end -- run-sample 04_capture_escape_rejected --format json
```

fresh result の核心は次である。

- `static_verdict = "malformed"`
- `reason_family = "capture_escape"`
- `constraints_failed`:
  - `{EphemeralToken} <= {RoomHistory}`
  - `Region.Session <= Region.Step`

つまり current repo は、

- capture set inclusion
- region / lifetime preorder

を finite-index first layer の actual static constraint として検査している。
ただし、これは final public borrow checker 完成を意味しない。現在あるのは **clean sample suite 上で動く first decidable slice** である。

#### 3.4.3 order / handoff: publication より前の handoff は actual static rejection

```mir
module CleanNearEnd.HandoffBeforePublicationRejected

use CleanNearEnd.IndexTheories

principal Alice : FingerprintAuthority.Admin
principal Bob   : FingerprintAuthority.Holder

resource dice_owner : Player
  authority_scope room
  lifetime Session

transition bad_handoff_before_publish(owner = Alice) {
  stage roll:
    draw <- perform roll_dice via authority_rng

  stage handoff:
    handoff dice_owner Alice -> Bob
      requires witness(draw_pub)

  stage publish:
    publish draw
      scope room
      produces witness draw_pub
}
```

実行 command:

```bash
cargo run -q -p mir-runtime --bin mir-clean-near-end -- run-sample 03_handoff_before_publication_rejected --format json
```

fresh result の核心は次である。

- `static_verdict = "malformed"`
- `reason_family = "handoff_before_publication"`
- `constraints_failed`:
  - `publish(draw) must precede handoff that requires witness(draw_pub)`

したがって current repo は、少なくとも current principal line では

- `requires_witness`
- `requires_publication`
- publication と handoff の順序制約

を actual static rule として持っている。

#### 3.4.4 model-check second line: relaxed memory では counterexample を返せる

```mir
module CleanNearEnd.PetersonRelaxedCounterexample

model PetersonRelaxedNoPublication {
  actor A
  actor B

  shared flag[A] : Bool = false
  shared flag[B] : Bool = false
  shared turn : Actor = A

  process A {
    flag[A] <- true
    turn <- B
    wait while flag[B] == true and turn == B
    critical A
    flag[A] <- false
  }

  process B {
    flag[B] <- true
    turn <- A
    wait while flag[A] == true and turn == A
    critical B
    flag[B] <- false
  }

  memory_model relaxed_without_publication_observation_edges

  property mutual_exclusion:
    never (in_critical(A) and in_critical(B))
}
```

実行 command:

```bash
cargo run -q -p mir-runtime --bin mir-clean-near-end -- run-sample 02_peterson_relaxed_counterexample --format json
```

fresh result の核心は次である。

- `model_check_result = "counterexample"`
- `property = "mutual_exclusion"`
- `checked_under = "relaxed_without_publication_observation_edges"`
- `counterexample_shape`:
  - `A writes flag[A] but B has not observed it`
  - `A writes turn <- B`
  - `A reads flag[B] as false`
  - `B writes flag[B] but A has not observed it`
  - `B writes turn <- A`
  - `B reads flag[A] as false`

ここで重要なのは、
mutex / weak-memory family が static typing first line ではなく、
**actual counterexample を返す second line**
として repo に入っていることである。

#### 3.4.5 modal / stage line: `stable` と `later` は current mode constraint として読める

```mir
module CleanNearEnd.StageStableLaterMinimal

stable config : GameConfig

transition staged_roll {
  stage prepare:
    cfg <- use stable config

  stage roll:
    draw <- perform roll_dice(cfg)
      produces value draw @ later

  stage publish:
    publish draw
      requires value draw @ later
      produces witness draw_pub
}
```

実行 command:

```bash
cargo run -q -p mir-runtime --bin mir-clean-near-end -- run-sample 01_stage_stable_later_minimal --format json
```

fresh result の核心は次である。

- `static_verdict = "valid"`
- `mode_constraints`:
  - `config : stable`
  - `draw available at later stage`
  - `publish requires later draw and produces witness`

つまり current repo は、raw `◯` / `□` を final syntax に固定してはいないが、
`stable` / `later` を **current mode evidence** として検査している。

#### 3.4.6 Lean: small proof fragment は actual に通る

Lean 側では、たとえば `samples/lean/foundations/CurrentL2FiniteIndexFirstLayer.lean` に次のような lemma がある。

```lean
theorem zero_budget_rejects_remote_call :
    ¬ remoteCallAllowed 0 := by
  simp [remoteCallAllowed]

theorem single_budget_is_exhausted_after_one_call :
    ¬ remoteCallAllowed (spendRemoteCall 1) := by
  simp [spendRemoteCall, remoteCallAllowed]
```

実行 command:

```bash
source "$HOME/.elan/env" && lean samples/lean/foundations/CurrentL2FiniteIndexFirstLayer.lean
```

2026-04-23 の fresh run では exit 0、stdout/stderr ともに空で成功した。
これは、

- capture / lifetime / cost の first mechanization-ready fragment がある
- ただし sample 全体の domain proof が full discharge 済みという意味ではない

ことを示している。

### 3.5 現在、どの言語機能が実装済みで、どこまで検査できるか

ここは言い切りを慎重に分ける必要がある。
「実装済み」と言ってよいのは、**repo-local helper / active sample suite / Lean foundation の current layer で actually 動いている範囲** である。

#### 3.5.1 built-in と user-defined の境界

`python3 scripts/current_l2_guided_samples.py closeout --format json` の fresh output が示す current built-in vocabulary は次である。

- `module`
- `index`
- `policy`
- `principal`
- `resource`
- `effect`
- `place`
- `option`
- `chain`
- `fallback`
- `lineage`
- `perform`
- `via`
- `require`
- `ensure`
- `atomic_cut`
- `transition`
- `stage`
- `publish`
- `observe`
- `handoff`
- `witness`
- `model`
- `property`

一方で、次は built-in ではない。

- `SecurityLabel`
- `FingerprintAuthority`
- `CaptureScope`
- `Region`
- `CostBudget`
- `FingerprintReleasePolicy`
- `draw_pub`
- `dice_owner`

これはとても重要である。
つまり current repo は、
**domain predicate / domain hierarchy を builtin magic として埋め込んでいない。**

#### 3.5.2 実装済み機能と検査範囲の正確な表

| 機能 | current 実装状態 | 現在 actually 検査できること | まだ検査できない / まだ final ではないこと |
|---|---|---|---|
| finite index theory declaration | 実装済み | finite preorder / lattice / powerset / cost-bound を sample 側宣言として読む | final public parser grammar と fully general index language |
| authority-sensitive declassification | 実装済み | `authority_ge`、policy-based declassification、label 変化の妥当性 | final public typed syntax、full dependent declassification theory |
| label flow checking | 実装済み | direct high-to-low flow rejection、policy のない公開拒否 | final public checker artifact / full IFC language |
| capture / lifetime checking | 実装済み | `capture_subset`、`region_outlives` に基づく static rejection | final ownership / borrow surface、general region inference |
| cost bound checking | 実装済み | `cost_leq`、simple pointwise budget rejection | richer cost algebra、production optimizer / scheduler integration |
| order / handoff witness discipline | 実装済み | `requires_witness`、`requires_publication`、publication-before-handoff rejection | final public handoff syntax、distributed finality / durability contract |
| relation extraction | 実装済み | `publication_order`、`witness_order`、`finalization_order`、`scoped_happens_before` の current relation summary | low-level `memory_order` public exact surface |
| model-check second line | 実装済み | curated model sample に対する `pass` / `counterexample` と counterexample shape | generic public model-check language、concrete production tool binding |
| modal / stage line | 実装済み | `stable`、`later`、`published(room)`、`witnessed(...)` を current mode constraint として確認 | final public modal syntax、full multimodal dependent calculus |
| Lean foundations | 実装済み | small proof fragment の Lean success | sample 全体の full theorem discharge |
| generated Lean stubs | 実装済み | sample ごとの theorem stub 生成と Lean accept | final public proof-object contract、domain-complete theorem corpus |
| clean sample surface | 実装済み | active suite 16 本を helper が実行・集計・closeout できる | final public language syntax と parser |
| parser / checker / runtime / verifier public API | 未完 | repo-local helper CLI はある | final public API contract 全体 |

#### 3.5.3 したがって、「実装済み」をどう読むべきか

次の言い方は正確である。

- finite-index first layer は **実装済み**
- order / handoff current principal line は **実装済み**
- model-check second line は **実装済み**
- modal current line は **実装済み**
- Lean first proof spine は **実装済み**

しかし、これらはすべて
**repo-local alpha-ready current layer として実装済み**
という意味であって、

- final public typed language complete
- final public parser complete
- final public theorem/model-check contract complete
- final public `memory_order` reinterpretation syntax complete

という意味ではない。

---

## 4. では「もう二大問題を完全に解決して、言語側の実装まで終わっている感じか」

答えは **no** である。

ただし、この `no` は
「まだ初期で、ほとんど未着手」という意味ではない。
正確には、次の意味で `no` である。

### 4.1 yes と言ってよい部分

次については yes と言ってよい。

- repo-local alpha-ready current layer は reached
- 二大問題の current first line は source-backed
- clean active suite は runnable
- first strong typing layer は実装済み
- `memory_order` reinterpretation の current principal line も実装済み
- model-check second line も実装済み
- Lean-first proof skeleton も repo-local に確認可能

### 4.2 yes と言ってはいけない部分

次については yes と言ってはいけない。

- 二大問題を final theory まで completely solved
- final public syntax まで clean に実装済み
- final parser / checker / runtime / verifier API まで完了
- final theorem/model-check production binding まで完了
- final witness/provider/emitted-artifact public contract まで完了
- installed binary / packaging / host integration まで完了

### 4.3 したがって、現在地を一文で言うなら何か

一文で言うなら、次が最も正確である。

**「二大問題の current first line は、clean near-end active suite と finite-index typing / order-handoff / model-check / Lean floor を伴う repo-local alpha-ready current layer として実装・検証済みだが、final public theory / public syntax / public API / production binding はまだ mixed gate と true user-spec gate に残っている。」**

---

## 5. 全体像に対して今どこにいるか

repo 全体を 1 本の進捗率で読むのは不正確である。
少なくとも lane と macro phase を分けて読む必要がある。

### 5.1 lane で読むとどこか

現在の lane split は、次の読みが正確である。

| lane | current reading | 意味 |
|---|---|---|
| execution lane | `Macro 4 active on base corpus + clean near-end suite` | current runnable sample floor はある |
| theory-lab lane | `Macro 5 current layer alpha-ready` | Problem 1 / 2 の current first line は current layer として close 済み |
| reserve / mixed lane | `Macro 6 / 7 residual` | final public seam、catalog、packaging は still later |

### 5.2 macro phase で読むとどこか

`progress.md` / `plan/17` の current reading をまとめると、概ね次である。

| Macro | 現在位置 | 読み方 |
|---|---|---|
| `Macro 4` | active on clean near-end floor | representative executable floor は reached |
| `Macro 5` | repo-local alpha-ready current layer | Problem 1 / Problem 2 の current principal line は close 済み |
| `Macro 6` | minimal working subset default | fabric / shared-space final catalog はまだ later |
| `Macro 7` | mixed | parser/public API / packaging / host integration residual がある |
| `Macro 8` | early | broader application target は未確定 |

### 5.3 「全体像のど真ん中で、今どこにいるか」を平たく言うと

平たく言うと、今は次の位置にいる。

1. foundation comparison の段階はもうかなり終わっている
2. repo-local alpha current layer は到達済み
3. 次の主戦場は suite widening ではなく public seam / public API residual
4. そこで必要なのは、何を final public に上げ、何を retained-later に残すかの narrowing
5. さらにその先で、配布・統合・上位アプリという user-spec dependent な層が残る

つまり、
**「理論以前」でも「最終完成直前」でもなく、
repo-local alpha current layer を終えたあと、
final public seam と final public syntax / API へ行く直前の narrowing 段階**
にいる。

---

## 6. その段階に持っていくために必要な判断や情報は何か

ここは 2 種類に分けるのが重要である。

1. repo が evidence を積みながらかなり自走できる **mixed gate**
2. user の目的がないと決められない **true user-spec gate**

### 6.1 mixed gate として必要な判断

#### 6.1.1 Problem 1 側

Problem 1 を complete に近づけるために、少なくとも次の narrowing が必要である。

- final typed source principal をどうするか
  - current finite-index first layerを public principal に据えるのか
  - stronger typed source surface を別段で導入するのか
- theorem side の final public contract をどうするか
  - theorem result object
  - proof object public schema
  - consumer-facing theorem payload
- model-check side の final public contract をどうするか
  - property language
  - checker artifact
  - verifier handoff
  - concrete tool seam
- syntax side をどうするか
  - conceptual judgment をどう surface syntax へ落とすか
  - declassification / capture / cost / theorem obligation / model-check obligation を
    syntax へどこまで見せるか

これらは大半が mixed gate であり、
repo 側が docs / sample / helper / test を使って相当程度は詰められる。
ただし、全部を自動的に final adoption にしてよいわけではない。

#### 6.1.2 Problem 2 側

Problem 2 を complete に近づけるために、少なくとも次の narrowing が必要である。

- `memory_order` family の最終 public stance
  - high-level relation family を principal のまま維持するのか
  - low-level exact surface を expose するならどの layer に置くのか
- order / handoff の final public wording
  - publication / witness / handoff / finalization / observation を
    syntax 上どう表すか
- emitted-artifact / witness-provider public contract
  - witness schema
  - provider receipt
  - combined public contract
- model-check second line との綺麗な境界
  - mutex / weak-memory family を final にどこまで second line のまま残すか
- `atomic_cut` と order/handoff family の public explanation
  - local finalization nucleus と global mutex / durable commit の誤読をどう防ぐか

これも大きくは mixed gate であり、
repo 側がかなり詰められる。
しかし、
**「low-level exact public syntax を採るかどうか」**
は final public language design の核に触るので、慎重な narrowing が必要である。

#### 6.1.3 cross-cutting に必要な判断

Problem 1 / Problem 2 をまたいで、次も必要である。

- final parser grammar
- final parser / checker / runtime / verifier API
- clean sample surface と final public syntax の関係
- repo-local helper と final public CLI/API の分離
- theorem / model-check / witness-provider / emitted-artifact の public contract 境界

### 6.2 true user-spec gate として必要な情報

これは repo 内 evidence だけでは final に決められない。

#### 6.2.1 配布と運用の target

少なくとも次が必要である。

- repo-local helper のままでよいか
- installed binary を first-class target にするか
- package / distribution / CI success criteria をどこに置くか
- FFI / engine adapter を要るものとして扱うか

#### 6.2.2 broader application target

少なくとも次が必要である。

- authoritative-room first line を延長するのか
- Mirrorea / shared-space を first application target にするのか
- Typed-Effect Wiring Platform を first integration target にするのか
- Prism / upper application を先に取りに行くのか

#### 6.2.3 public syntax の completion criterion

これも user 側の completion definition が必要である。

- final public syntax が要るのは research-grade か product-grade か
- notation / parser / tutorial / diagnostics をどこまで含めるか
- “綺麗に対応した構文” の acceptance criteria を何にするか

この 3 点が曖昧だと、
repo は clean current layer の先でどこまで public completion を目標化すべきか決めにくい。

---

## 7. それらに答えると repo はどこまで自走できるか

### 7.1 すでに user 追加入力なしでできること

すでに次はかなり自走できる。

- clean suite の maintenance
- finite-index first layer の hardening
- Lean foundations の small-proof hardening
- order/handoff relation family の docs-first hardening
- model-check second-line corpus maintenance
- parser/public API residual の narrow な整理
- Problem 1 public-seam residual narrowing
- Problem 2 public-shape residual narrowing

### 7.2 user が mixed gate の方向をある程度認めれば、かなり先まで自走できること

たとえば次の方向が認められれば、repo はかなり先まで自走しやすい。

- Problem 1 は finite-index first layer を public principal の first cut に置く
- Problem 2 は high-level relation family を public principal に置き、
  low-level `memory_order` exact wording は retained-later reference に留める
- theorem/model-check/witness-provider contract は helper-local surface をそのまま public 化せず、
  dedicated public contract として narrow する

この 3 点が大きく崩れないなら、
repo は public-seam residual までかなり長く自走できる。

### 7.3 それでも最後まで完全自走とはまだ言えない理由

それでも、次は最後まで完全自走とはまだ言えない。

- final public syntax / tutorial / diagnostics の acceptance criteria
- packaging / installed binary / FFI
- broader application target
- exhaustive shared-space catalog

これらは、repo 内で evidence を積んでも、
最終 completion definition そのものは user 側の目的に依存するからである。

---

## 8. 誤読を避けるための言い換え集

### 8.1 言ってよい

- 「repo-local alpha-ready current layer までは到達している」
- 「二大問題の current first line は source-backed で runnable」
- 「Problem 1 / Problem 2 の first executable slice はある」
- 「clean near-end active suite は principal sample set である」

### 8.2 言ってはいけない

- 「二大問題は完全解決した」
- 「言語実装は終わった」
- 「final public syntax まで終わった」
- 「production theorem/model-check binding まで終わった」
- 「installed binary / packaging まで終わった」

### 8.3 一番短い正確な言い方

**「現在は、二大問題の current first line を clean near-end active suite と finite-index typing / order-handoff / model-check / Lean floor で実装・検証した repo-local alpha-ready 段階であり、final public theory / syntax / API / packaging まで終わった段階ではない。」**

---

## 9. まとめ

問いに対する短い答えをもう一度まとめる。

1. **現状、どこまで何が終わっているか**
   - repo-local alpha-ready current layer は終わっている
   - clean near-end active suite、finite-index typing、order/handoff、model-check second line、Lean floor は終わっている
2. **もう二大問題を完全に解決して、言語側の実装まで終わっている感じか**
   - no
   - theory / implementation / public syntax まで含めた厳密な意味では、まだ終わっていない
3. **全体像に対して今どこか**
   - repo-local alpha current layer を終え、
     final public seam / public syntax / public API の narrowing に入る直前
4. **そこへ持っていくために必要な判断や情報は何か**
   - mixed gate:
     final public typed/order syntax、public contract、public API の narrowing
   - true user-spec gate:
     packaging、host integration、broader application target、completion criteria

この読みを保つ限り、
現在の repo を
「まだ何も無い研究」
とも
「もう完全に終わった完成品」
とも誤読しにくくなる。
