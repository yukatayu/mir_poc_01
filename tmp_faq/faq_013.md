# FAQ 013 — 現状と今後の判断 inventory

- 作成日時: 2026-05-02 17:48:26 JST
- 目的:
  repo の現状と、この先 autonomous に進める際に判断に迷いやすい論点を、source hierarchy に従って一箇所に集約する。
- 想定読者:
  この repo に途中参加した agent / reviewer / 実装者。
- 重要:
  この文書は **判断補助用の FAQ / dossier** であり、規範正本ではない。
  矛盾時の優先順位は `specs/` > `plan/` > `progress.md` / `tasks.md` / `samples_progress.md` > `docs/reports/` > この FAQ。

## 1. 最初に押さえるべき結論

### 1.1 この repo は何を達成済みで、何をまだ達成していないか

- 達成済みの主眼は **repo-local alpha-ready current layer** である。
- 達成していないのは **final public product** である。
- つまり、現状は「docs-only 構想メモ」ではないが、「public parser/runtime/verifier 完成品」でもない。

根拠:
- `README.md`
- `Documentation.md`
- `progress.md`

### 1.2 現在の active root は何か

- active runnable suite:
  `samples/clean-near-end/`
- active base corpus:
  `samples/current-l2/`
- active mechanization evidence:
  `samples/lean/`
- alpha-local scaffold root:
  `samples/alpha/`
- planned-only residual root:
  `samples/not_implemented/`
- generated bridge evidence root:
  `samples/generated/`

重要:
- `samples/alpha/` は **active runnable root ではない**。
- `samples/not_implemented/` は archive ではなく **planned family**。
- `samples/generated/` は source sample ではなく **generated evidence**。

### 1.3 Mirrorea Spaces alpha line の現在地

- current concrete phase:
  `Phase 8 — integrated alpha demo closeout`
- large stage percentages:
  `Stage B 40%`, `Stage C 35%`, `Stage D 35%`, `Stage E 60%`, `Stage F 60%`
- last closed package:
  `P-A0-17`
- current reopen point:
  safe な `P-A0-18` は **まだ promoted していない**

現在の alpha lane の最重要事実:

- `LIF-01/05..08` と `VAR-02/03/05/07/09/10/15` は
  `reason_codes_scope = alpha-static-floor` の negative checker floor
- `LIF-02/03/04` と `VAR-01/04/06` は
  `acceptance_scope = alpha-acceptance-floor` の helper-local positive acceptance floor
- この 2 つは **別 carrier**
- positive row の証拠は「negative reason code が無いこと」ではなく、
  **explicit acceptance row が一致すること**
- これは parser/runtime bridge でも public checker verdict でもない

### 1.4 いま autonomous に進める最大の制約は何か

- 一番大きい repo-side gate は `P-A0-18` 未昇格である。
- いま残っている positive row 群は、現 acceptance schema だけでは安全に widen できない。
- したがって next step は:
  1. 既存 acceptance schema のままで入る row を証明する
  2. あるいは overclaim しない新しい狭い carrier を切る
  のどちらかでなければならない。

### 1.5 `U1` は何か

- `U1` は actual product-shaping gate であり、
  alpha-local package line とは分離されている。
- alpha packages を閉じても `U1` は閉じない。
- packaging / host target / first shipped public surface / final catalog breadth などは
  依然として user-facing decision である。

## 2. この FAQ の使い方

### 2.1 迷ったら最初に見る順番

1. `README.md`
2. `Documentation.md`
3. `progress.md`
4. `tasks.md`
5. `samples_progress.md`
6. `specs/00..03`
7. `specs/09`
8. 問題に関係する subsystem spec
9. `plan/00-index.md`
10. 関係する `plan/*`
11. 直近 report

### 2.2 どの文書が何を決めるか

- `specs/`:
  規範正本
- `plan/`:
  repository memory
- `progress.md`:
  rough status snapshot
- `tasks.md`:
  current task map / blocker map
- `samples_progress.md`:
  runnable / non-runnable sample dashboard
- `docs/reports/`:
  作業証跡
- `tmp_faq/faq_013.md`:
  判断補助の集約メモ

### 2.3 この FAQ での分類

この文書では判断点を次の 4 種類に分ける。

- `決定済み`:
  repo がすでに守るべきと fix しているもの
- `repo が research/discovery で絞ること`:
  user 入力なしでも investigation は可能だが、まだ final ではないもの
- `user が決めること`:
  autonomous に固定してはいけない product-shaping choice
- `後段依存 / まだ触ると危険`:
  先行 semantic carrier や runtime substrate が無いので、今 widen すると overclaim になるもの

## 3. まず悩まなくてよいこと

### 3.1 stack は separable のまま保つ

これはすでに決まっている。

- Mir
- Mirrorea
- PrismCascade
- Typed-Effect Wiring Platform

は関連するが、1 つの runtime に collapse してはいけない。

特に:

- PrismCascade は Mir の sub-runtime ではなく **独立 kernel**
- Typed-Effect Wiring Platform は Mir core そのものではない
- Mirrorea alpha scope に PrismCascade 実装を持ち込まない

### 3.2 Mir core に standard I/O を入れない

これも決定済み。

- `stdin` / `stdout` / `stderr` を privileged core primitive にしない
- external world connection は typed external effect / adapter boundary に残す
- auth / membership / capability / witness / visualization / telemetry を transport に潰さない

### 3.3 `Place` を participant と同一視しない

`Place` は execution locus であり、少なくとも次を持つ読みが current line で固定されている。

- state
- queue
- capability
- visibility
- observation frontier

### 3.4 helper-local preview を public API と読まない

次は **non-final** である。

- helper-local preview
- report-local inventory
- generated bridge evidence
- runtime-private carrier names
- viewer prototype
- projection/codegen `equivalence` reading
- helper-local acceptance floor

### 3.5 `samples/alpha/` を runnable root と読まない

`samples/alpha/` は mixed scaffold であり、family ごとに

- scaffold-only
- checker-seed
- helper-local acceptance floor
- runner-backed non-public floor

が混在している。

存在しているだけでは runnable でも active root でもない。

## 4. すでに固定されている repo-wide guardrails

### 4.1 source hierarchy

- `specs/` = 規範正本
- `plan/` = repository memory
- `progress.md` / `tasks.md` = snapshot
- `samples_progress.md` = sample dashboard
- `docs/reports/` = evidence

### 4.2 invariant 群

次は repo の invariant として扱う。

- semantic event structure は DAG
- hidden backward edge を既定で許さない
- downstream addition が既定
- silent API shadowing は禁止
- compatibility-preserving overlay は hidden narrowing をしない
- lifetime degradation は monotone
- rollback は finalizing cut を越えない
- linear resource duplication を許さない
- visualization / telemetry は typed information-bearing effect

### 4.3 active/current line の principal split

current line は次を principal にする。

- finite decidable index fragment first
- high-level order / handoff relation first
- model-check second line for weak-memory / mutex

current line は principal にしない。

- low-level `memory_order_*` exact source principal
- full dependent typed source principal
- final public theorem / verifier contract

### 4.4 current sample taxonomy

固定済みの class:

- active canonical runnable suite
- base source corpus
- mechanization evidence
- alpha scaffold
- planned residual family
- generated bridge evidence
- historical archive

この class を混ぜると README / Documentation / progress / tasks / samples dashboard が壊れる。

## 5. 現在の autonomous lane の正しい読み

### 5.1 repo は何を自走してよいか

現在、自走してよいのは主に次である。

- stale docs cleanup
- validation rerun
- source hierarchy / docs scaffold maintenance
- report creation
- dashboard freshness
- focused regression repair
- narrow docs-first research inventory
- alpha-local theory-freeze / checker-runtime preparation の範囲内の honest widening

### 5.2 repo が自走してはいけないこと

次は user 入力または新しい semantics なしに固定してはいけない。

- final public parser grammar
- final public parser / checker / runtime / verifier API
- final public auth / visualization / projection / transport / hot-plug ABI
- installed binary / packaging adoption
- exact host integration target
- first shipped public surface
- final shared-space operational catalog breadth
- production transport / durable replay / durable distributed save-load
- production prover binding / model-check binding
- full VRM / VRChat / Unity compatibility

### 5.3 いまの reopen point

`P-A0-17` のあとで safe に reopen できるのは次だけ。

1. existing acceptance schema にそのまま入る追加 positive row があると証明する
2. runtime / remote / layer / adapter を過大主張しない narrower carrier を定義する

これができない限り `P-A0-18` は未昇格のまま。

## 6. user が決めること

以下は `tasks.md` に explicit blocker として整理済みの user-facing decisions である。

### 6.1 packaging shape / installed binary target

- 選択肢:
  `CLI` / `library` / `engine-adapter` / `hybrid`
- current recommendation:
  `library-first`
- 理由:
  現 evidence は helper / library / runner 寄りで、installed binary adoption fact はまだ無い

### 6.2 host integration target

- 選択肢:
  `browser` / `native process` / `engine` / `mixed`
- current recommendation:
  `native process`
- 理由:
  現 evidence は same-process / process-boundary helper が最も強い

### 6.3 first shipped public surface

- 選択肢:
  `parser/checker/runtime/verifier first`
  / `adapter/viewer/projection/hot-plug first`
  / `two-step split`
- current recommendation:
  `two-step split`

### 6.4 final shared-space operational catalog breadth

- 選択肢:
  `minimal subset`
  / `portal / multi-world expansion`
  / `fairness / quorum / exhaustive catalog`
- current recommendation:
  `minimal subset`

### 6.5 first network scope

- 選択肢:
  `local-only`
  / `Docker / local-subprocess`
  / `WAN / federation`
- current recommendation:
  narrow `Docker / local-subprocess` 維持

### 6.6 avatar compatibility first target

- 選択肢:
  `placeholder-only`
  / `custom Mir runtime`
  / `VRM skeleton`
  / `VRChat-compat skeleton`
- current recommendation:
  `placeholder + custom Mir runtime first`

### 6.7 native binary policy

- 選択肢:
  `forbidden`
  / `sandboxed / trust-policy limited`
  / `stronger provenance admission`
- current recommendation:
  `sandboxed / trust-policy limited`

### 6.8 save/load initial scope and UI target

- 選択肢:
  save/load = `local-only` or `multi-Place`
  UI = `CLI` / `HTML viewer` / `richer client`
- current recommendation:
  `local-only save/load + HTML/JSON viewer first`

## 7. repo が research/discovery で詰めること

### 7.1 immediate current queue

- remaining positive rows can fit current acceptance schema or not
- if not, what is the narrowest new carrier family
- remaining `CUT-10/12/16`
- remaining `VIS-04/09/12`
- Stage F completion evidence without overclaim

### 7.2 broader repo-wide research items already named in `tasks.md`

- exact public `VerificationLayer` law surface
- theorem-bridge / runtime-policy widening relation
- emitted rows / evidence carrier / downstream consumer composition
- `FAIRY-05` reopen criteria and final carrier
- projection equivalence evidence boundary
- public emitted-program contract
- witness/provider public-shape route
- combined-contract candidate
- emitted-handoff boundary
- order-handoff source wording route
- emitted-artifact schema candidate
- rollback / durable migration engine state machine after `P21`
- distributed activation ordering evidence
- production transport / replay design preserving auth/membership/capability/witness separation

### 7.3 broad unresolved categories from `specs/10`

これは current queue authority そのものではないが、将来の判断点として無視してはいけない。

- fallback/preference-chain final algebra
- `lease` final syntax
- lineage annotation final syntax
- static-vs-dynamic boundary for mixed cases
- `durable_cut` vocabulary / observation / audit
- future profile family beyond `all_of`
- aggregate evidence / audit standardization
- `perform` / `try` / `fallback` / `require` / `ensure` / `admit` final punctuation
- parser subset freeze and parser-side helper graduation
- route proof
- suspended task behavior across route change
- scaling / rebinding boundary
- exact host schema
- transport seam / ABI
- viewer / telemetry schema
- theorem result object / proof object schema / prover brand
- model-check property language / tool brand / checker artifact
- witness/provider schema and emitted-handoff contract
- multimodal foundation choice
- compatibility metatheory / export floor
- staged physical repo migration

## 8. subsystem ごとの詳細判断 map

## 8.1 Lifetime / Fallback / Guarded Access Path

### 8.1.1 すでに決まっていること

- fallback は target lifetime extension ではない
- fallback は guarded logical access-path availability extension
- monotone degradation を守る
- write capability は fallback で strengthen しない
- `plain ref` / `inherit_chain` / `snapshot_selected` は distinct
- splice/inheritance には explicit lineage evidence が必要
- underdeclared fallback は static error
- dynamic `Reject` は well-formed chain の admissible option exhaustion に限定
- remote observed ref は raw pointer ではない
- rollback / `atomic_cut` / load は hidden resurrection を起こさない

### 8.1.2 current helper-local floor

negative checker floor:

- `LIF-01`
- `LIF-05`
- `LIF-06`
- `LIF-07`
- `LIF-08`

positive acceptance floor:

- `LIF-02`
- `LIF-03`
- `LIF-04`

admitted acceptance kinds:

- `fallback_chain_canonicalized`
- `inherited_chain_spliced_with_lineage`
- `plain_ref_boundary_preserved`

### 8.1.3 いま未解決だが current queue に直結すること

- `LIF-11`:
  anchor/deletion outcome semantics が必要
- `LIF-13`:
  selected-option snapshot semantics が必要
- `LIF-15`:
  remote freshness/membership/frontier carrier が必要

したがって、この 3 行は current helper-local acceptance floor に入らない。

### 8.1.4 この family で agent が迷ったらどうするか

- 追加 row を acceptance floor に上げたい場合:
  まず current 3 kind で honest に表現できるか確認する
- 表現できない場合:
  acceptance floor widening を止め、new carrier design note に切り替える
- runtime / parser / remote bridge を導入したくなった場合:
  その package はもう `P-A0-17` の延長ではない

### 8.1.5 この family で言ってはいけないこと

- fallback が object lifetime を延ばす
- plain ref が hidden に inner chain を splice する
- read-only fallback から write が得られる
- rollback / load で stale lease / stale witness / stale membership が revive する
- helper-local acceptance が parser/runtime implementation を証明する

## 8.2 Contract Subtyping / Layer Compatibility

### 8.2.1 すでに決まっていること

- transparent overlay は input contravariant
- output covariant
- precondition strengthening 禁止
- postcondition weakening 禁止
- undeclared effect widening 禁止
- undeclared failure widening 禁止
- capability strengthening 禁止
- provided surface shrink / hidden shadowing 禁止
- observation label widening 禁止
- redaction weakening 禁止
- retention widening 禁止
- debug / telemetry は typed layer

### 8.2.2 current helper-local floor

negative checker floor:

- `VAR-02`
- `VAR-03`
- `VAR-05`
- `VAR-07`
- `VAR-09`
- `VAR-10`
- `VAR-15`

positive acceptance floor:

- `VAR-01`
- `VAR-04`
- `VAR-06`

admitted acceptance kinds:

- `transparent_observe_only_layer`
- `output_covariance_checked`
- `readonly_covariance_checked`

### 8.2.3 いま未解決だが current queue に直結すること

- `VAR-08`:
  declared-failure runtime evidence が必要
- `VAR-11`:
  label / redaction / retention state が必要
- `VAR-13`:
  activation-cut contract-update evidence が必要
- `VAR-14`:
  adapter-preservation semantics が必要

したがって、この 4 行も current helper-local acceptance floor に入らない。

### 8.2.4 layer/runtime side の current honest scope

current non-public runtime floor は attach-time layer insertion subset に限られる。

- accepted debug attach with authority
- rejected non-admin attach
- explicit auth contract-update path
- declared-failure rate-limit preview
- incompatible patch reject

これ以上の detach / migration / distributed ordering / public ABI は later。

### 8.2.5 この family で言ってはいけないこと

- auth / rate-limit widening を transparent overlay と書く
- debug / telemetry を label-less leak と書く
- mutable/read-write carrier を covariant と書く
- helper-local acceptance floor を runtime layer compatibility completion と書く

## 8.3 Cut / Save / Load / Checkpoint

### 8.3.1 すでに決まっていること

- `atomic_cut` は place-local rollback frontier
- global / durable / distributed commit ではない
- distributed save/load without consistent cut は禁止
- save/load には message/channel, publish/observe, witness, capability, auth, membership, hot-plug closure が必要
- load は stale lease / witness / membership / provenance を resurrect しない
- Z-cycle risk は explicit に扱う

### 8.3.2 current honest actualization

checker floor:

- `CUT-05`
- `CUT-07`
- `CUT-08`
- `CUT-09`
- `CUT-11`
- `CUT-13`
- `CUT-14`
- `CUT-15`

runtime-backed rows:

- `CUT-04` local save/load valid
- `CUT-17` load does not resurrect stale membership

### 8.3.3 いま blocked な行

- `CUT-10`:
  expired lease non-resurrection, but lease store carrier not actualized
- `CUT-12`:
  communication-induced checkpoint repair, but protocol state not actualized
- `CUT-16`:
  stale witness non-resurrection, but witness store carrier not actualized

### 8.3.4 いまの honest reading

- local-only save/load bridge は actualized 済み
- distributed save/load completion は **未claim**
- checker-backed invalid distributed-cut rejection はある
- しかしそれは distributed runtime completion ではない

### 8.3.5 この family で言ってはいけないこと

- local save/load = distributed save/load
- `atomic_cut` = durable commit
- cut checker = distributed persistence completion
- Z-cycle handled = distributed snapshot completed

## 8.4 Runtime Package / Avatar / Hot-plug

### 8.4.1 すでに決まっていること

- avatar / VRM / VRChat / Unity を Mir core primitive にしない
- runtime package admission は typed hot-plug discipline に載せる
- signature is provenance only
- semantic safety には sandbox / capability / effect / resource / revocation / audit が必要
- unsupported runtime fallback は monotone and visible
- detach は free-form deletion ではない

### 8.4.2 current actualized subset

runtime-private executable rows:

- `AV-01`
- `AV-02`
- `AV-06`
- `AV-08`
- `AV-09`
- `HP-11`
- `HP-12`
- `HP-15`

### 8.4.3 planned / mirrored / later rows

- `AV-03`
- `AV-04`
- `AV-05`
- `AV-07`
- `AV-10`
- many `HP-*`

### 8.4.4 current judgment points

- field inventory のまま保つか、validator/admission checker に進むか
- minimum checks をどこまで executable coverage に上げるか
- planned rows をいつ promote するか
- unsupported runtime fallback をどこまで case-by-case で明文化するか
- active dependent detach を reject にするか defer にするか
- adapter target widening をどこで実行 row に上げるか

### 8.4.5 この family で言ってはいけないこと

- signed = safe
- limited adapter skeleton = full compatibility
- runtime-private naming = final public ABI
- alpha avatar floor = final avatar API
- current native-policy subset = native execution completion

## 8.5 Network / Transport

### 8.5.1 すでに決まっていること

- current Stage-C cut は narrow Docker / TCP process-boundary floor
- transport と auth / membership / capability / witness を collapse しない
- observer-safe route trace を typed surface として保つ
- production WAN / durable replay / federation を claim しない

### 8.5.2 current actualized subset

- `NET-02`
- `NET-03`
- `NET-04`
- `NET-05`
- `NET-07`
- `NET-09`

### 8.5.3 current judgment points

- Docker/TCP cut を維持するか、route rebinding / partition / medium-change へ widen するか
- transport completion の最低条件をどこに置くか
- final public transport ABI / host schema をいつ議題化するか
- replay / durable semantics を transport line に入れるかどうか

### 8.5.4 この family で言ってはいけないこと

- Docker canary = production transport
- helper/runtime-private floor = public transport ABI
- auth lane preservation = full auth protocol completion

## 8.6 Visualization / Stage E

### 8.6.1 current actualized subset

- `VIS-01`
- `VIS-02`
- `VIS-03`
- `VIS-05`
- `VIS-06`
- `VIS-07`
- `VIS-08`
- `VIS-10`
- `VIS-11`

### 8.6.2 remaining rows

- `VIS-04`
- `VIS-09`
- `VIS-12`

### 8.6.3 current judgment points

- remaining 3 rowsを何の evidence で満たすか
- final viewer API / telemetry service を freeze せずにどう closeout するか
- report-local projection / timeline and final viewer surface をどう分け続けるか

### 8.6.4 この family で言ってはいけないこと

- current subset runner = Stage E completion
- viewer prototype inventory = final viewer API
- helper-local trace/redaction inventory = telemetry service completion

## 8.7 Integrated E2E / Stage F

### 8.7.1 current actualized subset

- `E2E-01`
- `E2E-02`
- `E2E-03`
- `E2E-04`
- `E2E-05`
- `E2E-06`
- `E2E-07`
- `E2E-09`
- `E2E-10`

planned-only:

- `E2E-08`

### 8.7.2 Stage F completion condition

少なくとも次が必要。

- multiple `Place` execution
- network/container E2E
- runtime package hot-plug
- layer hot-plug
- typed debug / visualization
- placeholder/custom avatar path
- local save/load or explicit checker-backed non-claim for distributed save
- negative tests for stale membership / missing capability / missing witness / incompatible patch / invalid cut / unsigned native package reject

### 8.7.3 current judgment points

- Stage F checklist の残りをどう満たすか
- Stage E 未完了のまま Stage F closeout と誤読されないようにどう保つか
- `E2E-08` upper-layer seed を completion count に混ぜないこと

### 8.7.4 この family で言ってはいけないこと

- thin integrated bridge runner = final alpha completion
- current local save/load path = distributed save/load completion
- planned-only upper-layer seed = alpha completion evidence

## 8.8 Projection / Viewer / VerificationLayer / shared public seams

### 8.8.1 current honest reading

- projection/codegen current `equivalence` reading は review-category alignment inventory に留まる
- generated place-program synthesis / optimizer / deployment planner / equivalence checker / proof / final emitted ABI は still later
- viewer は typed public prototype inventory
- `VerificationLayer` の emitted rows / previews / downstream consumers は current surfaces だが final public verifier contract ではない

### 8.8.2 current judgment points

- exact public `VerificationLayer` law surface
- theorem-bridge/runtime-policy widening relation
- projection equivalence evidence boundary
- public emitted-program contract
- witness/provider schema and emitted-handoff contract
- order-handoff source wording and emitted-artifact schema

## 9. いま一番危険な誤読

### 9.1 snapshot docs を normative source と誤読すること

- `progress.md`
- `tasks.md`
- `samples_progress.md`

は authority ではなく queue mirror / dashboard。

### 9.2 helper-local floor を implementation completion と誤読すること

特に危険なのは:

- acceptance floor
- checker floor
- projection equivalence
- viewer prototype
- runtime-private hot-plug names

### 9.3 historical helper 名を live command と誤読すること

active compatibility front door は `list / smoke-all / closeout` に限定されている。
historical `lane` / `reserve` / `reopen-map` / `emit-*` memory は live command claim へ戻してはいけない。

### 9.4 `U1` option inventory を actual commitment と誤読すること

`U1` readiness wording はあるが、
actual decision 自体はまだ行われていない。

### 9.5 `samples/alpha/` の runner-backed subset を active root と誤読すること

runner-backed row が増えても、

- active root 昇格
- Stage E completion
- Stage F completion
- production claim

は自動では成立しない。

## 10. autonomous に進めるときの実用ルール

### 10.1 迷ったら最初に確認する 8 点

1. これは `specs/` に already decided か
2. これは `plan/` に kept-later と書かれていないか
3. これは `progress.md` / `tasks.md` の current queue と矛盾しないか
4. これは `samples/alpha/` を active root へ誤昇格しないか
5. これは helper-local / runtime-private / public を混ぜないか
6. これは transport と auth/membership/capability/witness を collapse しないか
7. これは local-only evidence を distributed completion と誤読しないか
8. これは user-facing `U1` decision を勝手に固定しないか

### 10.2 そのまま進めてよいケース

- docs freshness / taxonomy sync
- validation freshness rerun
- source hierarchy / report schema maintenance
- report-only blocker clarification
- already-admitted floor の focused test hardening
- existing non-public runner の regression hardening

### 10.3 一度止まるべきケース

- new positive row が current acceptance schema に入るか怪しい
- parser/runtime bridge に見える変更になりそう
- distributed save/load を言いたくなった
- detach / migration / distributed activation ordering に踏み込みそう
- public API / ABI / host schema / packaging naming を決めたくなった
- native package execution を widen したくなった
- VRM / VRChat / Unity を core semantics に寄せたくなった
- PrismCascade を alpha implementation scope に混ぜたくなった

### 10.4 いま最も安全な next-step の読み

現状の safest reading は次である。

- repo は maintenance と narrow theory/runtime-prep を継続できる
- ただし safe な `P-A0-18` は未promote
- 次の本格 widening は
  `remaining positive rows に対する next-carrier decision`
  を経ないと危険
- user-facing product adoption line は `U1` 待ち

## 11. 直近の validation / evidence anchor

### 11.1 repo-wide current floor

```bash
python3 scripts/check_source_hierarchy.py
python3 scripts/validate_docs.py
python3 scripts/current_l2_source_sample_regression.py inventory
python3 scripts/current_l2_source_sample_regression.py regression --run-label <label> --artifact-root <root>
python3 scripts/current_l2_guided_samples.py closeout --format json
python3 scripts/clean_near_end_samples.py closeout
python3 scripts/current_l2_lean_sample_sync.py
python3 scripts/sugoroku_world_samples.py closeout --format json
python3 scripts/avatar_follow_samples.py closeout --format json
python3 scripts/typed_external_boundary_samples.py closeout --format json
python3 scripts/network_transport_samples.py check-all --format json
python3 scripts/projection_codegen_samples.py check-all --format json
python3 scripts/projection_codegen_samples.py closeout --format json
python3 scripts/visual_debugger_viewer_samples.py closeout --format json
cargo fmt --check
git diff --check
```

### 11.2 alpha-local relevant floor

```bash
python3 -m unittest \
  scripts.tests.test_alpha_lifetime_fallback_checker \
  scripts.tests.test_alpha_contract_variance_checker \
  scripts.tests.test_alpha_lifetime_fallback_acceptance \
  scripts.tests.test_alpha_contract_variance_acceptance \
  scripts.tests.test_alpha_cut_save_load_checker

cargo test -p mirrorea-core --test runtime_substrate
cargo test -p mir-runtime --test alpha_cut_save_load_runtime
python3 scripts/alpha_cut_save_load_samples.py check-all --format json
cargo test -p mir-runtime --test alpha_local_runtime
cargo test -p mir-runtime --test alpha_layer_insertion_runtime
cargo test -p mir-runtime --test alpha_network_runtime
python3 scripts/alpha_network_docker_e2e.py check-all --format json
cargo test -p mir-runtime --test alpha_avatar_runtime
python3 scripts/alpha_avatar_runtime_samples.py check-all --format json
python3 scripts/alpha_visualization_samples.py check-all --format json
python3 scripts/alpha_e2e_samples.py check-all --format json
```

## 12. 最後に残る一文

この repo は「何も決まっていない」状態ではない。
むしろ、

- invariant
- hierarchy
- non-claims
- sample taxonomy
- alpha-local accepted subset
- user-facing blocker list

はかなり強く固定されている。

本当に迷うのは、

- helper-local carrier でまだ honest に進められるか
- runtime/remote/layer/adapter semantics が新たに必要か
- それとも product-shaping decision を user に返すべきか

の三択である。

以後の autonomous progress は、この三択を誤らないことが最重要である。
