# FAQ 014 — 現状と今後の判断 inventory（`P-A0-19` 後）

- 作成日時: 2026-05-02 21:54:29 JST
- 目的:
  repo の現状、いま何が actualize 済みか、何が blocker か、今後 autonomous に進める際にどこで判断に迷いやすいかを、source hierarchy を崩さずに 1 つへ集約する。
- 想定読者:
  この repo に途中参加した agent / reviewer / 実装者 / 将来の自分。
- 重要:
  この文書は **判断補助用の FAQ / dossier** であり、規範正本ではない。
  矛盾時の優先順位は次である。
  `specs/` > `plan/` > `progress.md` / `tasks.md` / `samples_progress.md` > `docs/reports/` > `Documentation.md` / `README.md` > この FAQ

## 1. 最初に押さえるべき結論

### 1.1 この repo は何を達成済みで、何をまだ達成していないか

- 達成済みの主眼は **repo-local alpha-ready current layer** である。
- 達成していないのは **final public product** である。
- つまり現状は「docs-only 構想メモ」ではないが、「public parser / public checker / public runtime / public verifier 完成品」でもない。
- active clean suite、current-L2 base corpus、Lean evidence、Sugoroku world、avatar follow、typed external preview、network canary、projection/codegen bridge evidence、viewer prototype inventory、hot-plug narrow Rust floor、そして Mirrorea Spaces alpha-0 の non-public helper/runtime floors までは repo 内で再検証できる。
- ただし final grammar、public API、public ABI、production transport、production auth、durable distributed save/load、final viewer/verifier contract、installed binary / packaging target は未完である。

### 1.2 active root / active evidence root は何か

- active canonical executable suite:
  `samples/clean-near-end/`
- active base source corpus:
  `samples/current-l2/`
- active mechanization evidence:
  `samples/lean/`
- mixed alpha-local scaffold / non-public runner root:
  `samples/alpha/`
- residual planned family root:
  `samples/not_implemented/`
- generated bridge evidence root:
  `samples/generated/`
- archive:
  `samples/old/`

重要:

- `samples/alpha/` は **active runnable root ではない**。
- `samples/not_implemented/` は archive ではなく **planned family** である。
- `samples/generated/` は source sample ではなく **generated evidence** である。
- `samples/current-l2/` は active executable suite ではなく、active clean suite を支える **base corpus** である。

### 1.3 Mirrorea Spaces alpha line の現在地

- current concrete phase:
  `Phase 8 — integrated alpha demo closeout`
- large stage percentages:
  `Stage B 40%`, `Stage C 35%`, `Stage D 40%`, `Stage E 60%`, `Stage F 60%`
- last closed package:
  `P-A0-19`
- current reopen point:
  **safe な `P-A0-20` はまだ promoted していない**

この current alpha lane で最重要の事実:

- `LIF-01/05..08` と `VAR-02/03/05/07/09/10/15` は
  `reason_codes_scope = alpha-static-floor` の negative synthetic checker floor
- `LIF-02/03/04` と `VAR-01/04/06` は
  `acceptance_scope = alpha-acceptance-floor` の helper-local synthetic acceptance floor
- `VAR-08/11/13` は
  `runtime_mirror.scope = alpha-runtime-mirror-floor` の runtime-mirror floor
- この 3 つは **別 carrier**
- positive row の証拠は「negative reason code が無いこと」ではなく、
  **explicit acceptance row** または **explicit runtime-mirror row** が一致することである
- これは parser/runtime bridge でも public checker verdict でも runnable root promotion でもない

### 1.4 いま autonomous に進める最大の制約は何か

- `P-A0-19` は **remaining positive rows の row-specific blocker inventory** を固定した package であり、新しい actualization package ではない。
- 残っている positive rows のうち、
  `LIF-11/13/15` と `VAR-14`
  は現 acceptance floor と現 runtime-mirror floor のままでは安全に widen できない。
- したがって next step は次のどちらかでなければならない。
  1. 既存 carrier のままで 1 row だけ安全に入ることを証明する
  2. overclaim しない **より狭い dedicated carrier** を切る

### 1.5 `U1` は何か

- `U1` は actual product-shaping gate であり、alpha-local package line とは分離されている。
- alpha packages を閉じても `U1` は閉じない。
- packaging / host target / first shipped public surface / final catalog breadth などは依然として user-facing decision である。

## 2. この FAQ の使い方

### 2.1 迷ったら最初に見る順番

1. `README.md`
2. `Documentation.md`
3. `progress.md`
4. `tasks.md`
5. `samples_progress.md`
6. `specs/00-document-map.md`
7. `specs/01-charter-and-decision-levels.md`
8. `specs/02-system-overview.md`
9. `specs/03-layer-model.md`
10. `specs/09-invariants-and-constraints.md`
11. 問題に関係する subsystem spec
12. `plan/00-index.md`
13. 関係する `plan/*`
14. 直近 report

### 2.2 どの文書が何を決めるか

- `specs/`
  規範正本
- `plan/`
  repository memory
- `progress.md`
  rough status snapshot
- `tasks.md`
  current task map / blocker map
- `samples_progress.md`
  runnable / non-runnable sample dashboard
- `docs/reports/`
  作業証跡
- `tmp_faq/faq_014.md`
  判断補助の集約メモ

### 2.3 この FAQ の分類

この文書では判断点を次の 4 種類に分ける。

- `決定済み`
  repo がすでに守るべきと fix しているもの
- `repo が research / discovery で絞ること`
  user 入力なしでも investigation は可能だが、まだ final ではないもの
- `user が決めること`
  autonomous に固定してはいけない product-shaping choice
- `後段依存 / まだ触ると危険`
  先行 semantic carrier や runtime substrate が無いので、今 widen すると overclaim になるもの

## 3. source hierarchy と読み違えやすい基本原則

### 3.1 source hierarchy

- `specs/` = 規範正本
- `plan/` = repository memory
- `progress.md` / `tasks.md` = current snapshot
- `samples_progress.md` = sample dashboard
- `docs/reports/` = evidence
- `tmp_faq/` = decision helper

### 3.2 `progress.md` は rough snapshot であり規範ではない

- `progress.md` は queue authority と validation freshness を読む文書である。
- 進捗率や stage % は **rough estimate** であり、規範判断の正本ではない。
- ただし current line を誤読しないために、large stage map の percentage は explicit に維持されている。

### 3.3 `samples_progress.md` の % は evidence-backed である

- conceptual-only row は `25%` を超えてはいけない。
- `100%` は current scope において implementation、sample、debug/visualization、docs、report、tests、commit/push まで完了したときだけ使う。
- `samples/alpha/` family の多くは 100% ではなく、family 内 subset actualization と blocker coexist を明示している。

### 3.4 report は作業証跡であり、repo memory の代替ではない

- report は「その task で何をどう検証し、何を claim しなかったか」を残す。
- 直近判断の背景説明として重要だが、長期判断の正本は `plan/` と `specs/` に戻る。

## 4. すでに固定されている repo-wide guardrails

### 4.1 stack は separable のまま保つ

これは決定済み。

- Mir
- Mirrorea
- PrismCascade
- Typed-Effect Wiring Platform

は関連するが、1 つの runtime に collapse してはいけない。

特に:

- PrismCascade は Mir の sub-runtime ではなく **独立 kernel**
- Typed-Effect Wiring Platform は Mir core そのものではない
- Mirrorea alpha scope に PrismCascade 実装を持ち込まない

### 4.2 Mir core に standard I/O を入れない

これも決定済み。

- `stdin` / `stdout` / `stderr` を privileged core primitive にしない
- external world connection は typed external effect / adapter boundary に残す
- auth / membership / capability / witness / visualization / telemetry を transport に潰さない

### 4.3 `Place` を participant と同一視しない

`Place` は execution locus であり、少なくとも次を持つ読みが current line で固定されている。

- state
- queue
- capability
- visibility
- observation frontier

### 4.4 helper-local preview を public API と読まない

次は **non-final** である。

- helper-local preview
- report-local inventory
- generated bridge evidence
- runtime-private carrier names
- viewer prototype inventory
- projection/codegen current `equivalence` reading
- helper-local checker floor
- helper-local acceptance floor
- runtime-mirror floor

### 4.5 `samples/alpha/` を runnable root と読まない

`samples/alpha/` は mixed scaffold であり、family ごとに

- scaffold-only
- checker-seed
- helper-local acceptance floor
- runtime-mirror floor
- runner-backed non-public Rust floor

が混在している。

存在しているだけでは runnable でも active root でもない。

## 5. invariant と stop line

### 5.1 invariant 群

次は repo の invariant として扱う。

- semantic event structure は DAG
- hidden backward edge を既定で許さない
- downstream addition が既定
- silent API shadowing は禁止
- compatibility-preserving overlay は hidden narrowing をしない
- lifetime degradation は monotone
- rollback は finalizing cut を越えない
- linear resource duplication を許さない
- visualization / telemetry は typed information-bearing effect
- distributed save/load without consistent cut は禁止

### 5.2 Alpha-0 ですでに固定された特記事項

- fallback は **target object の lifetime を延長しない**
  guarded logical access path の availability を延ばすだけである
- layer insertion は
  precondition strengthening、guarantee weakening、undeclared effect/failure widening、observation-label leak を許さない
- signature は provenance を示すだけであり、semantic safety を証明しない
- avatar / VRM / VRChat / Unity は Mir core primitive ではない
- parser/runtime executed sample と helper-local synthetic evidence を混同しない

### 5.3 現在の stop line

current line で明示的に claim してはいけないもの:

- final public parser grammar
- final public parser / checker / runtime / verifier API
- final public theorem / model-check / witness-provider contract
- final public auth / visualization / projection / hot-plug / transport API
- production WAN federation
- durable distributed save/load completion
- durable migration / distributed activation ordering completion
- final public layer-attachment ABI
- final public avatar/package API
- native binary semantic safety
- Reversed Library completion
- PrismCascade integration

## 6. repo-wide current executable floor

### 6.1 active clean suite

active canonical executable suite は `samples/clean-near-end/` であり、主要 family は次である。

- `typing/`
  finite-index first strong typing layer
- `order-handoff/`
  publication / witness / handoff relation family
- `model-check/`
  Peterson / broken mutex による second-line verification
- `modal/`
  `stable` / `later` / `published(room)` / `witnessed(...)` の current mode line
- `sugoroku-world/`
  empty world server へ SugorokuGame を attach する repo-local logical multi-place emulator
- `avatar-follow/`
  representative slice
- `network-transport/`
  helper-local canary family

### 6.2 current-L2 の principal split

current line が principal にするもの:

- finite decidable index fragment first
- high-level order / handoff relation first
- model-check second line for weak-memory / mutex

current line が principal にしないもの:

- low-level `memory_order_*` exact source principal
- full dependent type source principal
- final public theorem / verifier contract

### 6.3 Lean / theorem / model-check floor

- `samples/lean/foundations/`
  small proof fragments
- `samples/lean/clean-near-end/`
  generated theorem stubs

重要:

- generated stub は **proof completion の完了宣言ではない**
- current mechanization spine は存在するが、全 sample の domain proof discharge 完了ではない

### 6.4 Mirrorea carrier / runtime floor

current repo で actualize 済みの代表 carrier / substrate:

- `TermSignature`
- `LayerSignature`
- `MessageEnvelope`
- `AuthEvidence`
- `verification_handoff_witness`
- `verification_model_check`
- `MembershipRegistry`
- `PlaceCatalog`
- `LogicalPlaceRuntimeShell`
- `HotPlugRequest`
- `HotPlugVerdict`
- `HotPlugRuntimeSkeletonReport`
- `HotPlugRuntimeEngineReport`

### 6.5 helper preview / generated bridge evidence floor

- typed external `EXT-03/04` preview
- network `NET-02..05` canary
- projection/codegen manifest bridge evidence
- viewer typed prototype inventory
- storage / LLVM guardrail

注意:

- projection/codegen current `equivalence` reading は
  committed generated manifest と helper/report-local anchor の review-category alignment inventory であり、
  generated place-program synthesis / optimizer / deployment planner / cross-place equivalence checker / proof completion / final emitted ABI ではない

### 6.6 storage / build guardrail

決定済み:

- heavy disposable artifact は external workdir を優先する
- default candidate は `/mnt/mirrorea-work` だが、毎回 mount / capacity を probe する
- cleanup は known disposable dir に限り、`--confirm` など explicit confirmation を要求する
- `llvm/src` は cleanup 対象から除外する

current guardrail surface:

- `bash scripts/env/mirrorea_storage_env.sh --ensure-dirs`
- `bash scripts/storage/detach_prepare.sh`
- `bash scripts/storage/cleanup_disposable_artifacts.sh --list`

## 7. Alpha-0 の current actualization map

### 7.1 LIF / VAR carrier family の current state

#### negative checker floor

- `LIF-01/05/06/07/08`
- `VAR-02/03/05/07/09/10/15`

これは sidecar の `expected_static.checked_reason_codes` と synthetic detached artifact の
`detached_noncore.reason_codes` を比較する non-public helper floor である。

重要:

- scope は `reason_codes_scope = alpha-static-floor`
- parser/runtime bridge ではない
- final public checker verdict ではない

#### helper-local acceptance floor

- `LIF-02`
  `fallback_chain_canonicalized`
- `LIF-03`
  `inherited_chain_spliced_with_lineage`
- `LIF-04`
  `plain_ref_boundary_preserved`
- `VAR-01`
  `transparent_observe_only_layer`
- `VAR-04`
  `output_covariance_checked`
- `VAR-06`
  `readonly_covariance_checked`

これは sidecar の `expected_acceptance.checked_acceptance_rows` と synthetic detached artifact の
`detached_noncore.acceptance_rows` を比較する non-public helper floor である。

重要:

- scope は `acceptance_scope = alpha-acceptance-floor`
- positive evidence は explicit row の一致で判断する
- negative reason code が無いことは positive proof ではない
- parser/runtime bridge ではない
- final public checker verdict ではない

#### runtime-mirror floor

- `VAR-08 <- LI-04`
- `VAR-11 <- LI-01`
- `VAR-13 <- LI-03`

これは target sidecar の `runtime_mirror` と source runtime-floor sidecar の
`status` / `claims` / `current_validation.mode` / `mirrors` / `expected_runtime`
を照合する non-public helper floor である。

重要:

- scope は `runtime_mirror.scope = alpha-runtime-mirror-floor`
- `contract-variance/` 自体が runnable root になったわけではない
- `VAR-*` sample 自体が parser/runtime execution されたわけではない
- final public layer checker / ABI 完了ではない

### 7.2 local runtime floor

actualized:

- `LR-01`
- `LR-02`

current reading:

- in-memory local runtime cut
- local queue
- `MessageEnvelope` dispatch
- membership freshness rejection
- report-local event DAG export hook

non-claim:

- distributed runtime
- parser front door
- final public runtime API

### 7.3 layer insertion floor

actualized:

- `LI-01`
  accepted debug attach with redacted trace
- `LI-02`
  non-admin attach rejected
- `LI-03`
  explicit auth contract update path
- `LI-04`
  declared-failure rate-limit path
- `LI-05`
  incompatible patch reject

current reading:

- attach-time contract comparison over narrow exact floor
- Stage-D first cut only

non-claim:

- completed lifecycle / detach / migration
- distributed ordering
- final public ABI

### 7.4 network / Docker floor

actualized:

- `NET-02/03/04/05/07/09`

current reading:

- non-public Rust Stage-C network floor
- local TCP process-boundary / Docker Compose bridge
- explicit membership/capability/witness/auth admission checks
- observer-safe route trace
- auth-evidence lane preserved separately from transport delivery

non-claim:

- production WAN federation
- replay service
- final transport ABI

### 7.5 avatar-runtime / hotplug-runtime floor

actualized:

- `AV-01/02/06/08/09`
- `HP-11/12/15`

current reading:

- runtime-private package/avatar manifest-admission floor
- placeholder/custom avatar acceptance
- unsupported runtime fallback
- undeclared-effect reject
- unsigned / over-capability / revoked native package reject

non-claim:

- full VRM / VRChat / Unity compatibility
- final avatar API
- native execution completion
- final runtime package ABI

### 7.6 cut / save-load floor

actualized:

- runtime-backed:
  `CUT-04`, `CUT-17`
- checker-backed:
  `CUT-05/07/08/09/11/13/14/15`

current reading:

- `CUT-04`
  local-only save/load bridge
- `CUT-17`
  local stale-membership non-resurrection bridge
- `CUT-11`
  Z-cycle / useless checkpoint inadmissibility

non-claim:

- distributed save/load runtime
- durable cut completion
- communication-induced checkpoint repair

### 7.7 visualization floor

actualized:

- `VIS-01/02/03/05/06/07/08/10/11`

current reading:

- dedicated Stage-E subset runner
- report-local / helper/runtime JSON evidence composition
- event DAG, place-catalog projection, route trace, membership epoch/incarnation timeline, hot-plug lifecycle bundle, fallback degradation, observer-redacted trace, on-demand trace gate, retention-floor enforcement

non-claim:

- `VIS-04/09/12`
- Stage E completion
- final public viewer / telemetry API

### 7.8 integrated E2E floor

actualized:

- `E2E-01/02/03/04/05/06/07/09/10`

current reading:

- thin integrated Stage-F bridge
- composition of already-existing Stage B/C/D/E subset floors
- `E2E-06` is backed by the local-only `CUT-04` save/load path

non-claim:

- `E2E-08`
- Stage F completion
- dedicated alpha devtools completion

## 8. current validation freshness の正しい読み

### 8.1 latest alpha-specific package closeout freshness

latest alpha-specific gate currently fixed in snapshot docs:

- `2026-05-02 19:34 JST`
- package:
  `P-A0-19`
- validation:
  `python3 -m unittest scripts.tests.test_alpha_lifetime_fallback_checker scripts.tests.test_alpha_lifetime_fallback_acceptance scripts.tests.test_alpha_contract_variance_checker scripts.tests.test_alpha_contract_variance_acceptance scripts.tests.test_current_l2_family_runtime_mirror_support scripts.tests.test_alpha_contract_variance_runtime_mirror scripts.tests.test_validate_docs`
  passed `48` tests
- plus:
  `python3 scripts/check_source_hierarchy.py`
  `python3 scripts/validate_docs.py`
  `cargo fmt --check`
  `git diff --check`

これは **blocker inventory sync** の freshness であり、
new acceptance widening / new runtime-mirror widening / parser/runtime execution / runtime-package completion の freshness ではない。

### 8.2 latest broad repo-wide freshness checkpoint

latest broad full-floor checkpoint currently fixed in snapshot docs:

- `2026-05-01 14:27 JST`

これは current-L2, clean-near-end, Lean sync, Sugoroku, avatar, typed external, network canary, projection, viewer, storage guardrail, Cargo crate tests を含む broad checkpoint である。

重要:

- current docs-only task がこれを自動的に更新するわけではない
- docs-only closeout では broad runtime rerun を claim しない

## 9. いま迷わなくてよいこと

### 9.1 `P-A0-20` はまだ存在しない

- 「次に何か package 名を振って進める」こと自体が goal ではない。
- safe な row-specific actualization がまだ見つかっていないので、`P-A0-20` は未promoteで正しい。

### 9.2 acceptance floor を広げるべきか

- 現時点では **広げない** が current reading である。
- `P-A0-17` で acceptance floor は
  `LIF-02/03/04`
  `VAR-01/04/06`
  に限定して閉じている。

### 9.3 parser/runtime bridge を作るべきか

- 現時点では **作らない** が current default である。
- いま必要なのは row-specific narrowing であり、generic parser/runtime bridge は scope が広すぎる。

### 9.4 `contract-variance/` を runnable root と見なすべきか

- 見なしてはいけない。
- `VAR-08/11/13` は runtime-mirror rows であって、family runner actualization ではない。

## 10. いま迷うべき論点の全体地図

### 10.1 現在の primary reopen point

現時点の primary reopen point は 1 つだけである。

- `P-A0-19` inventory から、first row-specific actualization を 1 つ選ぶ

候補 row:

- `LIF-11`
- `LIF-13`
- `LIF-15`
- `VAR-14`

### 10.2 secondary blocker 群

同時に snapshot 上で pending と明示されているもの:

- `CUT-10/12/16`
- `VIS-04/09/12`
- parser-front-door execution for `samples/alpha/`
- broader runtime/public boundary lines

### 10.3 user-facing gate 群

repo が勝手に閉じてはいけないもの:

- `U1` actual commitment
- packaging shape
- host target
- first shipped public surface
- final shared-space operational catalog breadth

## 11. row-specific blocker inventory の詳細

### 11.1 `LIF-11` bird / sparkle anchor inheritance

現在の status:

- **未actualize**
- `P-A0-19` で blocker inventory 化のみ

必要なもの:

- anchor / deletion outcome semantics
- deletion 後にどの anchor が残るか
- inherited chain と selected anchor の相互関係

なぜ current acceptance floor に入らないか:

- `fallback_chain_canonicalized`
- `inherited_chain_spliced_with_lineage`
- `plain_ref_boundary_preserved`

の 3 kind だけでは、anchor deletion 後の semantic outcome を十分に表せない。

いま research で絞れること:

- deletion outcome を purely helper-local row で narrow に表せるか
- それとも local runtime / snapshot / lineage substrate を要求するか

今まだ user に聞かずに決めてはいけないこと:

- final public anchor API
- final parser syntax for this row

current recommendation:

- `LIF-11` は row-specific dedicated carrier が必要という理解を保ち、現 acceptance floor に混ぜない

### 11.2 `LIF-13` snapshot selected anchor

現在の status:

- **未actualize**
- `P-A0-19` で blocker inventory 化のみ

必要なもの:

- selected-option snapshot semantics
- snapshot time と later fallback-chain mutation の切り分け
- `FriendHead>A` と `FriendHead>Shoulder>A` の distinction を保持する carrier

なぜ current acceptance floor に入らないか:

- canonical chain の一致だけでは
  「どの selected path が snapshot されたか」を十分に保証できない

いま research で絞れること:

- snapshot-selected row を pure helper-local row として narrow actualize できるか
- それとも explicit snapshot carrier / store surface が必要か

current recommendation:

- remaining LIF rows の中では比較的 narrow に切れる可能性があるが、
  まだ安全 actualization と断定しない

### 11.3 `LIF-15` remote read-only covariance

現在の status:

- **未actualize**
- `P-A0-19` で blocker inventory 化のみ

必要なもの:

- remote freshness / membership / frontier carrier
- observed remote access path と stale membership / frontier advance の関係
- remote read-only covariance の admissibility condition

なぜ current acceptance floor に入らないか:

- local helper-local acceptance rows は remote frontier semantics を持たない
- current runtime-mirror floor も layer-insertion source authorityに限定されており、remote observed path を支えない

なぜ今すぐ runtime package 側へ逃がしてはいけないか:

- それは remote semantics の generic widening になりやすく、
  current request の narrow row-specific actualization を超える

current recommendation:

- remaining LIF rows の中では最も runtime / remote 依存が強く、優先 actualization 候補としては慎重

### 11.4 `VAR-14` adapter transform preserves contract

現在の status:

- **未actualize**
- `P-A0-19` で blocker inventory 化のみ

必要なもの:

- adapter-target contract-preservation carrier
- source contract と transformed target contract の preservation law
- postcondition / provided surface / effect/failure containment / observation/redaction/retention / compatibility claim / fallback representation の整列

なぜ current acceptance floor に入らないか:

- helper-local positive rows は local type/variance law だけを扱っており、
  adapter target transformation を含まない

なぜ current runtime-mirror floor に入らないか:

- 既存 `LI-*` runtime floor は attach-time layer rows を actualize しているだけで、
  adapter transform preservation を source authority として持たない

どこに絡むか:

- `specs/14`
- `specs/16`
- `plan/40`
- `plan/42`

current recommendation:

- `VAR-14` は adapter-boundary dedicated carrier を要し、
  acceptance/runtime-mirror wideningの延長では扱わない

## 12. CUT-local blocker inventory

### 12.1 `CUT-10` load does not resurrect expired lease

現在の status:

- deferred

必要なもの:

- saved carrier に lease store / expiry frontier を持つこと
- restore 後に resurrect しないことを観測できる dedicated substrate

なぜ今 actualize しないか:

- current save/load cut は local savepoint + membership stale rejection まではあるが、
  explicit lease store semantics までは actualize していない

### 12.2 `CUT-12` communication-induced checkpoint repair

現在の status:

- deferred

必要なもの:

- communication-induced checkpoint protocol state
- in-flight channel-state representation
- distributed / protocol-aware repair semantics

なぜ今 actualize しないか:

- これは local-only save/load cut を超えた protocol semantics を要求する

### 12.3 `CUT-16` load does not resurrect stale witness

現在の status:

- deferred

必要なもの:

- witness store
- restore 後 witness frontier を追跡する carrier

なぜ今 actualize しないか:

- current runtime save/load carrier は membership stale line までは支えているが、
  explicit witness store / non-resurrection proof をまだ持たない

### 12.4 CUT lane の current recommendation

- `CUT-10/12/16` は save/load substrate package を明示的に reopen しない限り据え置きでよい
- current snapshot の recommendation どおり、first next package は CUT ではなく row-specific positive-row carrier review を優先するのが安全

## 13. Stage-E / Stage-F blocker inventory

### 13.1 `VIS-04/09/12`

現在の status:

- planned-only
- Stage-E subset runner の外に残している

current meaning:

- 現 visualization runner は widened subset だが family completion ではない
- `VIS-04/09/12` を残すことで Stage E completion non-claim を honest に保っている

なぜ今すぐ埋めないか:

- devtools / viewer / telemetry の final public directionと混ざりやすい
- remaining positive-row carrier review と別 lane で扱う方が安全

### 13.2 `E2E-08`

現在の status:

- planned-only upper-layer seed

current meaning:

- `alpha_e2e_samples.py` は thin integrated bridge runner であり、
  upper-layer realization completion ではない

### 13.3 Stage E / Stage F の completion 読み

- Stage E 60% は
  widened subset runner + planned residual rows がある状態
- Stage F 60% は
  thin integrated bridge + local save/load path + planned residual rows がある状態

どちらも completion ではない。

## 14. parser/runtime front-door execution についての判断

### 14.1 いま存在するもの

- `samples/alpha/` の identity anchor
- `.expected.json` sidecar
- helper-local checker floor
- helper-local acceptance floor
- runtime-mirror floor
- family-specific thin Rust/runtime-private runner

### 14.2 いま存在しないもの

- `samples/alpha/*.mir` を parser front door から execute する general runner
- final public parser grammar
- final public checker API
- family-wide contract-variance runtime

### 14.3 いま parser/runtime bridge を作らない理由

- scope が row-specific blocker review を超える
- final public parser/runtime claim と誤読されやすい
- current alpha lane で必要なのは row-specific honest widening であり、generic front-door expansion ではない

## 15. user decision blockers

### 15.1 packaging shape / installed binary target

- overview:
  first public shape を `CLI` / `library` / `engine-adapter` / `hybrid` のどれで取るか
- affects:
  `P18`, `U1`, backend / storage / distribution, future public API shape
- current recommendation:
  `library-first`
- reason:
  current shell / helper actualization は installed binary adoption fact ではない

### 15.2 host integration target

- overview:
  `browser` / `native process` / `engine` / `mixed`
- affects:
  typed external boundary, network transport, viewer, engine ABI, packaging
- current recommendation:
  `native process`
- reason:
  current evidence が same-process / process-boundary helper に最も近い

### 15.3 first shipped public surface scope

- overview:
  `parser/checker/runtime/verifier first`
  `adapter/viewer/projection/hot-plug first`
  `two-step split`
- affects:
  final public parser/API, viewer, adapter, projection, hot-plug ABI
- current recommendation:
  `two-step split`
- reason:
  public core と integration surface は依存関係が異なる

### 15.4 final shared-space operational catalog breadth

- overview:
  `minimal subset`
  `portal / multi-world expansion`
  `fairness / quorum / exhaustive catalog`
- affects:
  Sugoroku, avatar, network transport, hot-plug migration/replay, application realization
- current recommendation:
  `minimal subset`

### 15.5 first network scope

- overview:
  local-only か、Docker / local-subprocess か、WAN/federation か
- affects:
  transport trait shape, validation floor, non-claim wording
- current recommendation:
  `Docker / local-subprocess first`
- note:
  narrow Stage-C floor は actualize 済みだが、WAN/federation widening は kept-later

### 15.6 avatar compatibility first target

- overview:
  placeholder-only, custom Mir avatar runtime, VRM skeleton, VRChat-compat skeleton
- current recommendation:
  `placeholder + custom Mir runtime first`
- note:
  VRM / VRChat は non-core skeleton として kept-later

### 15.7 native binary policy

- overview:
  forbidden, sandboxed/trust-policy limited, stronger provenance policy
- current recommendation:
  `sandboxed / trust-policy limited`
- reason:
  signature alone is not semantic safety

### 15.8 save/load initial scope and UI target

- overview:
  local-only か multi-Place か、CLI か HTML viewer か richer client か
- current recommendation:
  `local-only save/load + HTML/JSON viewer first`
- reason:
  current viewer evidence と local-only bridge がこれに最も近い

## 16. research-discovery items

`tasks.md` が current snapshot として挙げている research-discovery items を、そのまま重要度付きで読む。

### 16.1 verification / theorem / checker seam

- exact public `VerificationLayer` law surface
- public naming
- theorem-bridge/runtime-policy widening relation
- composition contract among emitted rows / evidence carriers / downstream consumers / emitted verifier handoff artifacts

### 16.2 `FAIRY-05`

- reopen criteria
- final implementation carrier
- negative companion shape

### 16.3 projection equivalence

- evidence boundary
- future checker relation
- public emitted-program contract beyond committed manifest bridge

### 16.4 witness/provider seam

- public-shape route
- schema candidate
- combined-contract candidate
- final emitted-handoff boundary

### 16.5 order-handoff final seam

- source-wording route
- emitted-artifact schema candidate
- final source-surface wording / emitted-handoff / public witness-provider residual boundary

### 16.6 rollback / migration / activation ordering

- rollback / durable migration engine state machine after `P21`
- distributed activation ordering evidence once multi-place pressure is available

### 16.7 production transport / replay

- preserve transport / auth / membership / capability / witness separation
- do not collapse them into socket/protocol convenience

## 17. autonomous に進めるときの package promotion 判断基準

### 17.1 package を promote してよい最低条件

次を満たさない限り、新しい package を promote しない方がよい。

1. actualize 対象 row / surface が narrow である
2. 既存 carrier に honest に収まるか、または新 carrier が narrow に定義できる
3. parser/runtime completion や public API completion と誤読されない
4. validation command を current repo 内で実行できる
5. snapshot docs / plan / report まで同期できる

### 17.2 新 carrier を切る前に確認する質問

1. その carrier は `reason_codes` と `acceptance_rows` と `runtime_mirror` のどれとも混ざらないか
2. scope confinement は明示できるか
3. source authority はどこにあるか
4. absence-of-negative を positive proof と誤読させないか
5. current sample root を runnable root へ silently promote しないか
6. final public completion を claim しない wording にできるか

### 17.3 現時点での next-package recommendation

- `P-A0-20` は未promoteのままでよい
- 先にやるべきは
  1 row だけを選んだ carrier feasibility review
- 候補比較の current reading:
  - `LIF-13` は snapshot-local semantics なので相対的に narrow 候補に見える
  - `LIF-11` は anchor/deletion outcome semantics を要し、やや重い
  - `LIF-15` は remote frontier semantics を要し、かなり重い
  - `VAR-14` は adapter-boundary law を要し、layer/runtime/package を跨ぐため重い

これは **suggestion** であり、規範判断ではない。

## 18. よく迷うポイントと正しい答え

### 18.1 `claims.runnable = true` は active root か

- いいえ。
- dedicated Rust test/example があることを示す current package-local claim であり、
  `samples/alpha/` 全体や family 全体の active root promotion を意味しない。

### 18.2 `runtime_mirror` があるなら `VAR-*` を runtime 実行したことになるか

- ならない。
- source authority は `LI-*` 側にある。

### 18.3 acceptance row があるなら parser/runtime 実装済みか

- そうではない。
- helper-local synthetic evidence である。

### 18.4 `Stage F 60%` は alpha demo 完成か

- 完成ではない。
- thin integrated bridge runner と partial floors があるだけで、
  remaining Stage-E rows と distributed save/load completion などが未完である。

### 18.5 `samples/alpha/*.mir` は実行ソースか

- current line では source-ish anchor であり、
  family-specific runner が identity / intended scenario として参照するものが多い。
- generic parser front door ではまだ使っていない。

### 18.6 report-local inventory / generated bridge evidence は source sample か

- 違う。
- generated bridge evidence と source sample は明確に分ける。

### 18.7 network の executable anchor は `closeout` か

- いまの active reading では `check-all --format json` が executable canary anchor
- `closeout` は inventory evidence 寄り

### 18.8 projection の executable anchor は `closeout` か

- `check-all --format json` が live alignment validation
- `closeout --format json` は manifest inventory evidence

### 18.9 `current_l2_guided_samples.py` は何を front door として持つか

- `list`
- `smoke-all`
- `closeout`

legacy bundle / reserve / emit-* commands は active claim に戻さない。

## 19. validation anchor の current map

### 19.1 docs / hierarchy

```bash
python3 scripts/check_source_hierarchy.py
python3 scripts/validate_docs.py
python3 -m unittest scripts.tests.test_validate_docs
git diff --check
```

### 19.2 active current-L2 / clean suite

```bash
python3 scripts/current_l2_guided_samples.py smoke-all --format json
python3 scripts/current_l2_guided_samples.py closeout --format json
python3 scripts/current_l2_source_sample_regression.py inventory
python3 scripts/current_l2_source_sample_regression.py regression --run-label <label> --artifact-root <root>
python3 scripts/clean_near_end_samples.py closeout
python3 scripts/current_l2_lean_sample_sync.py
```

### 19.3 representative slices

```bash
python3 scripts/sugoroku_world_samples.py closeout --format json
python3 scripts/avatar_follow_samples.py closeout --format json
python3 scripts/typed_external_boundary_samples.py closeout --format json
python3 scripts/network_transport_samples.py check-all --format json
python3 scripts/projection_codegen_samples.py check-all --format json
python3 scripts/projection_codegen_samples.py closeout --format json
python3 scripts/visual_debugger_viewer_samples.py closeout --format json
```

### 19.4 alpha-local focused floors

```bash
python3 -m unittest \
  scripts.tests.test_alpha_lifetime_fallback_checker \
  scripts.tests.test_alpha_lifetime_fallback_acceptance \
  scripts.tests.test_alpha_contract_variance_checker \
  scripts.tests.test_alpha_contract_variance_acceptance \
  scripts.tests.test_current_l2_family_runtime_mirror_support \
  scripts.tests.test_alpha_contract_variance_runtime_mirror \
  scripts.tests.test_alpha_cut_save_load_checker

cargo test -p mir-runtime --test alpha_local_runtime
cargo test -p mir-runtime --test alpha_layer_insertion_runtime
cargo test -p mir-runtime --test alpha_network_runtime
cargo test -p mir-runtime --test alpha_avatar_runtime
cargo test -p mir-runtime --test alpha_cut_save_load_runtime

python3 scripts/alpha_cut_save_load_samples.py check-all --format json
python3 scripts/alpha_visualization_samples.py check-all --format json
python3 scripts/alpha_e2e_samples.py check-all --format json
```

### 19.5 storage

```bash
df -h .
free -h
bash scripts/env/mirrorea_storage_env.sh --ensure-dirs
bash scripts/storage/detach_prepare.sh
bash scripts/storage/cleanup_disposable_artifacts.sh --list
```

## 20. いま autonomous に進めるときの practical recommendation

### 20.1 safe に続行できるもの

- docs freshness audit
- snapshot drift repair
- source hierarchy / report-template / validate-docs guardrail maintenance
- focused validation rerun
- blocker inventory の精密化
- row-specific carrier feasibility memo
- report / FAQ / roadmap memory maintenance

### 20.2 すぐにやると危険なもの

- acceptance floor の安易な widening
- runtime-mirror floor の一般化
- parser/runtime bridge の generic actualization
- `samples/alpha/` の runnable-root promotion
- `VAR-14` を layer-insertion mirror の延長として扱うこと
- `LIF-15` を local helper row だけで remote semantics まで証明したことにすること
- Stage E / Stage F の completion 率を 100% にすること

### 20.3 current default stance

- `P-A0-19` 後は、急いで次 package 名を作るより、
  どの row が honest に narrow actualization できるかを first-class に吟味する
- no safe `P-A0-20` promoted yet

## 21. 最終確認用 short checklist

次の問いに全て `yes` と答えられないなら、その change は止めて再確認した方がよい。

1. これは `specs/` / `plan/` / snapshot / report の hierarchy を守っているか
2. actualize したと言う row / floor / runner は本当に fresh validation で支えられているか
3. active root と scaffold root を混同していないか
4. helper-local evidence を parser/runtime completion と誤読させていないか
5. positive proof を absence-of-negative で代用していないか
6. runtime-sensitive row に source authority が本当にあるか
7. stage percentage や completion wording を overclaim していないか
8. user decision blocker を autonomous に固定していないか
9. new report を追加したか
10. commit/push まで current scope を閉じる前提になっているか
