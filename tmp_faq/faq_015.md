# FAQ 015 — 説明論点の累積メモ

- 作成日時: 2026-05-04 22:20:06 JST
- 目的:
  今後の説明タスクで出た論点、未実装境界、具体例、誤解しやすい点を、短い追記で累積する。
- 重要:
  この文書は FAQ / helper memo であり、規範正本ではない。
  優先順位は `specs/` > `plan/` > `progress.md` / `tasks.md` / `samples_progress.md` > `docs/reports/` > `Documentation.md` / `README.md` > この FAQ。
- 運用メモ:
  構造を重くしすぎず、必要な論点を短文で追記する。
  具体例やコマンドが有用なら 1〜2 行で足す。
  この file は branch 上で育て、最後に user が必要な版だけを手動で `main` に取り込む想定にする。

- 2026-05-04 Mir 下位層:
  repo が自前で設計している最下層は Mir core でよい。
  ただし layer model 上はその下に前提として `L0 = existing operating-system / network substrate` がある。
  したがって「repo 設計層の最下層 = Mir」、「世界全体の最下層 = OS/network substrate」という読み分けが必要。

- 2026-05-04 Mir sample root:
  active canonical executable suite は `samples/clean-near-end/`。
  `samples/current-l2/` は base source corpus であり、active canonical executable suite ではない。
  `samples/lean/` は mechanization evidence。
  representative prose は `specs/examples/00-representative-mir-programs.md`。

- 2026-05-04 Mir 実行コマンド:
  current-L2 runtime sample は host plan sidecar 前提で、たとえば
  `cargo run -q -p mir-runtime --example mir_current_l2 -- run-source-sample e2-try-fallback --host-plan crates/mir-ast/tests/fixtures/current-l2/e2-try-fallback.host-plan.json --format json`
  で動く。
  clean near-end typing sample は
  `cargo run -q -p mir-runtime --example mir_current_l2 -- check-source-sample samples/clean-near-end/typing/01_authorized_declassification.mir --format json`
  で確認できる。
  model-check family は
  `python3 scripts/clean_near_end_samples.py run model-check --format json`
  で確認できる。

- 2026-05-04 Mir 実行具体例:
  `e2-try-fallback` は `static_verdict = valid`、`terminal_outcome = success`、`steps_executed = 8`、途中 event に `rollback` を含む。
  これは `try/fallback` と place-local rollback frontier の current runtime evidence として読む。
  current-L2 `.txt` は単独の final runtime source ではなく、host plan sidecar を伴う PoC surface である。

- 2026-05-04 model-check の current scope:
  model-check は static typing first line に押し込めず、`model-check second line` として分離されている。
  active evidence は `01_peterson_sc_pass`、`02_peterson_relaxed_counterexample`、`03_broken_mutex_counterexample`。
  `pass` と `counterexample` の両方を repo-local に確認できるが、一般の Mir program 全体に対する production-grade binding 完了は未主張。

- 2026-05-04 型システムの current reading:
  current first strong typing layer は `finite decidable index fragment`。
  authority hierarchy、security label hierarchy、capture、region、cost は built-in ではなく `user-defined finite theory`。
  current repo evidence で見えるのは、authority / label / capture / cost / lifetime / fallback / variance / contract-compatibility の first floor までであり、full typed calculus completion ではない。

- 2026-05-04 型で何を証明できるか:
  現行 evidence では、権限不足 declassification reject、label flow reject、capture escape reject、cost bound reject、fallback successor capability strengthening reject、mutable covariance reject、monotone degradation、contract / failure / observation safety の selected floor を machine-check できる。
  ただし全性質を 1 本の強い型システムで完結させる設計ではなく、residual obligation、model-check second line、Lean side proof line を分離している。

- 2026-05-04 型の強弱 / 層の付け外し:
  「完成した attachable type-layer 製品」があるわけではない。
  ただし repo の current line は、static typing first line、model-check second line、Lean mechanization side line を分離しており、強い性質を later line へ逃がす設計になっている。
  したがって conceptual には layered だが、public な on/off 機構が完成しているとは言わない。

- 2026-05-04 dependent type / Isabelle 感:
  README は `full dependent type theory` 未完を明記している。
  実感としては Isabelle 的な integrated proof language というより、Mir semantic core の横に Lean mechanization spine がある構図に近い。
  `samples/lean/foundations/` は small actual proof fragment、`samples/lean/clean-near-end/` は generated theorem stub であり、後者は full domain discharge completion を意味しない。

- 2026-05-04 Lean 具体例:
  `samples/lean/foundations/CurrentL2FiniteIndexFirstLayer.lean` には lifetime preorder、capture subset、simple remote-call budget の actual proof fragment がある。
  一方で `samples/lean/clean-near-end/01_peterson_sc_pass/01_peterson_sc_pass.lean` は `True := by trivial` の generated stub であり、Peterson の fully discharged theorem completion ではない。

- 2026-05-04 I/O と HelloWorld:
  Mir core には privileged `stdin` / `stdout` / `stderr` が無い。
  standard I/O は core primitive ではなく、typed external effect / adapter boundary 側に残す current decision である。
  したがって典型的な stdout HelloWorld を「Mir core 単体の代表 sample」としては持っていない。

- 2026-05-04 Mir の計算能力:
  Mir に計算能力が無いわけではない。
  current evidence では `perform`、`require`、`ensure`、`try/fallback`、`atomic_cut`、`publish/observe/handoff/witness`、property evaluation が動く。
  ただし current positioning は「汎用 scripting 言語」より「effect / contract / causality / rollback / verification を前面に出した意味論コア」である。

- 2026-05-04 native binary / binary split:
  current repo では Mir source から final native binary を生成できるとはまだ言えない。
  `plan/23-compiler-backend-llvm-guardrail-roadmap.md` は LLVM/backend の guardrail までで、actual LLVM build、final backend choice、installed-binary packaging success criteria は fixed していない。
  `README.md` と `Documentation.md` も current projection/codegen evidence を final emitted executable family / final public emitted-program ABI と読まないよう明記している。

- 2026-05-04 binary split の current reading:
  現時点では「Mir が place ごとに最終バイナリへ分割される」とは主張しない。
  `Place` はまず execution locus の意味論であり、安定した binary partition unit として public に凍結されていない。
  分割方法、配置指定、deployment planner、cross-place equivalence checker は kept-later / unresolved に属する。

- 2026-05-04 named input/output pipeline の current capability:
  「named adapter lane に request を流し、receipt / route / viewer で観測する」最低限の筋は partial にある。
  具体的には `EXT-03` が `RoomMessageAdapter#local_queue` を named adapter entry とし、`typed_effect_request` / `typed_effect_receipt` / `local_queue` / `redacted_observer_view` を分けた helper-local synthetic preview を返す。
  `python3 scripts/typed_external_boundary_samples.py run EXT-03 --debug envelopes --format json` と
  `python3 scripts/visual_debugger_viewer_samples.py run P16-VIEW-05 --format json`
  で request/receipt と viewer panel を確認できる。

- 2026-05-04 named pipeline の stop line:
  ただし `EXT-03` は active semantic execution sample ではなく synthetic preview helper subset である。
  `samples/not_implemented/typed-external-boundary/03_send_room_message_local_queue.mir` は source stub だが、current parser/runtime がこれを直接 semantic execution しているわけではない。
  したがって「外部入力を実際に入れる汎用 host boundary」が出来ているとはまだ言わない。

- 2026-05-04 console/log text pipeline:
  `EXT-01` `LogText` はまさに console-shaped scenario だが residual planned のままで、active runnable sample ではない。
  理由は stdio builtin 誤読を避けるためで、current indirect anchor は `provider_boundary` 側に留めている。
  したがって「Taro を入れたら Hello, Taro!」のような string console demo は current repo では未実装として読むのが正確。

- 2026-05-04 numeric output + viewer の current floor:
  外部入力つき generic pipeline は未完でも、active semantic runtime で数値 output を publish して viewer まで観測する線はある。
  `samples/clean-near-end/sugoroku-world/03_roll_publish_handoff.mir` では `roll_dice` が `output draw : Nat` を返し、publish 後に `sugoroku_world_samples.py` の visualization と `P16-VIEW-01` viewer bundle で観測できる。
  実行例では `draw = 4`、`roll_request#1` と `handoff_notice#1` の telemetry / panel が出る。

- 2026-05-04 arithmetic の current reading:
  算術そのものが absent というわけではない。
  たとえば `samples/clean-near-end/sugoroku-world/08_reset_interleaving_model_check.mir` には `game_epoch <- game_epoch + 1` がある。
  ただし current evidence は state update / model-check / sample-local transition の範囲であり、「任意の external integer input を受けて plus one して host output へ返す active sample」まではまだ無い。

- 2026-05-04 現時点の率直な結論:
  「typed request/receipt lane + local queue seam + viewer observation」は partial にある。
  「任意の string / integer を host input から流し、transform して host output へ返す generic runnable pipeline」はまだ無い。
  なので、toolchain の芽はあるが、user が自然に期待する最小 host-I/O demo は未 completion と答えるのが正確。

- 2026-05-04 「作ろうとすればもう作れるか」の current judgment:
  現時点では「Mir sample を 1 本書いて、そのまま compile/run/debug すれば host-I/O demo が作れる状態」とは言わない。
  率直には、まだ実装が必要な状態である。
  理由は、typed external の current floor が `EXT-03/04` synthetic preview と indirect anchor に留まっており、generic host input ingestion / transform / host output emission を direct semantic execution する runtime lane がまだ mainline に無いからである。

- 2026-05-04 何が足りないか:
  最低でも次が要る。
  1. typed external boundary sample を current parser/runtime で直接 semantic execution する lane
  2. host input payload の typed schema と adapter/provider boundary contract
  3. input payload を sample-local state / effect に渡す runtime bridge
  4. output payload / receipt / visualization を active viewer/devtools lane へつなぐ exact route
  5. string ないし integer transform を generic demo として再利用できる sample family

- 2026-05-04 したがって:
  今あるのは「将来その demo を作るための部品と、部品間の separation を示す evidence」までである。
  今まだ無いのは「sample author が新規 Mir sample を 1 本書くだけで、その demo が end-to-end に通る ready-to-use host toolchain」である。

- 2026-05-04 すごろく sample の current status:
  `samples/clean-near-end/sugoroku-world/` は active clean near-end sample family として runnable である。
  `python3 scripts/sugoroku_world_samples.py closeout --format json` は 10 samples を返し、`03_roll_publish_handoff`、`06_leave_non_owner`、`08_reset_interleaving_model_check` などが current evidence に入る。
  ただし place model は `single OS process logical multi-place emulator` であり、real networking、consensus、durable distributed commit、final public runtime API は未実装のままである。

- 2026-05-04 `atomic_cut` と consistent cut:
  `atomic_cut` は place-local rollback frontier であって、global sync point でも distributed consensus point でも durable checkpoint でもない。
  consistent cut は save/load 側の predicate であり、distributed save/load without consistent cut は禁止される。
  current practical save/load で通っているのは local-only floor と invalid distributed cut preflight reject であり、distributed durable save/load completion ではない。

- 2026-05-04 すごろく sample と cut/verification:
  `python3 scripts/sugoroku_world_samples.py run 08_reset_interleaving_model_check --debug verification --format json` は
  `no_old_epoch_handoff_after_reset = pass` を返し、broken variant では counterexample を返す。
  これは reset 後に old epoch handoff が commit しない current second-line model-check evidence である。
  ただし global consistent checkpoint / distributed recovery completion を証明しているわけではない。

- 2026-05-04 すごろくの membership freshness:
  current line は vector clock ではなく `membership_epoch` と `member_incarnation` を freshness lane として使う。
  join/leave で epoch が増え、leave で当人の incarnation も増える。
  `06_leave_non_owner` では stale action が old `membership_epoch = 0` / `member_incarnation = 0` のまま reject される。
  repo 検索では active line に `vector clock` / `vector_clock` の採用痕跡は無かった。

- 2026-05-04 vector clock の current reading:
  現行 repo は membership freshness を vector clock で管理している設計としては読まない。
  したがって「join/leave で vector clock entry が無限肥大化するか」という問題設定そのものが current active line の主設計ではない。
  current active line の leave 処理は membership frontier advance と stale action invalidation であり、distributed causal clock compaction protocol は未導入 / 未主張である。

- 2026-05-04 分割バイナリ / place-specific program:
  current projection/codegen evidence は `manifest_bridge_only` であり、final emitted executable program ではない。
  `python3 scripts/projection_codegen_samples.py closeout --format json` も
  `artifact_boundary = committed manifest bridge evidence only; not a final emitted executable program`
  と返す。
  したがって「すごろくから各 place ごとの最終バイナリが生成される」とはまだ言わない。

- 2026-05-04 network transport の current floor:
  helper-local process-boundary canary は `python3 scripts/network_transport_samples.py check-all --format json` で `NET-02..05` pass。
  practical alpha-1 transport floor は `python3 scripts/practical_alpha1_transport.py check-all --format json` で `TR-A1-01..07` pass。
  ただし helper canary は `loopback_socket` / subprocess JSON bridge であり、practical alpha-1 も local TCP / Docker Compose TCP first floor に限る。
  WAN federation、durable replay、continuous shared runtime state、final public transport ABI は未完のままである。

- 2026-05-04 thread / process / VM / network node の current reading:
  設計意図としては transport / auth / membership / capability / witness lane を collapse せず、carrier language を揃える方向である。
  しかし current evidence は「全部が operationally same になった」完成形ではない。
  single-process logical multi-place emulator、helper-local process-boundary canary、practical local TCP / Docker Compose TCP floor までが current actualized boundary である。

- 2026-05-04 auth layer attach の current floor:
  auth layer を attach path に乗せる first floor はある。
  `python3 scripts/practical_alpha1_attach.py check-all --format json` は `HP-A1-01..07` pass を返し、`HP-A1-03` は auth layer accepted only through explicit contract-update path を表す。
  ただしこれは final public auth platform completion ではなく、detach runtime lifecycle、rollback、durable migration、distributed activation ordering は kept-later である。

- 2026-05-04 hot-plug と network patch rollout:
  request / verdict / activation cut の typed hot-plug first floor はあるが、安全な network-wide patch distribution と simultaneous activation completion はまだ無い。
  current docs は attach-time compatibility / authority / witness checks と explicit deferred detach minimal contract までは actualize する一方で、distributed activation ordering は repeatedly deferred / non-claimed としている。
  したがって「各 node へ patch を安全配布して一斉適用する」が既に operational とは言わない。

- 2026-05-04 static checking の current floor:
  `python3 scripts/practical_alpha1_check.py check-all --format json` は `CHK-LIF-01..04`、`CHK-VAR-01..03`、`CHK-CUT-01`、`CHK-PKG-01/02` の 10 rows が pass する。
  ただし `spec18_typed_checking_complete = false` であり、full typed checking completion、runtime execution、Docker run、public CLI freeze は未主張である。

- 2026-05-04 現時点 summary matrix:
  `Layer 1 = Mir semantic core` は current-L2 / clean-near-end / Lean まで論じた。
  ここで動くのは event DAG、effect / contract、ownership / lifetime、fallback、`atomic_cut`、selected static typing、selected model-check。
  まだ無いのは stdio builtin、full dependent type theory、final grammar、final native binary emitter である。
  `alpha` 番号で言うと、ここは practical alpha-1 以前の理論 / active sample base line として読む。

- 2026-05-04 現時点 summary matrix 2:
  `Layer 3 = Mirrorea fabric/runtime substrate` は alpha-0 closeout evidence と practical alpha-1 carrier split まで論じた。
  ここで動くのは `MessageEnvelope`、`MembershipRegistry`、`PlaceCatalog`、`HotPlugRequest` / `HotPlugVerdict`、transport/auth/membership/capability/witness lane separation。
  まだ無いのは production auth protocol、WAN federation、distributed activation ordering、final public transport / hot-plug ABI である。

- 2026-05-04 現時点 summary matrix 3:
  `Layer 4 = practical alpha-1 front-door/checker` は `P-A1-01` / `P-A1-02` まで論じた。
  ここで動くのは limited `package.mir.json` front-door、`CHK-LIF-*`、`CHK-VAR-*`、`CHK-CUT-01`、`CHK-PKG-*`。
  まだ無いのは full typed checking completion、runtime-plan emission completion、public CLI freeze である。

- 2026-05-04 現時点 summary matrix 4:
  `Layer 5 = practical alpha-1 local runtime` は `P-A1-03` まで論じた。
  ここで動くのは checked package -> runtime plan -> local runtime report の narrow floor、event DAG export hook、stale membership reject。
  まだ無いのは same-session product runtime、reusable runtime breadth、public runtime API である。

- 2026-05-04 現時点 summary matrix 5:
  `Layer 6 = practical alpha-1 package / hot-plug` は `P-A1-04a/b/c` まで論じた。
  ここで動くのは debug/auth/rate-limit layer attach、stale-membership reject、missing-witness reject、narrow object attach preview、deferred detach minimal contract。
  まだ無いのは final object attach completion、detach runtime lifecycle、rollback / migration engine、distributed activation ordering である。

- 2026-05-04 現時点 summary matrix 6:
  `Layer 7 = practical alpha-1 transport` は `P-A1-05` まで論じた。
  ここで動くのは local TCP、Docker Compose TCP、stale-membership / missing-capability / missing-witness reject、observer-safe route trace、auth lane separation。
  まだ無いのは WAN federation、durable replay、continuous shared runtime state、final public transport ABI である。

- 2026-05-04 現時点 summary matrix 7:
  `Layer 8 = practical alpha-1 save/load` は `P-A1-07` と `P-A1-16` まで論じた。
  ここで動くのは local save/load first floor と invalid distributed cut preflight reject。
  まだ無いのは distributed durable save/load、stale witness / stale lease non-resurrection completion、queue/channel persistence である。

- 2026-05-04 現時点 summary matrix 8:
  `Layer 9 = devtools / visualization`、`Layer 10 = avatar preview`、`Layer 11 = product preview` は current practical first floors の存在まで論じたが、詳細はまだ掘っていない。
  ここで動くのは exact report 由来の export / preview bundle first floor である。
  まだ無いのは final viewer API、native avatar execution、same-session product runtime、public product completion である。

- 2026-05-04 優先度 judgement:
  user がここまで挙げた基礎機能群は、repo の project axis から見ても高優先度である。
  特に先に揃うべきなのは、typed front-door/check/check-run、membership/capability/witness を潰さない transport、attach-time hot-plug、local save/load、typed devtools/export である。
  一方で split binary emitter、WAN federation、distributed activation ordering、distributed durable save/load は高重要ではあるが、first practical substrate の後段に置く方が自然である。

- 2026-05-04 debug / visualization の位置づけ:
  debug / visualization は optional polish ではなく、repo の project axis である「検証・可視化できる」に直接入る必須層である。
  しかも untyped debug leak ではなく、`label` / `authority` / `redaction` / `retention` を持つ typed information-bearing effect として実装する current decision はかなり整理済みである。
  現 practical alpha-1 では `VIS-A1-01..07` が first floor を形成しているが、same-session viewer service や final public API はまだ later である。

- 2026-05-04 形式検証 / 理論の current judgment:
  理論部分は「何でも凍結済み」ではないが、bounded first-floor 実装を進めるには十分に固まっている。
  具体的には、L1 で守る invariant、finite decidable first typing layer、model-check second line、Lean mechanization spine、`atomic_cut` / consistent-cut / fallback / variance / layer-compatibility の alpha-local boundary はかなり明示されている。
  ただし full dependent type theory、production prover binding、final public verifier contract、distributed durable semantics の fully discharged theory はまだ later である。

- 2026-05-04 実装方針の current recommendation:
  「上限を決めたうえで、部分的に必要なものをいくつか実装する」という方向は repo の current line と整合している。
  つまり、broad final product を一気に取らず、non-final first floor を exact carrier split と non-claim つきで積み上げる ratchet 方式が current recommendation である。
  practical alpha-1 の package map と samples/expected/report discipline は、この bounded widening をかなり意識して整理されている。

- 2026-05-04 docs の整理状況:
  「何が出来て欲しいか」は相当に整理済みである。
  規範判断は `specs/13..18`、repository memory は `plan/39..44`、snapshot は `Documentation.md` / `progress.md` / `tasks.md` / `samples_progress.md`、実証は `samples/clean-near-end/`、`samples/alpha/`、`samples/practical-alpha1/` に分かれている。
  ただし safe next package の選び方、same-session runtime の切り出し方、broader save/load widening、typed external direct semantic execution lane などは、まだ実装順の整理が必要である。

- 2026-05-05 「α-1 まで完了扱い」に見えた理由:
  第一に、repo は `current-scope evidence closeout` と `practical alpha-1 readiness` を分けつつ、どちらにも `100%` を使っていたため、表面上かなり前に進んで見えやすかった。
  第二に、practical alpha-1 自体も「final public product」ではなく、non-final first floor を順に閉じる定義だったため、`PA1-1..7` の `100%` は narrow floor closeout を意味していた。
  第三に、user 側の lower bound にある host boundary / stronger runtime continuity / same-session runtime / broader operational completion が、alpha-1 の閉じ条件に十分強く入っていなかった。

- 2026-05-05 では計画が雑だったのか:
  完全に雑だったというより、package slicing 自体はかなり意図的で、carrier split と non-claim を保ちながら ratchet で積む設計だった。
  ただし phase name と `100%` 表記は optimistic に読める余地があり、capability-based milestone より artifact-based milestone が前に出ていた点は弱かった。

- 2026-05-05 では目標共有が足りなかったのか:
  かなり yes である。
  docs には non-claim が多く書かれている一方で、「user が自然に alpha-1 で期待する lower bound」が閉じ条件として十分強く固定されていなかった。
  特に typed external direct execution、same-session runtime、broader save/load semantics、more product-like operational continuity は later に押し出されていた。

- 2026-05-05 current recommendation from this mismatch:
  以後は `alpha-1 complete` のような裸の表現を避け、少なくとも
  `current-scope evidence closeout`、
  `practical first-floor closeout`、
  `user lower-bound unmet`、
  `public/product readiness`
  を明示的に分けて書く方が安全である。
