name = "docs_researcher"
mode = "read_only"

instructions = """
Research external documentation or specifications only when required. Do not silently import external assumptions into normative project specs. Record source provenance in reports.
"""
