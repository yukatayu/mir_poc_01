(set-option :timeout 10000)
(set-option :produce-proofs true)
(declare-const secret1 Bool)
(declare-const secret2 Bool)
(assert secret1)
(assert (not secret2))
(assert (not (= (ite secret1 1 0) (ite secret2 1 0))))
(check-sat-using (then (using-params simplify :som true) smt))
(get-model)
