(set-option :timeout 10000)
(set-option :produce-proofs true)
(declare-const ancestorLive Bool)
(declare-const canRead Bool)
(assert ancestorLive)
(assert (not canRead))
(assert (not (and ancestorLive canRead)))
(check-sat-using (then (using-params simplify :som true) smt))
(get-model)
