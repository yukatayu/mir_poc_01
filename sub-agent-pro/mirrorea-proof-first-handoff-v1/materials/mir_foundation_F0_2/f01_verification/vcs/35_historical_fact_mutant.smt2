(set-option :timeout 10000)
(set-option :produce-proofs true)
(declare-const sample Int)
(declare-const now Int)
(assert (= sample 1))
(assert (= now 2))
(assert (not (= now sample)))
(check-sat-using (then (using-params simplify :som true) smt))
(get-model)
