(set-option :produce-proofs true)
(set-option :produce-models true)
(set-option :timeout 15000)
(set-logic ALL)
(declare-const held Int)
(declare-const acting Int)
(assert (distinct held acting))
(assert (= held acting))
(check-sat)
(get-proof)
