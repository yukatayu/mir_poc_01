# 文書の検査記録

## 実行したこと

`python validation/check_structure.py`を実行し、119要件、18個の意図アンカー、30未決事項、18保証対象、24受入れシナリオ、7能力判定点、14リスクの参照関係を検査しました。17領域への分類、IDの一意性、要件詳細の存在、全要件から受入れ仕様への対応、HTML内リンク、外部script/style/imageの不使用を確認し、エラーは0件でした。

能力判定点の依存は非循環でした。要件の共同設計参照には `CT-05 ↔ EV-04` と `ID-07 ↔ TY-04` の二つの相互依存があり、その事実を記録しました。これは実装順や循環論法の受理ではありません。

生成HTMLをChromiumへ文字列として読み込み、119件の詳細表示、全文検索、分類絞込み、展開、内部リンクによる要件の表示を検査しました。desktopと390px幅のmobileで表示を確認し、document全体の横方向overflow、JavaScript error、初期表示での外部HTTP要求はありませんでした。ローカルファイルURLへのnavigateは実行環境により拒否されたため、この検査は同一HTML文字列を `set_content` で表示しています。配布先すべてのbrowserでの動作保証ではありません。

## 実行していないこと

MirroreaリポジトリやF0.1/F0.2のプログラム・テスト・SMTの再実行、手証明の再監査、Lean/Rocqでの証明検査、今回設計した24シナリオの実行、119要件の意味上の達成判定は行っていません。既存の実行結果は出典とscopeを付けて参照しただけです。

この検査が示すのは、要求文書の内部追跡と表示の整合性です。未知の要求が存在しないこと、理論の健全性、Canonへの採用を示すものではありません。

## 再確認

標準Pythonだけで `python validation/check_structure.py` を実行できます。結果は `VALIDATION.json` へ出力されます。browserの確認結果は `validation/BROWSER_CHECK.json`、表示の記録は同じディレクトリにあります。
