# 417 — current L2 stable malformed capability second source-backed widening actualization

## 目的

`specs/examples/401...402` で fixed した
`e13/e20` capability pair の source-backed widening actualization を、

- authored source sample
- lowering / runner / verification ladder
- emitted artifact wiring
- regression inventory / smoke bundle

まで narrow に actual code へ反映した current cut として整理する。

ここで fixed するのは
**`e13/e20` source-authored static-stop pair の actualized surface**
であり、

- duplicate cluster actualization
- `TryFallback` / `AtomicCut` malformed-static broader reopen
- theorem/model-check/public-checker widening
- final public CLI / final public runtime API

は still later に残す。

## source-backed floor

- current family judgment は `e13/e20` pair に置く。
- current entry evidence は
  - helper-local capability compare
  - stage 1 reconnect widening
  - fixture-static capability rows
  である。
- current source-backed widening first cut は
  `e13/e20` source-authored static-stop pair を authored corpus に加え、
  source lowerer / source sample runner / verification ladder / regression helper に widen する。

## actualized surface

| surface | current actualization |
|---|---|
| source sample files | `samples/current-l2/e13-malformed-capability-strengthening.txt` と `samples/current-l2/e20-malformed-late-capability-strengthening.txt` を authored row に追加 |
| accepted sample set | `run_current_l2_source_sample` が `e13/e20` stem / explicit path を受ける narrow accepted set に widen |
| lowering | `current_l2_source_lowering` が `e13/e20` の static-stop pair と stage1 reconnect cluster を確認 |
| source sample runner | `current_l2_source_sample_runner` が `e13/e20` malformed static stop と `capability_strengthening_floor` を確認 |
| verification ladder | `current_l2_source_sample_verification_ladder` が `e13/e20` を `fixture_static_cluster` formal-hook reached row に追加 |
| emitted artifact wiring | helper-local emitted route が `e13/e20` static rows から review-unit / model-check carrier fan-out を確認 |
| regression helper | inventory authored rows を dozen へ widen し、static formal-hook smoke に `e13/e20` を追加 |

## current judgment

1. capability family widening は `e13` lead singleton ではなく **`e13/e20` source-authored static-stop pair** で閉じる。
2. current actualized surface は source sample / lowerer / runner / ladder / emitted helper / regression bundleに留める。
3. current formal-hook top は `fixture_static_cluster` のまま保ち、capability family widening を theorem/model-check public contract へ昇格させない。
4. current authored corpus は dozen になったが、これは final grammar や final public runner を意味しない。

## guard

- `e13/e20` pair judgment を staging note で weaken しない。
- public/report shape `run_current_l2_source_sample` / `CurrentL2SourceSampleRunReport` は維持する。
- duplicate cluster と `TryFallback` / `AtomicCut` malformed-static broader reopen は later に残す。
- low-level memory-order line、typed/theorem/model-check line、public operational shell lineを capability widening package に混ぜない。

## next promoted line

execution lane の next reopen point は、
**malformed duplicate-cluster later reopen comparison**
に置く。

repo-level near-term order 全体では、
reserve integration lane の
**public operational CLI concrete shell actualization**
を先に進めてよい。

## what is not decided here

- duplicate cluster の actual source-backed widening timing
- `TryFallback` / `AtomicCut` malformed-static broader family の reopen ordering
- public operational CLI concrete shell の final packaging
- theorem/model-check/public-checker widening timing
