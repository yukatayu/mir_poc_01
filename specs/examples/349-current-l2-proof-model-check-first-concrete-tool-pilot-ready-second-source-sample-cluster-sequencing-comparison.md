# 349 — current L2 proof-model-check-first-concrete-tool-pilot-ready second-source-sample-cluster-sequencing comparison

## 目的

`specs/examples/348-current-l2-proof-model-check-first-concrete-tool-pilot-ready-minimal-proof-model-check-first-concrete-tool-pilot-threshold.md`
で proof/model-check first concrete tool pilot の current cut を fixed した次段として、

- post-sextet first cluster をどの sample family に置くか
- source / fixture / formal-hook / regression の 4 層をどの narrow cluster で同期するか
- public surface inventory や broader sample widening をどこまで後段に残すか

を比較する。

ここで固定するのは
**current L2 proof-model-check-first-concrete-tool-pilot-ready second-source-sample-cluster-sequencing comparison**
であり、

- actual `e22` source row actualization
- stable static malformed post-contrast cluster sequencing
- parser / checker / runtime public surface inventory
- model-check / public-checker second reserve inventory

はまだ固定しない。

## scope

- entry criteria は `specs/examples/315...348` の source corpus / lowering / runner / ladder / theorem-first pilot lineに置く。
- current authored source sample は `e1` / `e2` / `e3` / `e21` / `e4` / `e23` の sextet に留める。
- current package は docs-only sequencing close であり、new source file や accepted set widening は次段に残す。

## current 前提

current repo では次が成立している。

1. current authored sextet のうち `e1` / `e2` / `e21` は `runtime_try_cut_cluster` formal-hook top に reached している。
2. `e3` は source-authored row まで actualize 済みだが、formal hook stage は `not reached (guarded)` に留まる。
3. `e22-try-atomic-cut-place-mismatch` は representative prose / fixture / runtime test / detached-loop compare 側に already anchor を持つが、source-authored row にはまだ入っていない。
4. expiry / monotone degradation family、request-contract family、stable static malformed familyは fixture / runtime / parser evidence を持つが、current authored sextet と同じ narrow lineへはまだ入れていない。

したがって current 問いは、
**current post-sextet first cluster を `e21` / `e22` try-rollback locality contrast に置き、broader family は still later に残すのが自然か**
である。

## 比較観点

1. current authored sextet と current formal-hook top を壊さないか
2. next actual source row を 1 本に絞りつつ、fixture / detached-loop evidence を活かせるか
3. expiry / request-contract / static malformed family を premature に混ぜないか
4. parser / checker / runtime public surface inventory を still later に押し分けられるか

## 比較対象

### 案 1. `e21` / `e22` try-rollback locality contrast を first post-sextet cluster にする

#### shape

```text
second_source_sample_cluster = {
  cluster_kind = current_l2_first_post_sextet_runtime_contrast_cluster,
  cluster_rows = [
    {
      sample_stem = e21_try_atomic_cut_frontier,
      current_status = already_source_authored,
      cluster_role = frontier_update_anchor
    },
    {
      sample_stem = e22_try_atomic_cut_place_mismatch,
      current_status = deferred_target_only,
      cluster_role = place_mismatch_contrast_pair
    }
  ],
  guard_refs = [
    keep_runtime_try_cut_cluster_as_current_formal_hook_top,
    keep_e3_guarded_non_reached_row_unchanged,
    defer_broader_runtime_guarded_families,
    defer_public_surface_inventory
  ]
}
```

#### 利点

- `e21` existing authored row と `e22` existing fixture/runtime evidence をそのまま対で読める。
- `e22` は current `runtime_try_cut_cluster` family の内側に留まるため、`e3` のような guarded widening を新たに増やさずに済む。
- detached-loop contrast support が already あるため、next actualization の regression route が明瞭である。

#### 欠点

- source-authored row の追加は次段 task に残る。

### 案 2. expiry / monotone degradation family (`e6` / `e7` / `e8` / `e9`) を先に widen する

#### 利点

- guarded option chain / `lease` 読みを厚くできる。

#### 欠点

- current formal-hook top の外に new guarded runtime row を増やしやすい。
- post-sextet first cluster としては family が広い。

### 案 3. stable static malformed family を先に widen する

#### 利点

- `fixture_static_cluster` side を厚くしやすい。
- typed reason-code current lineと接続しやすい。

#### 欠点

- first post-sextet cluster としては breadth が大きい。
- runtime contrast evidence と source-authored row wideningを同じ narrow line で扱えない。

### 案 4. request-contract family (`e10` / `e11`) を先に widen する

#### 利点

- parser second tranche と runtime request-contract evidence を接続しやすい。

#### 欠点

- request-clause family の parser pressure を source sample wideningと同時に呼び込みやすい。
- current post-sextet first cluster としては narrow さが弱い。

## current judgment

current L2 で最も自然なのは、
**案 1. `e21` / `e22` try-rollback locality contrast を first post-sextet cluster にする**
である。

理由は次の通り。

1. `e22` は current sextet のうち `e21` と最も強い contrast pair を already 持つ。
2. `e22` は current `runtime_try_cut_cluster` family に留まり、new guarded row widening を要求しない。
3. expiry / request-contract / stable static malformed family は、それぞれ broader family pressure を持つため、first post-sextet cluster より一段後ろへ置く方が staged line に合う。

## current first choice details

- first post-sextet cluster は `e21-try-atomic-cut-frontier` / `e22-try-atomic-cut-place-mismatch` contrast pair に置く。
- next actual source row 候補は `e22-try-atomic-cut-place-mismatch` に置く。
- source / fixture / formal-hook / regression の 4 層では、
  - source: `e22` を next authored-row actualization target に置く
  - fixture: existing `e21` / `e22` runtime contrast pair を current anchor に置く
  - formal hook: `runtime_try_cut_cluster` current top を維持する
  - regression: detached-loop contrast evidence と runtime formal-hook smoke family を next actualization route に使う
- stable static malformed family は post-contrast broader follow-up cluster に残す。
- expiry / monotone degradation family と request-contract family は still later に残す。

## next promoted line

next promoted line は、
**second-source-sample-cluster-sequencing-ready actual-e22-contrast-row-actualization comparison**
に置く。

## open questions

- `e22` source row を representative prose mirror と helper-compatible fixed subset のどちらへ寄せるか
- post-contrast second cluster を stable static malformed family のどの subcluster から reopen するか
- public surface inventory を `e22` actualization の前後どちらで reopen するか
