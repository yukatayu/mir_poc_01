# 514 — current L2 theorem public seam compression after local Lean-unavailable probe

## 目的

`specs/examples/506`、
`510`、
`513`
と
`e5-underdeclared-lineage`、
`p05-dice-owner-guarded-chain`、
`p06-typed-proof-owner-handoff`、
`p07-dice-late-join-visible-history`、
`p08-dice-stale-reconnect-refresh`
を前提に、

- theorem public seam compression
- theorem final public-contract reopen threshold carry-over
- Lean-stub representative bridge carry-over
- actual Lean execution environment stop line
- actual runnable evidence
- retained alternatives
- stop line

を 1 本に束ねる。

ここで actualize するのは、
**theorem final public-contract reopen threshold と Lean-stub representative trace-alignment bridge を保ったまま、actual Lean execution unavailable probe を environment stop line として取り込み、remaining theorem public seam residual を helper-local compression manifest に actualize する current cut**
であり、

- final public theorem result object
- consumer-shaped theorem payload public contract
- concrete theorem prover brand
- proof object public schema
- actual Lean execution
- final public verifier contract

は fixed しない。

## source-backed floor

current repo では、少なくとも次が source-backed である。

1. theorem final public-contract reopen threshold
   - result-object and payload first
   - prover-brand and proof-schema second
   - final public verifier contract third
2. theorem Lean-stub representative trace-alignment bridge
   - representative runtime/static/prototype corpus `e2 / e5 / p06 / p07 / p08`
   - guard-only `p05`
   - repo-local pair alignment
3. theorem actual Lean execution availability probe
   - local `lean` / `lake` / `elan` unavailable なら Lean-stub floor keep
   - actual Lean execution は environment-conditional reserve

したがって current open problem は、
theorem mixed gate を unordered のまま残すことではなく、
**result-object/payload pair、prover-brand/proof-schema pair、final public verifier contract、actual Lean execution environment gate の residual を 1 つの current recommendation に圧縮できるか**
である。

## current compression cut

current package では、次を採る。

1. theorem public seam residual は
   - result-object and payload first
   - prover-brand and proof-schema second
   - final public verifier contract third
   - actual Lean execution environment-conditional
   に圧縮する
2. repo-local theorem evidence は Lean-stub representative trace alignment を carry-over する
3. actual Lean execution unavailable probe は environment stop line として取り込む
4. representative reached sample は `e5 / p06 / p07 / p08` に取り、`p05` は guard-only に残す
5. final public theorem contract adoption 群には上げない

## actual runnable evidence

| evidence | current reading |
|---|---|
| `build_current_l2_source_sample_theorem_public_seam_compression` | theorem threshold + Lean-stub bridge + environment stop line を束ね、remaining theorem public seam residual を helper-local に actualize する runtime support |
| `current_l2_theorem_public_seam_compression` | `e5 / p06 / p07 / p08` reached、`p05` guard-only の theorem public-seam compression を machine-check する focused runtime test |
| `theorem_public_seam_residual:*` refs | current theorem mixed gate を subject-local residual matrix として固定する repo-local ref family |
| `environment_probe:current_l2.theorem_actual_lean_execution_availability_probe` | actual Lean execution が current local environment gate を越えていないことを示す external probe anchor |

## current recommendation

1. theorem line は public seam residual を compression manifest に actualize してよい。
2. Lean-stub bridge は actual Lean execution unavailable の間、current theorem execution evidence として keep してよい。
3. actual Lean execution は environment-conditional reserve に戻す。
4. final public theorem result object、payload public contract、prover brand、proof schema、final public verifier contract は still later に残す。

## retained alternatives

- result-object / payload pair を分離して reopen
- prover-brand / proof-schema pair を分離して reopen
- actual Lean execution first reopen
- final public verifier contract first reopen
- unified theorem public contract first adoption

## stop line

current package は次で止める。

- final public theorem result object
- consumer-shaped theorem payload public contract
- concrete theorem prover brand
- proof object public schema
- actual Lean execution
- final public verifier contract
