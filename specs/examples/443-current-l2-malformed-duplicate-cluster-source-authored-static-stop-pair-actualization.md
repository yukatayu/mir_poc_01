# 443 — current L2 malformed duplicate-cluster source-authored static-stop pair actualization

## 目的

`specs/examples/438-current-l2-malformed-duplicate-cluster-source-sample-widening-comparison.md`
で fixed した

- duplicate cluster `e14/e15` pair-first widening
- duplicate non-promotion guard
- broader `TryFallback` / `AtomicCut` malformed-static family kept-later line

を前提に、`e14/e15` を current L2 source-authored static-stop pair として narrow actualize する。

ここで actualize するのは

- source sample
- accepted runner set
- lowering / runner / verification ladder
- emitted artifact wiring
- regression inventory / smoke planning

までであり、duplicate typed / mirrored promotion や broader malformed-static reopen は still later に残す。

## source-backed floor

- duplicate cluster `e14/e15` は static-only actual corpus として already ある。
- duplicate cluster は current helper cut では `checked_reasons` / detached `reason_codes` に上げていない。
- `specs/examples/430` は duplicate cluster を broader malformed-static family より先に reopen する sequencing を fixed 済みである。
- `specs/examples/438` は `e14/e15` pair-first widening を next exact cut に fixed 済みである。

## actualization cut

current L2 では、`e14/e15` を
**source-authored static-stop pair**
として actualize するのが最小である。

actualized surface は次である。

1. `samples/current-l2/` authored corpus
2. `mir-runtime::current_l2` accepted sample set
3. source lowering / source sample runner tests
4. verification ladder static formal-hook path
5. emitted artifact wiring
6. repo-local regression helper inventory と smoke planning

## current judgment

current L2 では、
**duplicate cluster `e14/e15` を source-authored static-stop pair として actualize し、execution lane の current fixed-subset widening closeout をここで閉じる**
のが自然である。

理由は次の通り。

1. option-side / chain-side の duplicate fixture は already pair で actual corpus にある。
2. `e14` only や `e15` only の uneven widen より、pair 単位で non-promotion guard を固定する方が誤読に強い。
3. broader `TryFallback` / `AtomicCut` malformed-static family をこの package に混ぜないことで、execution lane を narrow に閉じられる。

## keep / drop line

### keep

- `e14/e15` pair-first authored widening
- static-stop ladder reached
- `fixture_static_cluster` formal-hook route
- emitted review-unit / model-check carrier helper route
- duplicate cluster non-promotion guard

### drop from current package

- duplicate cluster を `checked_reasons` / detached `reason_codes` に昇格させること
- duplicate typed / mirrored promotion
- nested-scope / try-fallback duplicate variant actualization
- broader `TryFallback` / `AtomicCut` malformed-static family reopen

## current result

current authored source sample corpus は、subsequent underdeclared source actualization を含む current snapshot では authored sixteen であり、その内訳に `e14/e15` duplicate pair を含む。

current runner / ladder / emitted artifact route では、

- `e14` = duplicate option declaration static stop
- `e15` = duplicate chain declaration static stop

を source-authored row として通す。

ただし duplicate cluster は引き続き
`checked_reasons` / detached `reason_codes`
の stable cluster には入れない。

## next promoted line

next promoted line は、
**modality internalization trigger note**
に置く。

## what is not decided here

- broader `TryFallback` / `AtomicCut` malformed-static family の reopen timing
- duplicate cluster typed / mirrored promotion timing
- duplicate cluster nested-scope / try-fallback variant actualization
- final malformed family catalog
