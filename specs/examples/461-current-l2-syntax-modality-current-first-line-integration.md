# 461 — current L2 syntax / modality current first-line integration

## 目的

`specs/examples/409`、`410`、`422`、`429`、`437`、`444`、`458`、`460` と
`samples/not_implemented/README.md`
を前提に、

- syntax / semantics coupling principle
- syntax candidate evaluation axis
- rough syntax stimuli の位置づけ
- modal basis reading
- stronger candidate family
- reduction timing
- internalization trigger
- current recommendation
- retained alternatives
- stop line
- remaining mixed gate

を 1 本の current integration note に束ねる。

ここで fixed するのは **Package D の current first-line / stop-line integration judgment** であり、

- final parser grammar
- final adopted modal foundation
- final source-surface modality markers
- concrete checker / theorem / runtime integration
- full dependent calculus

は fixed しない。

## scope

- syntax / modality theory line だけを扱う。
- order / handoff / typed / theorem / model-check の current first line は、それぞれ `specs/examples/460` と `459` に委ねる。
- rough syntax stimuli A-D は comparison material として扱い、adopted surface form と同一視しない。

## source-backed floor

current repo では、少なくとも次が source-backed にある。

1. syntax / semantics coupling principle
   - 自然に書けるものは自然な挙動をするべきである
   - 理論的に破綻した動作は書けない / 書きにくいべきである
   - semantic honesty を compactness より優先する
   - packed row / implicit precedence / outer-inner drift を避ける
2. syntax candidate principal axis
   - semantic honesty
   - checker legibility
   - modal adequacy
   - misreading resistance
   - lowering friendliness
3. rough syntax stimuli A-D
   - publication / handoff / witness separation
   - room-local serial transition sugar
   - low-level / backend fence family
   - room macro / transaction-like sugar
   は comparison material に留める
4. modal basis reading
   - `lambda_circle_box` は partial basis candidate
   - guarded lambda-calculus、MDTT、MTT、Fitch-style multimodal は stronger candidate family
5. reduction timing
   - guarded と MDTT/MTT cluster の exact reduction は threshold-gated parallel keep
   - if stronger lead must be named, `MTT` is only working hypothesis
6. internalization trigger
   - stronger foundation candidate を前面化してよい trigger は boundary-pressure trigger

## syntax candidate comparison

| stimulus / family | extracted cluster | current reading | current guard |
|---|---|---|---|
| stimulus A | publication / handoff / witness separation | useful comparison material | adopted syntax と書かない |
| stimulus B | room-local serial transition sugar | useful comparison material | current first line と書かない |
| stimulus C | low-level / backend fence family | useful comparison material | low-level public token と書かない |
| stimulus D | room macro / transaction-like sugar | useful comparison material | room-profile macro adoption と書かない |

## syntax evaluation axis note

| axis | why it is principal | current misuse to avoid |
|---|---|---|
| semantic honesty | surface が causal / rollback / authority readingを隠さないため | compactness-first drift |
| checker legibility | structural floor と obligation owner を読みやすくするため | prose sugar への逃避 |
| modal adequacy | stage / later / authority / witness pressure を測るため | single-axis 過信 |
| misreading resistance | outer-inner drift、implicit precedence を防ぐため | packed metadata row |
| lowering friendliness | backend / helper / emitted-artifact bridge を壊さないため | source syntax を low-level API へ premature に寄せること |

## modal foundation comparison

| family | current role | current recommendation | retained later as |
|---|---|---|---|
| `lambda_circle_box` | partial basis candidate | partial basis に留める | stage / later / always micro-core |
| guarded lambda-calculus | stronger candidate family | retained stronger family | coinductive / staged observation line |
| MDTT | stronger candidate family | retained stronger family | richer modal dependent basis |
| MTT | stronger candidate family | retained stronger family、lead を置くなら working hypothesis | multi-axis mode theory candidate |
| Fitch-style multimodal | stronger candidate family | retained stronger family | modality presentation family |

## reduction timing and trigger note

### reduction timing

current repo が採るのは、
**threshold-gated parallel keep**
である。

- guarded と MDTT/MTT cluster は promotion threshold が actual pressure になるまで並行 keep する
- `lambda_circle_box` only hold に戻らない
- early reduction で final foundation を寄せない

### internalization trigger

current repo が採るのは、
**boundary-pressure trigger**
である。

trigger は、少なくとも次が同時に起きた時点に置く。

1. single-axis stage / later / always 語彙だけでは principal boundary package を説明し切れない
2. place / authority / witness / visibility の複数軸を同時に internalize しないと stop line を明確に保てない
3. それでも final syntax / final public contract / concrete tool binding へは進まない

## current judgment

1. syntax / modality の current first line は、
   - semantic honesty axis
   - rough stimuli A-D comparison only
   - `lambda_circle_box` partial basis
   - guarded / MDTT / MTT / Fitch-style multimodal retained stronger family
   - threshold-gated parallel keep
   - boundary-pressure trigger
   に置く。
2. syntax candidate は少なくとも
   semantic honesty / checker legibility / modal adequacy / misreading resistance / lowering friendliness
   の 5 軸で比較する。
3. `MTT` lead は working hypothesis に留め、adoption と書かない。
4. rough syntax stimuli A-D と snake_case relation names を final parser-facing surface と書かない。

## retained alternatives

- early reduction to guarded
- early reduction to MDTT/MTT
- `lambda_circle_box` only hold
- explanation-layer trigger
- tool-binding trigger
- compact syntax / hidden precedence / packed metadata row

## stop line

current package は次で止める。

- final parser grammar
- final adopted modal foundation
- final source-surface modality markers
- concrete checker / theorem / runtime integration
- full dependent calculus
- stronger typed surface actual adoption

## remaining mixed gates

- final foundation adoption gate
- final source-surface modality marker gate
- checker / theorem / runtime integration timing gate
- stronger typed surface actual adoption gate

## current package close

Package D は、syntax axis / modal basis / stronger candidate family / reduction timing / trigger を
rough syntax stimuli preservation bucket と docs-first comparison chain の上で再統合した時点で close と読む。

next promoted line は `Package E` integration である。
