# 566. current L2 finite-index first layer capture / lifetime / cost actualization

## 位置づけ

- historical Phase 5 / Package 92 closeout memory。
- `p15-typed-capture-escape-rejected` と
  `p16-typed-remote-call-budget-exceeded`
  は、2026-04-22 clean-sample migration 前の
  pre-clean-near-end corrected prototype anchor として保持する。
- current sample-side first strong typing line は
  active clean-near-end typing family
  `01_authorized_declassification`、
  `02_unauthorized_declassification_rejected`、
  `03_label_flow_rejected`、
  `04_capture_escape_rejected`、
  `05_cost_bound_rejected`
  を front door に置く。
- final typed calculus、final typed source principal、
  final public checker payload schema、
  final public verifier contract を fixed する task ではない。

## この package で固定する current cut

1. `p15 / p16` は
   `samples/old/2026-04-22-pre-clean-near-end/prototype/current-l2-typed-proof-model-check/`
   にある historical prototype anchor として保ってよい。
2. current repo-local alpha first strong typing line の sample-side compare floor は
   active clean-near-end typing familyに置き、
   capture / lifetime / simple cost の current execution/inspection は
   `04_capture_escape_rejected` と `05_cost_bound_rejected`
   から読む。
3. helper-local checker summary widening family は repo-local threshold 群に留め、
   final public checker / verifier contract へ昇格しない。

## current recommendation

- `p15 / p16` は historical package closeout memory として読む。
- current active sample-side verification / inspection / Lean sync floor は、
  archived prototype path ではなく
  `samples/clean-near-end/typing/`
  と `samples/lean/clean-near-end/`
  を優先する。
- finite-index first strong typing lineは、
  IFC / taint・capture / lifetime・simple cost を保ったまま、
  checker-adjacent first layer に留める。

## actualized evidence

- archived prototype anchors:
  - `samples/old/2026-04-22-pre-clean-near-end/prototype/current-l2-typed-proof-model-check/p15-typed-capture-escape-rejected.txt`
  - `samples/old/2026-04-22-pre-clean-near-end/prototype/current-l2-typed-proof-model-check/p16-typed-remote-call-budget-exceeded.txt`
- current clean-near-end typing compare floor:
  - `samples/clean-near-end/typing/04_capture_escape_rejected.mir`
  - `samples/clean-near-end/typing/05_cost_bound_rejected.mir`
- helper / runtime anchors:
  - `crates/mir-runtime/tests/current_l2_source_sample_runner.rs`
  - `crates/mir-runtime/tests/current_l2_verifier_preview_alignment.rs`
  - `crates/mir-runtime/tests/current_l2_model_check_projection_prefloor.rs`
  - `crates/mir-runtime/tests/current_l2_operational_cli.rs`

## stop line

- final typed source principal
- final typed calculus
- final public checker payload schema
- final public verifier contract
- archived prototype anchor の active path 復帰

## retained-later line

- stronger typed source principal
- final capture / lifetime source syntax
- final simple cost source syntax
- theorem-side richer cost proof
- full dependent core
