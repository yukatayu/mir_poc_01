# 474 — current L2 theorem-prover experimental binding preflight

## 目的

`specs/examples/466-current-l2-problem1-actual-adoption-package-and-theorem-first-pilot.md`
と
`470-current-l2-theorem-first-experimental-pilot-actualization.md`
を前提に、

- theorem-first external integration target
- notebook-first review-unit principal
- brand-neutral theorem request preflight
- no public theorem / verifier contract promotion

を 1 package 分 actualize する。

ここで actualize するのは、
**theorem-first pilot を concrete theorem prover brand へ bind せずに external-theorem-first pressure へ晒す helper-local preflight cut**
であり、

- concrete theorem prover production binding
- actual discharge transport
- public theorem contract
- proof object public schema

は fixed しない。

## current actualization cut

current repo では、theorem-prover experimental binding preflight route を次に置く。

1. principal row = `proof_notebook_review_unit`
2. evidence floor = symbolic `evidence_refs` only
3. binding preflight manifest は brand-neutral request ref に留める
4. compare floor = theorem-first pilot actualization + theorem-binding preflight
5. public theorem / verifier contract へは上げない

## actual runnable evidence

| evidence | current reading |
|---|---|
| `cargo test -q -p mir-semantics --test current_l2_formal_hook_support --test current_l2_proof_notebook_review_unit_support` | `e2 / e5` から `proof_notebook_review_unit` と formal-hook rows を抽出でき、brand-neutral preflight の principal floor が現行 tree で生きている |
| `cargo test -q -p mir-runtime --test current_l2_source_sample_runner --test current_l2_source_sample_verification_ladder` | accepted current-L2 source corpus では `e5-underdeclared-lineage` が theorem-side current subject として引き続き green である |
| `cargo test -q -p mir-semantics --test current_l2_lean_theorem_stub_support --test current_l2_lean_theorem_stub_actual_probe` | preflight の先でも non-production Lean stub までしか上げず、concrete discharge transport / public theorem contract を採らない current bridge floor を再確認する |
| `cargo run -q -p mir-runtime --example mir_current_l2 -- check-source-sample samples/clean-near-end/order-handoff/05_delegated_rng_service.mir --format json` | clean-near-end representative output が theorem obligations / layer signatures を持ち、runtime-private compare floor を維持している |

historical `p05 / p06 / p07` は compare-floor memory としては参照してよいが、current accepted theorem-preflight target ではない。

## actualization shape

current helper-local preflight manifest は少なくとも次を持つ。

- `preflight_status`
- `preflight_subject_kind` / `preflight_subject_ref`
- `principal_review_unit_refs`
- symbolic `evidence_refs`
- `binding_preflight_manifest_refs`
- `adapter_boundary_refs`
- `compare_floor_refs`
- `guard_refs`
- `kept_later_refs`

current `adapter_boundary_refs` は、少なくとも次を表す。

- principal review-unit first
- symbolic evidence refs only
- brand-neutral theorem request

## current recommendation

1. theorem-first external integration target では、brand-neutral theorem binding preflight を helper-local に actualize してよい。
2. principal source は notebook-first review unit に維持する。
3. preflight manifest は compare floor と repo-local preflight evidence に留める。
4. concrete theorem prover brand / actual discharge transport / public theorem contract は still later に残す。
5. model-check line は引き続き second line に留める。

## retained alternatives

- concrete theorem prover brand early binding
- public theorem contract early adoption
- actual discharge transport early fixation
- proof object public schema early fixation
- model-check-first external integration

## stop line

current package は次で止める。

- concrete theorem prover production binding
- actual discharge transport
- public theorem contract
- proof object public schema
- final public verifier contract

## remaining mixed gates

- theorem prover concrete brand / tool binding gate
- actual discharge transport gate
- public theorem contract gate
- proof object public schema gate
- final public verifier contract gate

## current maintenance reading

current theorem-side active evidence floor は maintenance lane で保持し、nearby theorem docs / repository memory mirror と focused validation を先に揃える。product-shaping reopen point は引き続き `U1` actual commitment である。
