# 466 — current L2 Problem 1 actual adoption package and theorem-first pilot

## 目的

`faq_007.md`、`specs/examples/459`、`463`、`464`、`465` と
`p06-typed-proof-owner-handoff` を前提に、

- verifier-boundary / typed / theorem / model-check の current first line
- user-authorized default
- actual adoption package
- theorem-first experimental pilot line
- retained alternatives
- stop line
- remaining mixed gate

を 1 本に束ねる。

ここで fixed するのは、
**compare-floor で surviving していた Problem 1 の current first line を、near-end の actual adoption package として読む current judgment**
であり、

- final public verifier contract
- full strong type system
- final public typed surface
- concrete theorem / model-check production binding
- settled property language finalization

は fixed しない。

## applied defaults

current package では、user-authorized default として次を採る。

1. stronger typed surface は early source principal に上げない
2. checker-adjacent principal + source-visible structural marker family first を維持する
3. theorem-first external integration target を current default にする
4. notebook-first theorem line + row-local model-check carrier first を維持する
5. final public verifier contract はこの package で採らない
6. concrete tool binding は theorem-first experimental pilot までに留める

## source-backed floor

current repo では、少なくとも次が source-backed にある。

1. verifier-boundary 4-way split
   - `core_static_checker`
   - `theorem_prover_boundary`
   - `protocol_verifier_boundary`
   - `runtime_policy_boundary`
2. typed current first line
   - checker-adjacent semantic carrier principal
   - source-visible structural marker family first
   - request / predicate / `try` cluster grouped reserve
3. theorem current first line
   - notebook-first review unit first
   - symbolic-ref-only admissible evidence floor
   - abstract discharge-entry reserve
4. model-check current first line
   - row-local machine-facing carrier first
   - same-subject stage-local small-cluster projection reserve
   - property-language seam / tool-binding seam later
5. helper-local compare floor
   - verifier preview alignment pre-floor
   - theorem discharge pre-floor
   - model-check projection pre-floor
6. corrected runnable prototype
   - `p06-typed-proof-owner-handoff`

## actual runnable evidence

| evidence | current reading |
|---|---|
| `p06-typed-proof-owner-handoff` CLI success | typed/theorem/model-check bridge floor が runnable |
| helper-local `verification_preview` | `formal_hook_status = reached` を sample-visible に確認できる |
| helper-local `artifact_preview` | review-unit / model-check carrier derived row を sample-visible に確認できる |
| `current_l2_verifier_preview_alignment` | preview と sample-local preview-aligned typed artifact route の compare floor が green |
| `current_l2_theorem_discharge_prefloor` | review-unit / discharge-entry reserve / transport seam / public-contract seam の compare floor が green |
| `current_l2_model_check_projection_prefloor` | row-local carrier / small-cluster projection / property/tool seam の compare floor が green |

## actual adoption package

### typed line

current actual adoption package では、

- principal source = checker-adjacent semantic carrier
- first source-visible family = structural markers
- `p06` = corrected runnable prototype / bridge-floor evidence

に置く。

したがって、
**stronger typed surface を source principal に上げることは current package では採らない。**

### theorem line

current actual adoption package では、

- theorem-first external integration target
- notebook-first review unit first
- symbolic `evidence_refs` first
- abstract discharge-entry reserve

に置く。

current theorem-first pilot は、
**public theorem contract ではなく、experimental pilot line**
として次を first package に置くのが自然である。

1. `proof_notebook_review_unit`
2. sample-local preview-aligned typed artifact route
3. theorem discharge pre-floor
4. repo-local emitted artifact / compare floor

### model-check line

current actual adoption package では、

- theorem-first target を current default に採るため、
  model-check line は second line に置く
- principal source は引き続き row-local machine-facing carrier
- property language / concrete tool seam は still later

と読む。

ここでいう second line は「捨てる」ではなく、
**current first package で finalization しない**
という意味である。

## current recommendation

1. Problem 1 の near-end actual adoption package は、
   - checker-adjacent semantic carrier principal
   - source-visible structural marker family first
   - theorem-first external integration target
   - notebook-first theorem line
   - row-local model-check carrier first
   に置く。
2. `p06` と 3 本の pre-floor test は、
   final public verifier contract の evidence ではなく、
   current bridge / compare floor の executable evidence と読む。
3. theorem-first experimental pilot は、
   repo-local emitted artifacts + reproducible compare floor までを次 package にしてよい。
4. stronger typed surface actual promotion、theorem public contract、settled property language、concrete tool brand は still later に残す。

## retained alternatives

- stronger typed surface actual promotion
- dedicated attachment block
- unified obligation block
- theorem export checker first
- public verifier contract first
- model-check-first external integration
- settled property language early adoption
- concrete model-check tool brand early adoption

## stop line

current package は次で止める。

- stronger typed surface source-principal adoption
- final public typed surface
- final public verifier contract
- actual discharge transport format
- proof object public schema
- concrete theorem prover production binding
- first settled property language
- concrete model-check production binding

## remaining mixed gates

- stronger typed-surface experimental adoption gate
- theorem discharge transport seam gate
- theorem public-contract seam gate
- theorem prover experimental brand/binding gate
- settled property language gate
- model-check concrete tool-seam gate

## historical package-local next self-driven line

historical package-local next line としては
`theorem-first experimental pilot actualization`
を `specs/examples/470` に進める chain が compare-anchor memory に残る。

minimum として保持していた

1. notebook-first review-unit principal
2. symbolic refs only
3. repo-local emitted artifact route
4. reproducible compare floor
5. no public verifier contract promotion

も package-memory の follow-up shape として読める。

ただし current repo-level queue authority は `progress.md` / `tasks.md` にあり、
2026-04-30 current-line maintenance closeout 後に
この package から追加の self-driven implementation line を promote しない。
