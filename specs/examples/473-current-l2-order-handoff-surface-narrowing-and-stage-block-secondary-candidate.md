# 473 — current L2 order-handoff surface narrowing and stage-block secondary candidate

## 目的

`specs/examples/467-current-l2-problem2-actual-adoption-package-and-authoritative-room-default-profile.md`、
`468-current-l2-syntax-modality-convergence-and-current-recommendation.md`、
`472-current-l2-minimal-companion-experimental-order-handoff-surface.md`
を前提に、

- order / handoff source-facing recommendation の narrowing
- explicit edge-row continuation principal
- `stage` / `after` / `witness` family の secondary experimental candidate
- authoritative-room-only `serial` sugar reserve

を 1 本に整理する。

ここで fixed するのは、
**current source principal を final grammar に上げずに、surviving wording candidate を principal / secondary / reserve へ狭める current judgment**
であり、

- final parser grammar
- final source-surface handoff wording
- final emitted-artifact schema
- authoritative-room serial sugar adoption
- low-level `memory_order` exact source surface

は fixed しない。

## source-backed floor

current repo では、少なくとも次が source-backed である。

1. relation decomposition principal
2. semantic honesty > compactness
3. explicit edge-row / vertical continuation family
4. low-level `std::memory_order` / `std::kill_dependency` family は retained-later reference
5. `p07` / `p08` authoritative-room default profile は corrected runnable prototype
6. minimal companion helper surface は helper-local compare floor に actualize 済み

## current narrowing

current repo は、order / handoff surface を次に狭める。

### principal

- explicit edge-row continuation
- readable high-level relation vocabulary
  - `publish`
  - `observe`
  - `handoff`
  - `witness`
- continuation line で order / obligation を見せる

### secondary experimental candidate

- `stage` / `after` / `witness` family
- `stage-block + witness` は helper-local compare floor で actualize してよい
- principal source wording には上げない

### reserve candidate

- authoritative-room-only `serial` / `serial on <owner>` sugar
- room-profile-specific high-level sugar として reserve に残す
- current principal source wording には上げない

## actual runnable evidence

| evidence | current reading |
|---|---|
| `current_l2_order_handoff_minimal_companion_surface` | edge-row / vertical continuation principal の helper-local actualization |
| `current_l2_order_handoff_stage_block_surface` | `stage-block + witness` を secondary experimental candidate として helper-local actualization |
| `p07-dice-late-join-visible-history` | `publish -> handoff -> observe` line を principal / secondary candidate の両方で比較できる |
| `p08-dice-stale-reconnect-refresh` | rollback / refresh / replay invalidation を principal / secondary candidate の両方で比較できる |
| `p05-dice-owner-guarded-chain` | reached と guard-only を混同せずに止められる |

## current recommendation

1. source principal は explicit edge-row continuation に置く。
2. readable high-level relation vocabulary を source-facing recommendation に保つ。
3. `stage-block + witness` は strong secondary experimental candidate に留める。
4. authoritative-room-only `serial` sugar は reserve に留める。
5. low-level `memory_order` exact surface は retained-later reference に留める。
6. `◯` / `□` のような modal token を current source principal には出さず、必要なら後段で modal foundation へ lower する。

## retained alternatives

- `stage-block + witness` principal promotion
- authoritative-room `serial` sugar promotion
- richer companion block notation
- packed metadata line
- low-level fence / memory-order wording import
- final grammar adoption

## stop line

current package は次で止める。

- final parser grammar
- final source-surface handoff wording
- final emitted-artifact schema
- authoritative-room serial sugar adoption
- low-level exact source wording
- final modal foundation adoption

## remaining mixed gates

- edge-row principal と stage-block secondary candidate のどこまでを final public companion に mirror するか
- `witness(...)` clause と replay / payload contract をどこで public row に上げるか
- `serial` sugar を authoritative-room default へ昇格させるか
- final handoff wording / emitted-artifact schema / modal marker gate

## next self-driven line

current next self-driven line は、

1. reserve strengthening:
   `auditable_authority_witness`
2. reserve practical package:
   `delegated_rng_service`
3. model-check second-line concretization

を優先しつつ、
surface narrowing 自体は current helper-local compare floor で保持する読みが自然である。
