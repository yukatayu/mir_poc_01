# 459 — current L2 verifier-boundary and typed/theorem/model-check current first-line integration

## 目的

`specs/examples/126`、`127`、`133`、`134`、`135`、`413`、`418`、`433`、`439`、`440`、`445`、`446`、`447` と
`p06-typed-proof-owner-handoff` を前提に、

- verifier-boundary / typed-core / theorem-side / model-check side の current first line
- retained alternative families
- stop line
- remaining mixed gate

を 1 本の current integration note に束ねる。

ここで fixed するのは **Package B の current first-line / stop-line integration judgment** であり、

- full strong type system
- final public verifier contract
- concrete theorem prover / model-check tool binding
- settled property language adoption

は fixed しない。

## scope

- typed / theorem / model-check の current first line だけを扱う。
- mainline actualization と theory-lab line は分けて書く。
- `p06` は historical sample-visible corrected prototype anchor として扱い、current clean-near-end theorem/model-check live floor、final typed calculus、final public verifier contract と同一視しない。
- order / handoff / syntax / modality の current package close は扱わない。

## source-backed floor

current repo では、少なくとも次が source-backed にある。

1. 4-way verifier boundary
   - `core_static_checker`
   - `theorem_prover_boundary`
   - `protocol_verifier_boundary`
   - `runtime_policy_boundary`
2. typed-core current line
   - checker-adjacent semantic carrier principal
   - source-visible structural marker family first
   - request / predicate / `try` cluster grouped reserve
   - two-tier split
3. theorem-side current line
   - semantic-core lemma family first
   - notebook-first consumer threshold
   - symbolic-ref-only admissible evidence floor
   - abstract discharge-entry reserve
4. model-check current line
   - row-local machine-facing carrier first
   - same-subject stage-local small-cluster projection reserve
   - property-language seam / tool-binding seam later-gate framing
5. historical sample-visible corrected prototype anchor
   - `p06-typed-proof-owner-handoff`
   - helper-local `verification_preview`
   - helper-local `artifact_preview`

## Package B で閉じる問い

current question は、

- stronger typed surface を first public recommendation に上げるか
- theorem discharge / public verifier contract を current contract へ近づけるか
- model-check property language / concrete tool seam を current package で fixed とみなすか

ではない。

current question は、

- どこまでが current first line か
- 何を retained alternative family として残すか
- どこで stop line を引くか

を source-backed にまとめることである。

## property-to-boundary matrix

| property family | principal boundary / source | current reading | sample / preview evidence | not promoted here |
|---|---|---|---|---|
| declared target / lineage / capability structural floor | `core_static_checker` | local / structural / decidable floor に残す | authored current-l2 static-stop rows | theorem / protocol / runtime policy obligation への collapse |
| typed attachment and source-visible cue | checker-adjacent semantic carrier | principal source は checker attachment、source-visible marker は mirror | `capability` / `lease` / `admit` / lineage / `require` / `ensure`、`p06` source text | dedicated typed block、unified obligation block、final typed syntax |
| canonical normalization / no re-promotion / rollback-cut non-interference | `theorem_prover_boundary` | notebook-first theorem line + symbolic-ref-only evidence + abstract discharge-entry reserve | `proof_notebook_review_unit`、`p06` review-unit preview | theorem result adoption、public theorem/public verifier contract |
| row-local verification carrier | `protocol_verifier_boundary` のうち model-check reserve side | row-local machine-facing carrier first、same-subject stage-local small cluster reserve | `model_check_concrete_carriers`、`p06` carrier preview | settled property language、concrete tool seam、room protocol/fairness/replay adoption |
| activation / retry / retain / policy drift | `runtime_policy_boundary` | runtime policy に残す | current helper/runtime reports | theorem/model-check principal への混入 |

## typed-surface candidate comparison

| candidate family | current status | current recommendation | retained later as |
|---|---|---|---|
| checker-adjacent semantic carrier | source-backed principal | first typed attachment candidate | principal source を維持 |
| explicit structural marker family | source-backed mirror | first source-visible candidate | source-visible grouped family |
| request / predicate / `try` cluster | source-backed reserve cue | grouped reserve に留める | stronger typed surface pressure の reserve family |
| dedicated attachment block | comparison only | current packageでは採らない | stronger typed-surface candidate |
| unified obligation block | comparison only | current packageでは採らない | stronger typed-surface candidate |
| theorem/model-check artifact promoted family | helper-visible derived family | current packageでは採らない | downstream derived family |

## theorem / model-check public-contract threshold note

### theorem-side

- first practical consumer class は `proof_notebook`
- minimum contract row core は `obligation_kind + evidence_refs`
- notebook first attachment は `goal_text` only
- abstract discharge-entry row は reserve に留める
- theorem discharge transport seam と public-contract seam は distinct な later gate に残す

### model-check side

- principal source は row-local machine-facing carrier
- same-subject stage-local small cluster は reserve keep line
- property language seam と tool-binding seam は adjacent later gate に残す
- theorem / typed / room protocol family を current model-check principal に混ぜない

## `p06` の位置づけ

`p06-typed-proof-owner-handoff` は、

- typed marker を含む corrected runnable prototype
- theorem / model-check current-first-line bridge memory の historical sample-visible anchor
- helper-local `verification_preview` / `artifact_preview` の実行 anchor

である。

current CLI output では、

- `formal_hook_status = reached`
- `subject_kind = runtime_try_cut_cluster`
- `proof_notebook_review_unit_obligations = rollback_cut_non_interference`
- `model_check_concrete_carrier_obligations = rollback_cut_non_interference`

が見える。

したがって `p06` は、

- full typed calculus
- final theorem discharge result
- final public verifier contract
- settled property language

の evidence ではなく、
**current-first-line bridge memory が sample-visible で reached まで動く**
ことの evidence と読む。

## current judgment

1. typed / theorem / model-check の current first line は、
   - checker-adjacent semantic carrier principal
   - source-visible structural marker family first
   - notebook-first theorem line
   - row-local model-check carrier first
   に置く。
2. `p06` は historical sample-visible corrected prototype anchor に留める。
3. helper-local `verification_preview` / `artifact_preview` は current-first-line bridge memory の evidence として使ってよいが、current clean-near-end theorem/model-check live floor や public contract に昇格させない。
4. stronger typed surface actual adoption、theorem discharge result / public-contract concretization、settled property language / concrete tool seam は mixed gate に残す。

## retained alternatives

- stronger typed surface actual promotion
- dedicated attachment block
- unified obligation block
- proof assistant adapter first
- theorem export checker first
- abstract discharge-result family
- settled property language adoption
- concrete model-check tool seam

## stop line

current package は次で止める。

- full strong type system
- inference / annotation design
- final typed syntax / public typed API
- concrete theorem prover brand
- proof object public contract
- actual discharge transport format
- first settled property language
- concrete model-check tool brand / schema
- final public verifier contract

## remaining mixed gates

- stronger typed-surface actual adoption gate
- theorem discharge transport seam gate
- theorem public-contract seam gate
- model-check property-language seam gate
- model-check concrete tool seam gate

## current package close

Package B は、current first line / retained alternatives / stop line / mixed gate を
`p06` evidence と 4-way verifier-boundary matrix の上で再統合した時点で close と読む。

next promoted line は `Package C` order / handoff relation decomposition である。
