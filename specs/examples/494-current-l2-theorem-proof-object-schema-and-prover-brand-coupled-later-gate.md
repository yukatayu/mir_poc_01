# 494 — current L2 theorem proof-object schema and prover-brand coupled later gate

## 目的

`specs/examples/474`、
`487`、
`491`
と
`e5-underdeclared-lineage`、
`p05-dice-owner-guarded-chain`、
`p06-typed-proof-owner-handoff`、
`p07-dice-late-join-visible-history`、
`p08-dice-stale-reconnect-refresh`
を前提に、

- proof-object-schema coupled later gate
- prover-brand coupled later gate
- result-object preview keep
- brand-neutral preflight anchor keep
- repo-local emitted artifact refs first
- actual runnable evidence
- retained alternatives
- stop line

を 1 本に束ねる。

ここで actualize するのは、
**theorem result-object preview / proof-object-schema reserve actualization と theorem-prover experimental binding preflight を保ったまま、proof-object schema side と prover-brand side を adjacent but distinct later gate として helper-local manifest に actualize する current cut**
であり、

- final public theorem result object
- consumer-shaped theorem payload public contract
- concrete theorem prover brand adoption
- proof object public schema adoption
- final public verifier contract

は fixed しない。

## source-backed floor

current repo では、少なくとも次が source-backed である。

1. theorem review-unit transport / notebook-contract actual adoption
   - review-unit transport first
   - notebook-consumer contract first
   - brand-neutral binding reserve keep
2. theorem result-object preview / proof-object-schema reserve actualization
   - notebook-consumer object first
   - consumer-shaped payload preview only
   - proof-object-schema reserve keep
   - brand-neutral binding reserve keep
3. theorem-prover experimental binding preflight
   - brand-neutral theorem request manifest
   - adapter-boundary refs
4. theorem-first pilot actualization
   - symbolic `evidence_refs`
   - repo-local emitted artifact refs

したがって current open problem は、
proof-object schema / prover-brand line を compare-only の later reserve として保持し続けることではなく、
**proof-object schema side と prover-brand side をどこまで coupled-later gate として current recommendation に上げるか**
である。

## current coupled-later cut

current package では、次を採る。

1. proof-object-schema side は
   - result-object preview adjacent
   - refs-only public-schema candidate
   - public contract not adopted
   に置く
2. prover-brand side は
   - brand-neutral preflight anchor keep
   - adapter-boundary refs keep
   - concrete brand not adopted
   に置く
3. proof-object-schema side と prover-brand side は adjacent に読むが、collapse しない
4. result-object preview keep は current compare floor として carry-over してよいが、
   final public theorem result object と同一視しない
5. reached sample は `e5 / p06 / p07 / p08` に取り、`p05` は guard-only に残す

## actual runnable evidence

| evidence | current reading |
|---|---|
| `current_l2_theorem_proof_object_schema_prover_brand_coupled_later_gate` | `e5 / p06 / p07 / p08` reached、`p05` guard-only の coupled-later manifest を machine-check する |
| `current_l2_theorem_result_object_preview_actualization` | result-object preview keep と proof-object-schema reserve keep を compare floor として carry-over する |
| `current_l2_theorem_prover_binding_preflight` | brand-neutral preflight anchor と adapter-boundary refs keep を compare floor として carry-over する |
| `current_l2_theorem_review_unit_transport_actual_adoption` | review-unit transport first / notebook-consumer contract first を theorem-side source-backed floor として再確認する |
| `current_l2_source_sample_runner` / `current_l2_operational_cli` | representative runtime / static corpus の runnable floor 自体は引き続き green である |

## actualization shape

current helper-local coupled-later manifest は少なくとも次を持つ。

- `actualization_subject_kind` / `actualization_subject_ref`
- `proof_object_schema_candidate_refs`
- `prover_brand_candidate_refs`
- `coupled_default_refs`
- `repo_local_emitted_artifact_refs`
- `compare_floor_refs`
- `guard_refs`
- `kept_later_refs`

### proof-object schema candidate refs

current helper-local cut では、proof-object-schema side を

- `theorem_proof_object_schema_candidate:<subject_ref>:result_object_preview_adjacent`
- `theorem_proof_object_schema_candidate:<subject_ref>:refs_only_public_schema_candidate`
- `theorem_proof_object_schema_candidate:<subject_ref>:public_contract_not_adopted`

として actualize する。

これは proof object public schema adoption ではない。

### prover brand candidate refs

current helper-local cut では、prover-brand side を

- `theorem_prover_brand_candidate:<subject_ref>:brand_neutral_preflight_anchor`
- `theorem_prover_brand_candidate:<subject_ref>:adapter_boundary_refs_keep`
- `theorem_prover_brand_candidate:<subject_ref>:concrete_brand_not_adopted`

として actualize する。

これは concrete theorem prover brand adoption ではない。

## current recommendation

1. theorem line は、proof-object schema side と prover-brand side を coupled-later gate として current recommendation に上げてよい。
2. current package は
   - result-object preview keep
   - refs-only public-schema candidate only
   - brand-neutral preflight anchor keep
   - concrete brand not adopted
   - final public contract later
   に置くのが自然である。
3. `e5 / p06 / p07 / p08` reached、`p05` guard-only の組み合わせは semantically honest である。
4. current package を final public theorem result object、consumer-shaped theorem payload public contract、concrete theorem prover brand adoption、proof object public schema adoption、final public verifier contract に昇格させない。

## retained alternatives

- final public theorem result object first adoption
- consumer-shaped theorem payload public contract first adoption
- concrete theorem prover brand first adoption
- proof object public schema first adoption
- theorem result / proof schema / prover brand unified public payload

## stop line

current package は次で止める。

- final public theorem result object
- consumer-shaped theorem payload public contract
- concrete theorem prover brand
- proof object public schema
- final public verifier contract

## next line

theorem line の remaining mixed gate は、

1. final public theorem result object / consumer-shaped theorem payload public contract
2. concrete theorem prover brand / proof object public schema
3. final public verifier contract

として kept-later に残す。
