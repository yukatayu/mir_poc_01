# 484 — current L2 order-handoff surface / artifact threshold default

## 目的

`specs/examples/467`、
`471`、
`472`、
`473`、
`476`、
`477`、
`483`
と
`p05-dice-owner-guarded-chain`、
`p07-dice-late-join-visible-history`、
`p08-dice-stale-reconnect-refresh`
を前提に、

- order-handoff source wording / emitted-artifact threshold default
- edge-row / vertical continuation principal keep
- `stage` / `after` / `witness` secondary keep
- repo-local emitted artifact refs first
- helper-local threshold actualization cut
- actual runnable evidence
- retained alternatives
- stop line

を 1 本に束ねる。

ここで actualize するのは、
**authoritative-room vertical slice、minimal companion surface、stage-block secondary surface、witness/provider/artifact public-shape threshold を保ったまま、final source-surface handoff wording / final emitted-artifact schema mixed gate の current default threshold を helper-local に actualize する current cut**
であり、

- final parser grammar
- final public parser / checker / runtime API
- final source-surface handoff wording
- final emitted-handoff contract
- final public witness schema
- authoritative-room `serial` sugar adoption
- low-level `memory_order` exact source surface

は fixed しない。

## source-backed floor

current repo では、少なくとも次が source-backed である。

1. Problem 2 actual adoption package
   - relation decomposition principal
   - authoritative-room first actual adoption profile
   - low-level family retained-later reference
2. authoritative-room vertical-slice actualization
   - profile axis refs
   - repo-local emitted artifact refs
3. minimal companion surface
   - explicit edge-row / vertical continuation principal
   - readable relation vocabulary
4. stage-block secondary surface
   - `stage` / `after` / `witness` strong secondary candidate
5. witness/provider/artifact public-shape threshold default
   - repo-local emitted artifact refs first
   - optional attachment refs only
   - combined public contract later

したがって current open problem は、
source wording や emitted-artifact reserve line が何も無いことではなく、
**edge-row principal / stage-block secondary / repo-local emitted artifact refs first の current default threshold をどこで止めるか**
である。

## current actualization cut

current package では、次を採る。

1. principal source は引き続き explicit edge-row / vertical continuation に置く
2. secondary source は
   - `stage`
   - `after`
   - `witness`
   familyに留める
3. emitted-artifact side は
   - repo-local emitted artifact refs first
   - final public witness/provider/artifact contract later
   に分ける
4. authoritative-room `serial` sugar と low-level exact source wording は reserve / kept-later に留める
5. reached sample は `p07 / p08` に取り、`p05` は guard-only に残す

## actual runnable evidence

| evidence | current reading |
|---|---|
| `python3 scripts/clean_near_end_samples.py run order-handoff --format json` | current clean near-end order-handoff family が旧 `p07` / `p08` reached と `p05` guard-only の threshold reading を carry-over していることを再確認する |
| `cargo run -q -p mir-runtime --bin mir-clean-near-end -- run-sample 01_authorized_roll_publish_handoff --format json` | edge-row / vertical continuation principal を threshold が carry-over していることを再確認する |
| `cargo run -q -p mir-runtime --bin mir-clean-near-end -- run-sample 04_stage_block_authorized_handoff --format json` | `stage` / `after` / `witness` secondary candidate を threshold が carry-over していることを再確認する |
| `cargo run -q -p mir-runtime --bin mir-clean-near-end -- run-sample 01_authorized_roll_publish_handoff --format json` | profile axis refs と repo-local emitted artifact refs を threshold が保持することを再確認する |
| `python3 scripts/clean_near_end_samples.py closeout --format json` | final public witness/provider/artifact contract を採らず、repo-local emitted artifact refs first を threshold が保持することを再確認する |

## actualization shape

current helper-local threshold manifest は少なくとも次を持つ。

- `profile_axis_refs`
- `principal_surface_lines`
- `secondary_surface_lines`
- `repo_local_emitted_artifact_refs`
- `threshold_default_refs`
- `compare_floor_refs`
- `contrast_refs`
- `guard_refs`
- `kept_later_refs`

### threshold default refs

current helper-local cut では、threshold default を

- `surface_threshold_default:edge_row_vertical_continuation_principal`
- `surface_threshold_default:readable_high_level_relation_vocabulary`
- `surface_threshold_default:stage_block_secondary_candidate`
- `surface_threshold_default:repo_local_emitted_artifact_refs_first`
- `surface_threshold_default:witness_clause_public_row_later`

として actualize する。

これは final source wording でも final emitted-artifact schema でもなく、
final source-surface handoff wording / final emitted-artifact schema mixed gate の current default threshold である。

### guard refs

current helper-local cut では、threshold guard を

- `guard:surface_threshold_only`
- `guard:edge_row_principal`
- `guard:stage_block_secondary_only`
- `guard:repo_local_emitted_artifact_refs_first`
- `guard:final_wording_later`

として actualize する。

これは threshold package が止まる場所を明示する guard であり、
final source wording / final emitted-artifact schema adoption を意味しない。

## current recommendation

1. order-handoff source wording / emitted-artifact threshold package は、この threshold default で close してよい。
2. current default は
   - edge-row / vertical continuation principal
   - readable relation vocabulary
   - stage-block secondary candidate
   - repo-local emitted artifact refs first
   に置くのが自然である。
3. `p07 / p08` reached、`p05` guard-only の組み合わせは semantically honest である。
4. final source-surface handoff wording / final emitted-artifact schema 自体は still later mixed gate に残し、current threshold manifest を final public wording や final public witness/provider/artifact contract に昇格させない。

## retained alternatives

- stage-block principal promotion
- authoritative-room `serial` sugar promotion
- packed metadata line
- low-level exact source wording import
- combined public contract first adoption

## stop line

current package は次で止める。

- final parser grammar
- final public parser / checker / runtime API
- final source-surface handoff wording
- final emitted-handoff contract
- final public witness schema
- authoritative-room `serial` sugar adoption
- low-level exact source wording
- final modal foundation adoption

## current kept-later mixed-gate status

current threshold package を close した後の later mixed gate は、

1. final source-surface handoff wording actual adoption
2. final emitted-artifact / witness-provider public contract actual adoption
3. final modal foundation / source marker adoption

として kept-later に残す。
