# 351 — current L2 second-source-sample-cluster-sequencing-ready actual-e22-contrast-row-source-actualization comparison

## 目的

`specs/examples/350-current-l2-second-source-sample-cluster-sequencing-ready-minimal-second-source-sample-cluster-sequencing-threshold.md`
で first post-sextet cluster minimum を fixed した次段として、

- `e22-try-atomic-cut-place-mismatch` をどの shape で actual source row に上げるか
- current lowerer / runner / regression / ladder / runtime formal-hook smoke をどこまで同じ package で同期するか
- nested `place` block を current stage 2 bridge floor でどこまで受けるか

を比較する。

ここで固定するのは
**current L2 second-source-sample-cluster-sequencing-ready actual-e22-contrast-row-source-actualization comparison**
であり、

- stable static malformed post-contrast sequencing
- parser / checker / runtime public surface inventory
- concrete theorem / model-check tool binding

はまだ固定しない。

## scope

- entry criteria は `specs/examples/315...350` で fixed 済みの source corpus / lowering / runner / ladder / theorem-first pilot / second source-sample cluster sequencing に置く。
- current contrast pair `e21` / `e22` のうち、今回 actualize するのは deferred source row `e22` だけである。
- public parser API や final grammar を widen せず、current helper-local stage 2 bridge floor の extension に留める。

## current 前提

current repo では次が成立している。

1. `e22-try-atomic-cut-place-mismatch` は representative prose / fixture / runtime test / detached-loop contrast に already anchor を持つ。
2. current source-sample path では `e22` sample file、runner accepted set、regression inventory、ladder row がまだ未同期である。
3. `e22` source rowは nested `place profile_annotation { atomic_cut }` を含むため、current line-based stage 2 bridge parser は `place ... {` をそのまま読むと fail-closed に止まる。
4. semantic checker-floor summary では nested `PlaceBlock` 自体を top-level `Other` statement head として扱えば十分であり、`e22` は current `runtime_try_cut_cluster` family の内側に留まる。

したがって current 問いは、
**`e22` を current helper-local surfaceの中で actual source row に上げつつ、nested place block は stage 2 bridge では top-level `Other` として畳むのが最小か**
である。

## 比較観点

1. `e22` を source-authored runtime contrast row として narrow に actualize できるか
2. current stage 2 bridge floor を final grammar に見える shapeまで widen せずに済むか
3. runner / regression / ladder / formal-hook smoke を同じ task で同期できるか
4. stable static malformed post-contrast line へ clean に handoff できるか

## 比較対象

### 案 1. `e22` を helper-compatible source rowに actualize し、nested place block は stage 2 bridge で top-level `Other` として畳む

#### shape

```text
e22_actualization = {
  source_row = helper_compatible_nested_place_atomic_cut_mismatch,
  runner_status = accepted_source_sample,
  bridge_extension = {
    target = current_stage2_try_rollback_floor,
    nested_place_reading = top_level_other_statement_head,
    kept_non_goal = final_public_parser_contract
  },
  reached_stage = {
    static_gate = reached_valid,
    interpreter = reached_success,
    formal_hook = reached_runtime_try_cut_cluster
  }
}
```

#### 利点

- `e22` を current authored corpus に入れても `runtime_try_cut_cluster` current top を保てる。
- nested `place` block は stage 2 structural floor の内側で `Other` として扱えるため、new parser family を起こさずに済む。
- source file / runner / regression / ladder / smoke route を同じ narrow package で揃えられる。

#### 欠点

- stage 2 bridge parser には nested brace block を 1 statement に畳む helper-local extensionが入る。

### 案 2. `e22` は docs-only のまま残し、source-authored row は stable static malformed family の後に送る

#### 利点

- current code path の変更は減る。

#### 欠点

- `e21` / `e22` contrast pair が source-backed ladder で閉じない。
- stable static malformed sequencing の前に narrow runtime contrast row を閉じる staged line を崩す。

### 案 3. `e22` actualization と stable static malformed sequencing を同じ package にまとめる

#### 利点

- Macro 4 の remaining tasks は減る。

#### 欠点

- runtime contrast-row close と broader static-family sequencing が混線する。
- report / spec / snapshot の current judgmentが太くなりすぎる。

## current judgment

current L2 で最も自然なのは、
**案 1. `e22` を helper-compatible source rowに actualize し、nested place block は stage 2 bridge で top-level `Other` として畳む**
である。

理由は次の通り。

1. `e22` は current first post-sextet cluster の deferred half であり、先に source-backed contrast row として閉じるのが staged line に合う。
2. checker-floor summary が必要とするのは nested place block の exact tree ではなく top-level structural floor なので、`Other` folding で十分である。
3. stable static malformed family と public surface inventory は broader reopen line であり、ここに混ぜない方が current package は narrow である。

## current first choice details

- `samples/current-l2/e22-try-atomic-cut-place-mismatch.txt` を helper-compatible source rowとして actualize する。
- `mir_runtime::current_l2` accepted sample set、lowering ratchet、source sample runner、verification ladder に `e22` row を加える。
- `python3 scripts/current_l2_source_sample_regression.py` authored inventory / regression bundle に `e22` runtime formal-hook smoke を加える。
- current stage 2 bridge floor では nested `place` block を top-level `Other` statement head として読む。
- `e22` は `static gate = reached(valid)`、`interpreter = reached(success)`、`formal hook = reached(runtime_try_cut_cluster)` に置く。
- stable static malformed broader cluster と public surface inventory は still later に残す。

## next promoted line

next promoted line は、
**actual-e22-contrast-row-source-actualization-ready stable-static-malformed-post-contrast-sequencing comparison**
に置く。

## open questions

- stable static malformed family のどの subcluster を post-contrast second cluster に置くか
- parser / checker / runtime public surface inventory で parser-free public helper stack と current helper-local parser/runtime line をどう切り分けるか
- `e22` と同様の nested block pressure を later request-clause familyへどこまで generalize するか
