# 472 — current L2 minimal companion experimental order-handoff surface

## 目的

`specs/examples/468-current-l2-syntax-modality-convergence-and-current-recommendation.md`
と
`specs/examples/471-current-l2-authoritative-room-vertical-slice-emitted-artifact-ratchet.md`
を前提に、

- semantic honesty
- helper-local companion wording
- authoritative-room default profile
- `p07` / `p08` corrected prototype

を 1 package 分 actualize する。

ここで actualize するのは、
**final grammar を凍らせずに semantically honest な minimal companion lines を helper-local に切る current cut**
であり、

- final parser grammar
- final public parser / checker / runtime API
- low-level `memory_order` exact source surface
- final modal foundation adoption

は fixed しない。

## current actualization cut

current repo では、minimal companion surface を次に置く。

1. current reached sample は `p07` / `p08` のみ
2. source principal は relation decomposition / authoritative-room default profile のまま維持する
3. companion lines は helper-local output に留める
4. low-level exact token import は採らない
5. guarded sample は guard-only floor に残す

## actual runnable evidence

| evidence | current reading |
|---|---|
| `python3 scripts/clean_near_end_samples.py run order-handoff --format json` | clean near-end order-handoff family が旧 `p07` / `p08` reached と `p05` guard-only の minimal companion reading を carry-over していることを再確認する |
| `p07-dice-late-join-visible-history` | publication / handoff / late-join-visible-as-past を compact に潰しすぎず helper-local lines へ actualize できる |
| `p08-dice-stale-reconnect-refresh` | rollback / refresh / replay invalidation を helper-local lines へ actualize できる |
| `p05-dice-owner-guarded-chain` | companion surface を reached と偽装せず guard-only floor に残せる |

## helper-local surface shape

current helper-local surface は少なくとも次を持つ。

- `surface_status`
- `companion_lines`
- `compare_floor_refs`
- `guard_refs`
- `kept_later_refs`

current companion lines は例として次の family に留める。

- `profile authoritative_room_default`
- `activation authority-ack`
- `authority single_room_authority`
- `consistency authoritative_serial_transition`
- `rng authority_rng`
- publication / handoff / late_join / rollback / refresh / replay の semantically honest line

## current recommendation

1. minimal companion surface は helper-local / examples-support cut として actualize してよい。
2. companion lines は current recommendation の説明と compare floor を助けるためのものであり、final grammar ではない。
3. low-level `memory_order` exact surface や packed metadata wording は引き続き採らない。
4. guarded case を reached と見せる shortcut は current companion cut では採らない。

## retained alternatives

- richer companion block notation
- packed metadata line
- low-level fence / memory-order wording import
- final grammar adoption
- modal foundation reduction

## stop line

current package は次で止める。

- final parser grammar
- final public parser / checker / runtime API
- low-level exact source wording
- final modal foundation adoption
- full dependent / multimodal calculus adoption

## next self-driven line

syntax / modality line の next self-driven line は、
surface narrowing note `specs/examples/473` と theorem-prover experimental binding preflight `specs/examples/474` を close 済みと読んだ上で、
reserve strengthening package を優先しつつ、
companion surface 自体は helper-local cut に留める読みが自然である。
