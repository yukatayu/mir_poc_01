# 426 — current L2 proof artifact and bridge stop-line refresh

## 目的

theorem-side pilot で、

- tool-neutral formal hook
- row-local review unit
- docs-only notebook bridge sketch
- theorem discharge

の stop line を tighten する。

ここで fixed するのは
**review / bridge / discharge を collapse しない current cut**
であり、

- concrete theorem prover brand
- proof object public contract
- final review workflow

は still later に残す。

## source-backed floor

- current lemma order は
  `canonical_normalization_law -> no_re_promotion -> rollback_cut_non_interference`
  である。
- current first concrete consumer は notebook-first line に置く。
- current floor artifacts は
  formal hook、`proof_notebook_review_unit`、docs-only bridge sketch
  までである。

## current stop-line shape

```text
proof_bridge_stop_line = {
  formal_hook_refs,
  review_unit_refs,
  bridge_sketch_refs,
  threshold_refs,
  stop_line_refs,
  kept_later_refs
}
```

## current judgment

1. tool-neutral formal hook は proof object ではなく、
   theorem-side consumer への entry evidence に留める。
2. `proof_notebook_review_unit` は discharge result ではなく、
   review-oriented row-local consumer に留める。
3. docs-only bridge sketch は review unit aggregation line として扱い、
   concrete discharge transport や retained exchange body を fixed しない。
4. current first threshold は
   **human review checklist / walkthrough pressure**
   に置き、prover binding pressure を second later に残す。

## guard

- formal hook を proof object と書かない。
- review unit を theorem discharge result と書かない。
- concrete theorem prover brand を fixed しない。
- proof object public contract を fixed しない。

## next promoted line

next promoted line は、
**sample-visible theorem/model-check property summary wording**
に置く。

## what is not decided here

- admissible evidence contraction の exact cut
- concrete theorem prover brand
- actual discharge transport
- public checker migration timing
