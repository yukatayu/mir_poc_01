# 559 — current L2 phase6-parser-side-follow-up-package-sequencing threshold helper mirror

## 目的

`specs/examples/311-current-l2-phase6-reserve-formal-tool-binding-inventory-ready-phase6-parser-side-follow-up-package-sequencing-comparison.md`
と
`specs/examples/312-current-l2-phase6-parser-side-follow-up-package-sequencing-ready-minimal-phase6-parser-side-follow-up-package-sequencing-threshold.md`
で source-backed に残していた
parser-side follow-up package sequencing の current first choice を、
crate-level manifest と helper-local operational CLI summary に narrow に mirror する。

ここで actualize するのは
**phase6 parser-side follow-up package sequencing threshold helper mirror**
であり、

- shared single attachment frame の actual code shape
- request clause suite publicization
- perform head final public parser API
- source-sample corpus scope / file layout
- final public parser / checker / runtime surface

はまだ固定しない。

## helper mirror shape

current cut は、
次の 7 要素だけを inspectable にする。

```text
sequencing_kind
fixed_entry_criteria_refs
selected_next_package_ref
deferred_reopen_refs
minimum_guard_refs
next_comparison_target_ref
kept_later_refs
```

これにより、

1. shared single attachment frame を next parser-side package に置く
2. request clause suite / perform head / richer diagnostics / final grammar を deferred reopen に残す
3. source-sample path を parser-side follow-up cut の後段へ押し戻す
4. existing stage3 predicate fragment surface reuse を current guard に維持する
5. current task が actual code widening ではなく sequencing threshold である

という minimum を code と CLI summary から読める。

## actualization cut

- crate anchor:
  `CurrentL2Phase6ParserSideFollowupPackageSequencingManifest`
- runtime getter:
  `current_l2_phase6_parser_side_followup_package_sequencing_manifest()`
- test anchor:
  `crates/mir-runtime/tests/current_l2_parser_side_followup_package_sequencing_manifest.rs`
- helper preview:
  `actual_phase6_parser_side_followup_package_sequencing_threshold`

helper preview では、
`p07 / p08 / p09` representative shared-space trio が
`actual_phase6_reserve_formal_tool_binding_inventory_threshold`
reached 後にだけ
parser-side follow-up package sequencing threshold を `reached` として見せる。

## practical reading

- shared single attachment frame next-package selection は actual code promotion ではなく sequencing minimum である。
- request clause suite / perform head / richer diagnostics / final grammar は捨てず、deferred reopen として helper-local summary に残す。
- source-sample path はこの package で reopen せず、
  **phase6-parser-second-tranche-shared-single-attachment-frame-first-package**
  の後段に返す。

## stop line

- shared single attachment frame actual code shape
- request clause suite publicization
- perform head final public parser API
- source-sample corpus scope / file layout
- final public parser / checker / runtime surface

## next promoted line

next promoted line は、
**phase6-parser-second-tranche-shared-single-attachment-frame-first-package ratchet**
に置く。
