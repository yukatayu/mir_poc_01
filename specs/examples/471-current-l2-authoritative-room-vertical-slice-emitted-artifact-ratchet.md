# 471 — current L2 authoritative-room vertical-slice emitted-artifact ratchet

## 目的

`specs/examples/467-current-l2-problem2-actual-adoption-package-and-authoritative-room-default-profile.md`
で fixed した Problem 2 actual adoption package を前提に、

- authoritative-room first default profile
- `p07` / `p08` corrected prototype
- room-default evidence refs
- append-friendly contrast target

を 1 package 分 actualize する。

ここで actualize するのは、
**authoritative-room default profile を helper-local vertical-slice manifest へ ratchet する current cut**
であり、

- final source-surface handoff wording
- final emitted-artifact schema
- final public handoff contract
- exhaustive shared-space catalog

は fixed しない。

## current actualization cut

current repo では、authoritative-room vertical-slice helper を次に置く。

1. current reached sample は `p07` / `p08` のみ
2. profile axis は
   - `authority-ack`
   - `single room authority`
   - `authoritative serial transition`
   - `authority_rng`
   - late join visible history as past / stale reconnect fail then refresh / replay invalidation
3. relation decomposition principal と authority-handoff refs を manifest に残す
4. repo-local emitted artifact refs と runtime evidence refs を同じ helper-local cut に束ねる
5. append-friendly room は contrast target として残す

## actual runnable evidence

| evidence | current reading |
|---|---|
| `current_l2_authoritative_room_vertical_slice_actualization` | vertical-slice helper が `p07` / `p08` reached と `p05` guard-only を machine-check する |
| `p07-dice-late-join-visible-history` | published history visible as past を authoritative-room default profile で actualize できる |
| `p08-dice-stale-reconnect-refresh` | stale reconnect fail-then-refresh と replay invalidation を actualize できる |
| `p05-dice-owner-guarded-chain` | current default room profile へは reached しない guarded case を guard-only floor に保てる |

## actualization shape

current helper-local vertical-slice manifest は少なくとも次を持つ。

- `slice_status`
- `profile_axis_refs`
- `relation_refs`
- `authority_handoff_refs`
- `runtime_evidence_refs`
- `repo_local_emitted_artifact_refs`
- `compare_floor_refs`
- `contrast_refs`
- `guard_refs`
- `kept_later_refs`

`kept_later_refs` は current cut で少なくとも次を保持する。

- `auditable_authority_witness`
- `delegated_rng_service`
- `distributed_randomness_provider`
- final emitted handoff contract
- exhaustive shared-space catalog

## current recommendation

1. authoritative-room default profile は helper-local vertical-slice manifest まで actualize してよい。
2. `p07` / `p08` は final replay theorem や final catalog の evidence ではなく、current default profile の runnable evidence と読む。
3. relation decomposition principal と low-level retained-later mapping の区別を保ったまま vertical slice を組む。
4. append-friendly room は contrast target として retained し、authoritative-room default profile を shared-space exhaustive default と誤読しない。

## retained alternatives

- `auditable_authority_witness` strengthening
- `delegated_rng_service` practical package
- low-level wording import
- exhaustive shared-space catalog
- distributed fairness theorem

## stop line

current package は次で止める。

- final source-surface handoff wording
- final emitted-artifact schema
- final public handoff contract
- exhaustive shared-space catalog
- distributed fairness theorem
- provider receipt / attestation public schema

## next self-driven line

Problem 2 側の next self-driven line は、次の 2 本に分かれる。

1. `auditable_authority_witness` strengthening
2. `delegated_rng_service` practical package

minimal companion / experimental order-handoff surface は、
source wording 側の next package として別 line で扱う。
