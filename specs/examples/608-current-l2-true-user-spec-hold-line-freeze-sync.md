# 608. current-l2 true user-spec hold line freeze sync

## 位置づけ

- current Phase 6 / Package 135 closeout。
- `specs/examples/469`、`605`、`606`、`607` の次段として、
  shared-space exhaustive final catalog、
  installed-binary / packaging / FFI / engine adapter / host integration target、
  upper-layer application target beyond authoritative-room first scenario
  を
  `hold-line`
  helper へ再同期する。
- final public parser / checker / runtime API、final public verifier contract、
  final public witness/provider/artifact contract、concrete theorem/model-check production binding を fixed する task ではない。

## この package で固定する current cut

1. `python3 scripts/current_l2_guided_samples.py hold-line`
   は true user-spec residual を explicit hold line として示してよい。
2. `closeout` / `reserve`
   は closeout 用 numbered queue を空にし、true user-spec residual については
   `hold-line`
   command へ案内してよい。
3. current live queue は
   reserve integration reopen
   later mixed gate reopen
   true user-spec hold line
   に移してよい。

## current recommendation

- true user-spec residual は mixed gate や reserve integration lane と混ぜず、
  explicit hold line として actualize してよい。
- current cut は
  - repo-local runnable CLI + tests + emitted artifacts + reproducible compare floor
  - authoritative-room minimal working subset first
  - theorem-first external integration target first
  を near-end success default として保ったまま、
  user が決めるべき残件だけを helper-local hold line に集約するところに留める。
- exhaustive shared-space catalog、installed-binary / packaging / FFI / engine adapter / host integration target、
  upper-layer application target beyond authoritative-room first scenario を self-driven queue へ戻さない。

## actualized evidence

- helper tests:
  - `python3 -m unittest scripts.tests.test_current_l2_guided_samples.CurrentL2GuidedSamplesTests.test_once_through_closeout_summary_json_contains_next_packages scripts.tests.test_current_l2_guided_samples.CurrentL2GuidedSamplesTests.test_reserve_integration_summary_json_contains_package_ids_and_next_queue scripts.tests.test_current_l2_guided_samples.CurrentL2GuidedSamplesTests.test_true_user_spec_hold_line_text_mentions_defaults_and_reopen_triggers scripts.tests.test_current_l2_guided_samples.CurrentL2GuidedSamplesTests.test_true_user_spec_hold_line_json_contains_items_and_hold_command scripts.tests.test_current_l2_guided_samples.CurrentL2GuidedSamplesTests.test_main_hold_line_command_uses_renderer`
- helper commands:
  - `python3 scripts/current_l2_guided_samples.py hold-line`
  - `python3 scripts/current_l2_guided_samples.py hold-line --format json`
  - `python3 scripts/current_l2_guided_samples.py closeout --format json`
  - `python3 scripts/current_l2_guided_samples.py reserve --format json`

## stop line

- exhaustive shared-space final catalog adoption
- installed-binary / packaging adoption
- concrete FFI / engine adapter / host integration target adoption
- upper-layer application target beyond authoritative-room first scenario

## retained-later line

- reserve integration reopen
- later mixed gate reopen
- final public seam adoption
