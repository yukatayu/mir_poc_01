# 468 — current L2 syntax / modality convergence and current recommendation

## 目的

`faq_007.md`、`specs/examples/461`、`plan/06-surface-notation-status.md` と
Problem 1 / Problem 2 の actual adoption package を前提に、

- syntax / semantics coupling principle
- syntax candidate evaluation axes
- modal foundation current recommendation
- order/handoff wording との接点
- retained alternatives
- stop line
- remaining mixed gate

を near-end closeout に必要な current recommendation として整理する。

ここで fixed するのは、
**syntax / modality line を compare-floor で放置せず、current recommendation に寄せる current judgment**
であり、

- final parser grammar
- final adopted modal foundation
- final source-surface modality markers
- full dependent calculus

は fixed しない。

## applied defaults

current package では、user-authorized default として次を採る。

1. `lambda-circle-box` は partial basis candidate のままにする
2. guarded / MDTT / MTT / Fitch-style multimodal は stronger candidate family に残す
3. final calculus adoption はこの task で行わない
4. order/handoff current wording choiceを支える範囲で current recommendation を出す
5. minimal companion / experimental surface は semantically honest なら refining してよいが、final parser grammar にしない

## current recommendation

### syntax / semantics coupling principle

current principal は次である。

1. 自然に書けるものは自然な挙動をするべきである
2. 理論的に破綻した動作は書けない / 書きにくいべきである
3. semantic honesty を compactness より優先する
4. packed metadata / implicit precedence / outer-inner drift を current principal に採らない

### syntax candidate evaluation axes

current principal axis は次の 5 つである。

1. semantic honesty
2. checker legibility
3. modal adequacy
4. misreading resistance
5. lowering friendliness

### modal foundation reading

| family | current role |
|---|---|
| `lambda-circle-box` | partial basis candidate |
| guarded lambda-calculus | retained stronger family |
| MDTT | retained stronger family |
| MTT | retained stronger family |
| Fitch-style multimodal | retained stronger family |

current repo が採るのは、
**partial basis + stronger family parallel keep**
である。

### order/handoff wording との接点

current syntax/modality package は、
Problem 2 の current wording choiceを次の形で支える。

- relation decomposition principal を許す
- low-level exact token import を要求しない
- explicit row / explicit stage / explicit order family を許す
- minimal companion / experimental wording を semantically honest な範囲で整える

## retained alternatives

- early reduction to guarded
- early reduction to MDTT / MTT
- `lambda-circle-box` only hold
- compact syntax / hidden precedence / packed metadata row
- final parser-first notation reduction

## stop line

current package は次で止める。

- final parser grammar
- final adopted modal foundation
- final source-surface modality markers
- full dependent calculus
- concrete checker / theorem / runtime integration timing finalization

## remaining mixed gates

- final foundation adoption gate
- final source-surface modality marker gate
- final parser/public API gate
- minimal companion / experimental surface gate

## current judgment

1. syntax / modality line は compare-floor のまま維持せず、
   current recommendation まで上げてよい。
2. `lambda-circle-box` を partial basis に留める current line は near-end closeout に十分である。
3. stronger family は keep するが、this task で final reduction しない。
4. order/handoff source-facing recommendation は、
   minimal companion / experimental surface を semantically honest な範囲で整えるところまで前進してよい。
