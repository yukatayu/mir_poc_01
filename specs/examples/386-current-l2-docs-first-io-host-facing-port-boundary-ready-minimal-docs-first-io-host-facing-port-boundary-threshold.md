# 386 — current L2 docs-first-io-host-facing-port-boundary-ready minimal-docs-first-io-host-facing-port-boundary threshold

## 目的

`specs/examples/385-current-l2-sample-facing-theorem-model-check-evidence-summary-and-bless-review-flow-ready-docs-first-io-host-facing-port-boundary-comparison.md`
で docs-first I/O / host-facing port boundary の current first choice を fixed した次段として、

- current docs-only boundary の minimum をどこまでに留めるか
- consumer/provider 側、guard、kept-later refs をどこまで threshold に残すか
- next line を stable malformed missing-option first reopen actualization comparison へどう handoff するか

を比較する。

ここで固定するのは
**current L2 docs-first-io-host-facing-port-boundary-ready minimal-docs-first-io-host-facing-port-boundary threshold**
であり、

- actual host interface contract
- FFI / engine adapter actualization
- final terminology selection

はまだ固定しない。

## 比較観点

1. docs-first boundary の current cut を lossless に minimum へ残せるか
2. consumer/provider 側と kept-later adapter line を分けて持てるか
3. working label と OPEN affiliation を guard で維持できるか
4. next line を stable malformed missing-option first reopen actualization comparison に clean に handoff できるか

## 比較対象

### 案 1. `boundary_kind + entry_criteria_refs + current_boundary_refs + consumer_provider_refs + guard_refs + kept_later_refs` を持つ

#### 利点

- capability-scoped port / adapter boundary と consumer/provider 側の split を lossless に残せる。
- privileged `stdin/stdout` 回避、working label 維持、current host harness 非昇格を guard として残せる。
- FFI / engine adapter、actual host interface contract、final term selection を kept-later line に clean に押し出せる。

#### 欠点

- fields はやや多い。

### 案 2. capability-scoped port という prose summary だけを残す

#### 利点

- 軽い。

#### 欠点

- visualizer / host substrate / host runtime と later adapter line の分離が抜けやすい。
- guard が薄くなり、privileged primitive 読みが戻りやすい。

### 案 3. final terminology 候補まで threshold に含める

#### 利点

- naming は一見明快になる。

#### 欠点

- OPEN judgment を threshold で premature に固定する。

## current judgment

current L2 で最も自然なのは、
**案 1. `boundary_kind + entry_criteria_refs + current_boundary_refs + consumer_provider_refs + guard_refs + kept_later_refs` を持つ**
である。

理由は次の通り。

1. current package の本体は docs-first boundary close であり、consumer/provider 側、guard、later adapter line を minimum に残す必要がある。
2. actual host interface、FFI / engine adapter、final naming は still later に押し出す方が current docs reading と整合する。
3. next line を missing-option actualization comparison に handoff しつつ、Macro 7 側の later integration pressure を kept-later refs に残せる。

## current first choice shape

```text
docs_first_io_host_facing_boundary = {
  boundary_kind = current_l2_docs_first_host_facing_io_adapter_boundary,
  entry_criteria_refs = [
    sample_facing_theorem_model_check_summary,
    public_operational_later_gate
  ],
  current_boundary_refs = [
    capability_scoped_input_output_port_boundary,
    docs_only_host_adapter_boundary
  ],
  consumer_provider_refs = [
    visualizer_consumer_side,
    host_substrate_provider_side,
    host_runtime_provider_side
  ],
  guard_refs = [
    avoid_privileged_stdin_stdout_primitive,
    keep_current_host_harness_non_production,
    keep_working_label_non_final,
    keep_typed_effect_and_mirrorea_affiliation_open
  ],
  kept_later_refs = [
    stable_malformed_missing_option_first_reopen_actualization,
    actual_host_interface_contract,
    ffi_adapter_line,
    game_engine_adapter_line,
    final_term_selection
  ]
}
```

## practical reading

current minimal docs-first I/O / host-facing port boundary が示すのは、

- language core に privileged `stdin/stdout` を入れない
- capability-scoped port / adapter boundary を first docs-only cut に置く
- visualizer / host substrate / host runtime を consumer/provider 側に置く
- current host harness は production host interface に昇格させない
- FFI / engine adapter と final naming は later gate に残す

という最小 cut である。

## next promoted line

next promoted line は、
**docs-first-io-host-facing-port-boundary-ready stable-malformed-missing-option-first-reopen-actualization comparison**
に置く。

## open questions

- actual host interface contract をどの adapter boundary から narrow に reopen するか
- FFI / game engine adapter を visualizer / host substrate と別 gate のまま保つか
- working label を final term へ寄せる条件をどこで満たしたとみなすか
