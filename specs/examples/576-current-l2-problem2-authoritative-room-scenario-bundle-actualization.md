# 576 — current L2 Problem 2 authoritative-room scenario bundle actualization

## 目的

`specs/examples/467-current-l2-problem2-actual-adoption-package-and-authoritative-room-default-profile.md`、
`569-current-l2-order-handoff-source-surface-artifact-route-tightening.md`、
`570-current-l2-authoritative-room-first-scenario-helper-summary-tightening.md`、
`571-current-l2-authoritative-room-reserve-strengthening-lane-tightening.md`、
`572-current-l2-guided-problem-sample-entrypoints-and-runner.md`、
`574-current-l2-problem2-public-shape-residual-bundle-matrix.md`
を前提に、

- guided sample `problem2`
- residual matrix `problem2`
- authoritative-room first scenario helper summary
- reserve strengthening lane helper summary
- negative static-stop pair
- `samples/lean/current-l2/` representative artifact

を、`bundle problem2` helper と sample README によって
**一本道の repo-local authoritative-room scenario bundle**
として読む current cut を 1 本に束ねる。

ここで actualize するのは、
**Problem 2 の current first line / reserve lane / negative pair を docs / helper / Lean artifact path まで bundle 化する current cut**
であり、

- final public witness schema
- final public provider receipt schema
- final public witness/provider/artifact contract
- exhaustive shared-space catalog

は fixed しない。

## source-backed floor

current repo では、少なくとも次が source-backed にある。

1. Problem 2 actual adoption package
   - relation decomposition principal
   - authoritative-room first default profile
   - thread/node same causal language keep
   - repo-local emitted artifact refs first
2. authoritative-room first scenario helper summary
   - `p07 / p08` representative reached pair
   - `p09` delegated RNG reserve route
   - `p13 / p14` negative static-stop pair
3. reserve strengthening lane
   - `auditable_authority_witness`
   - `delegated_rng_service`
   - model-check second line
4. guided helper floor
   - `show problem2`
   - `run problem2`
   - `matrix problem2`

したがって current open problem は、
Problem 2 current first line そのものを追加比較することではなく、
**既に存在する representative pair / reserve route / negative pair / Lean artifact / spec-report anchor をどう一本道で辿れる形にするか**
である。

## current actualization cut

current package では、次を採る。

1. `scripts/current_l2_guided_samples.py`
   に `bundle problem2` を追加する
2. `bundle problem2` は
   - `show problem2`
   - representative sample 実行コマンド
   - `matrix problem2`
   - `run problem2 --all --format json`
   のおすすめ順を出す
3. representative pair として
   - `p07`
   - `p08`
   - 対応する `samples/lean/current-l2/`
   を bundle の中心に置く
4. reserve / negative として
   - `p09`
   - `p13 / p14`
   - 対応する `samples/lean/current-l2/`
   を同じ bundle 内で辿れるようにする
5. anchor docs / reports と stop line を bundle 本文に明示する

## actual runnable evidence

| evidence | current reading |
|---|---|
| `python3 scripts/current_l2_guided_samples.py bundle problem2` | Problem 2 representative pair、reserve route、negative pair、Lean artifact、anchor docs / reports、stop line を 1 画面で辿れる |
| `python3 scripts/current_l2_guided_samples.py bundle problem2 --format json` | scenario bundle manifest を repo-local machine-readable shape で inspect できる |
| `python3 -m unittest scripts.tests.test_current_l2_guided_samples` | bundle helper と residual matrix helper の current behavior を machine-check する |
| `samples/prototype/current-l2-order-handoff/README.md` | Problem 2 README から `bundle problem2` 導線を日本語で辿れる |

## current recommendation

1. Problem 2 authoritative-room first line は、final public witness/provider/artifact contract を採る前に
   guided helper / Lean artifact / spec-report anchor の scenario bundle 化まで actualize してよい。
2. bundle の中心は `p07 / p08` representative pair に置き、
   `p09` は reserve practical route、`p13 / p14` は negative static-stop pair として同じ bundle 内で読む。
3. bundle 化は first completion line と reserve line の境界を保ったまま adoption debt を減らすための repo-local helper actualization であり、
   final public schema や exhaustive shared-space catalog を意味しない。

## retained alternatives

- `show` / `matrix` を separate のままにして bundle を足さない
- `p09` を representative pair へ昇格させる
- final public witness/provider/artifact contract を先に詰める
- exhaustive shared-space catalog を先に作る

## stop line

current package は次で止める。

- final public witness schema
- final public provider receipt schema
- final public witness/provider/artifact contract
- exhaustive shared-space catalog

## next self-driven line

current package を close した後の next self-driven line は、

1. experimental parser-side companion surface bundle
2. parser-side carrier から current helper summary への thin bridge

に置くのが自然である。
