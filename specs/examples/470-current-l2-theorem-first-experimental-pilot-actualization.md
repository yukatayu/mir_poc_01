# 470 — current L2 theorem-first experimental pilot actualization

## 目的

`specs/examples/466-current-l2-problem1-actual-adoption-package-and-theorem-first-pilot.md`
で fixed した Problem 1 actual adoption package を前提に、

- notebook-first theorem line
- repo-local emitted artifact
- reproducible compare floor
- no public verifier contract promotion

を 1 package 分 actualize する。

ここで actualize するのは、
**theorem-first pilot を helper-local / repo-local compare floor まで進める current cut**
であり、

- final public verifier contract
- actual discharge transport format
- proof object public schema
- concrete theorem prover production binding

は fixed しない。

## current actualization cut

current repo では、theorem-first pilot の helper-local actualization route を
次に置く。

1. principal row = `proof_notebook_review_unit`
2. evidence floor = symbolic `evidence_refs` only
3. repo-local emitted artifact refs を preview-aligned route から作る
4. compare floor = preview-aligned typed artifact route + theorem discharge pre-floor
5. public theorem contract / public verifier contract へは上げない

この route は test/support helper に留め、
mainline public surface は widening しない。

## actual runnable evidence

| evidence | current reading |
|---|---|
| `current_l2_theorem_first_pilot_actualization` | theorem-first pilot actualization route が representative corpus で green |
| `e5-underdeclared-lineage` | fixture-static cluster でも notebook-first pilot route を repo-local artifact refs まで actualize できる |
| `p06-typed-proof-owner-handoff` | corrected typed prototype を theorem-first pilot の bridge-floor evidence に保てる |
| `p07-dice-late-join-visible-history` / `p08-dice-stale-reconnect-refresh` | order/handoff prototype も theorem-first pilot compare floor に乗せられる |
| `p05-dice-owner-guarded-chain` guarded result | reached しない case を guard-only compare floor に分けられる |

## actualization shape

current helper-local pilot manifest は少なくとも次を持つ。

- `pilot_status`
- `pilot_subject_kind` / `pilot_subject_ref`
- `principal_review_unit_refs`
- symbolic `evidence_refs`
- `repo_local_emitted_artifact_refs`
- `compare_floor_refs`
- `guard_refs`
- `kept_later_refs`

`kept_later_refs` は current cut で少なくとも次を保持する。

- `theorem_discharge_transport`
- `public_theorem_contract`
- `concrete_theorem_prover_binding`
- `proof_object_public_schema`

## current recommendation

1. theorem-first pilot は helper-local / repo-local emitted-artifact route まで actualize してよい。
2. pilot manifest は compare floor の evidence に留め、final public verifier contract に昇格させない。
3. `p06` は typed calculus adoption の evidence ではなく、pilot bridge-floor evidence に留める。
4. order/handoff prototype を theorem-first compare floor に再利用してよいが、Problem 2 actual adoption package と collapse しない。

## retained alternatives

- stronger typed surface source-principal promotion
- public theorem contract early adoption
- discharge transport format early fixation
- theorem prover concrete brand early binding
- model-check-first external integration

## stop line

current package は次で止める。

- final public verifier contract
- actual discharge transport format
- proof object public schema
- concrete theorem prover production binding
- stronger typed-surface source-principal adoption

## historical package-local next self-driven line

theorem-prover experimental binding preflight は `specs/examples/474` で close 済みである。
historical package-local next line としては、

1. `auditable_authority_witness`
2. `delegated_rng_service`
3. model-check second-line concretization

を compare-anchor memory として読むのが自然である。

ただし current repo-level queue authority は `progress.md` / `tasks.md` にあり、
2026-04-30 current-line maintenance closeout 後に
この package から追加の self-driven implementation line を promote しない。
