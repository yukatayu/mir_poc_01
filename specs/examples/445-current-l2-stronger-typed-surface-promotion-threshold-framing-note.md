# 445 — current L2 stronger typed-surface promotion threshold framing note

## 目的

`specs/examples/418-current-l2-first-source-visible-typed-surface-comparison.md`、
`433-current-l2-request-predicate-try-cluster-typed-surface-reserve-note.md`、
`439-current-l2-typed-surface-family-unification-keep-drop-note.md`
で fixed した

- checker-adjacent semantic carrier principal
- source-visible structural marker family first
- request / predicate / `try` reserve cluster
- typed family two-tier split

を前提に、
**stronger typed surface を reopen してよい threshold と non-goal**
を docs-first に整理する。

ここで fixed するのは threshold framing だけであり、

- stronger typed surface の actual adoption
- full type calculus
- final typed syntax / public typed API

は still later に残す。

## source-backed floor

- principal typed source は checker-adjacent semantic carrier にある。
- source-visible first candidate は structural marker family である。
- request / predicate / `try` cluster は reserve cue に留まり、principal source ではない。
- theorem/model-check artifact や handoff row は typed source of truth ではなく derived reserve である。

## threshold comparison

| candidate | reading | strengths | current risk |
|---|---|---|---|
| immediate promotion | structural marker family から stronger typed surface へすぐ昇格する | long-run typed story は見やすい | final syntax / attachment shape を premature に拘束する |
| threshold framing only | reopen 条件と non-goal だけを先に fixed する | current repo memory と最も整合する | “いつか昇格” とだけ見えて dry に見える |
| indefinite reserve | stronger typed surface を常に later に送る | safest | current stop line が曖昧に残る |

## current judgment

current L2 で最も自然なのは、
**threshold framing only**
である。

current reopen threshold は、少なくとも次が揃ったときに初めて pressure があると読む。

1. checker-adjacent principal attachment と source-visible structural marker familyだけでは、同じ obligation family を明示的に切り分けられない。
2. request / predicate / `try` reserve cluster を reserve のままでは、source-visible explanation と checker legibility が同時に保てない。
3. theorem/model-check boundary を principal typed source に繰り上げずに、なお stronger typed surface が必要だと示せる。

## keep

- principal typed source は checker attachment に残す
- source-visible structural marker family と reserve cluster family の two-tier split を維持する
- stronger typed surface は threshold framing に留める
- theorem/model-check boundary を typed source of truth に混ぜない

## drop from current package

- stronger typed surface の actual adoption
- request / predicate / `try` cluster を source-level typed block に昇格させること
- unified obligation block を current source surface にすること
- final typed syntax / inference / annotation design を fixed すること

## next promoted line

next promoted line は、
**theorem discharge transport / public-contract later-gate framing note**
に置く。

## what is not decided here

- stronger typed surface の exact shape
- shared attachment shape
- full type calculus
- inference / annotation design
- final typed syntax
- public typed API
