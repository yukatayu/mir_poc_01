# avatar-head-transform

Planned-only representative root for session-bound avatar head transform publication.

The target is:

```text
avatar head pose -> PoseSnapshot -> observer-safe transform summary
```

This root stays non-executable until `P-POSE-02`.
