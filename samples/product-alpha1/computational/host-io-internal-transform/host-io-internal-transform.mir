module Computational.HostBoundary

fn add_one(x: Int64) -> Int64 {
  return x + 1
}

transition main at Place[HostPlace] {
  input <- perform read_int via host_input
  output <- add_one(input)
  perform write_int(output) via host_output
}
