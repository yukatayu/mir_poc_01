# comp-02-pure-add-one

Planned-only representative root for the first Mir-owned computation proof point.

The target is:

```text
host input -> Mir add_one -> host output
```

This root stays non-executable until `P-COMP-02`.
