# practical-alpha1 preview manifests

This root contains thin practical alpha-1 product-preview manifests.

- Each subdirectory exposes one `preview.json`.
- These manifests are not final public schemas.
- They bundle exact practical package/report evidence for the current non-final product-preview floor.
- They do not replace the underlying `packages/` front-door fixtures.
- Current actualized preview rows are `PE2E-01..07`.
- `PE2E-04` is narrowed to placeholder object companion preview evidence only.
- `AV-A1-02` custom Mir avatar runtime and `AV-A1-03` unsupported runtime fallback remain later.

