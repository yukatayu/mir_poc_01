# practical-alpha1 source

This directory is reserved for later textual alpha source fixtures.

`P-A1-01` does not freeze the final public grammar and therefore keeps the
initial practical front-door on `package.mir.json` package inputs instead.
