# SURF-09 Role Named S Positive

`S[self] { ... }` is rejected as place-scope syntax only when `S` is a place
scope head. If `S` is a declared role, namespace resolution keeps it a
role-instance block.
