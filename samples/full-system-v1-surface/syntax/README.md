# Surface Mir Syntax Samples

This directory is the P-SURF-01 parser evidence lane.

The `.mir` files are semantic source samples for the alpha parser. Expected
JSON files are parser projections used by `scripts/surface_mir_samples.py`;
they are evidence artifacts, not source authority.
