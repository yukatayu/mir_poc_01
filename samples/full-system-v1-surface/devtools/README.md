# Surface Mir devtools diagnostics samples

P-SURF-08 alpha evidence root. These rows build static observer-safe source/Core diagnostic bundles from `.mir` source authority. The indexed-state panel is backed by the P-SURF-02 semantic checker, and helper/release outputs keep raw patch/auth/witness payloads out of the devtools projection. They are not final viewer, telemetry ABI, runtime transport, or production devtools completion.
