# `imported-semantic-negative`

Imported-module static rejection row reused across:

- `P-MIR-02` typed-check imported semantic closure failure
- `P-MIR-03` source-derived runtime static rejection classification
