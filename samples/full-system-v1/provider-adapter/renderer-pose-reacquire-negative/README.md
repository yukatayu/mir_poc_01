# renderer-pose-reacquire-negative

Negative `P-ENG-03` row. The renderer packet is structurally admitted, but PoseGraph
runtime rejects stale fallback-only anchor state with `reacquire_required`, so
delivery is blocked.
