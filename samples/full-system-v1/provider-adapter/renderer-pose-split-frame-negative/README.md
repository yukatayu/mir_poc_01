# renderer-pose-split-frame-negative

Negative `P-ENG-03` row. Provider admission remains bounded and accepted, but the
PoseGraph package violates `no_split_frame`, so renderer delivery is blocked before
any ownership claim.
