# Portal Admission Denied Negative

Negative `P-FSV1-02` row for PortalWorldLink.

- resolves one unavailable portal destination
- exports one fallback summary through the bounded diagnostic boundary
- must fail as `contract_require_failed`
