# Full System V1 WorldCore Samples

This root carries the `P-FSV1-01` source-first WorldCore lane.

- `shared/src/world-core-base.mir` is the semantic support module for bounded world bootstrap rows.
- `expected/manifest.json` stores the generated package-manifest subset derived from source.
- `expected/run.json` stores the bounded runtime report subset derived from source execution.
- These rows prove bounded local world bootstrap / observer-safe summary evidence only.

Non-claims:

- no final world declaration grammar
- no final membership registry runtime
- no final devtools/export ABI
