# Chat Room Message Positive

Positive `P-FSV1-01` row for MembershipChat.

- imports the WorldCore and MembershipChat support modules
- keeps chat text as a Mir-owned transform before boundary export
- carries `RateLimited` only as declared failure-row evidence
