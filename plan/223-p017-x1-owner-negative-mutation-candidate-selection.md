# Plan 223: P017 X1 Owner-Negative/Mutation Candidate Selection

## Role and authority

This is LAB candidate-selection memory for the post-WRK-0041 P017 X1 screen.
`mirrorea_canon/` remains normative. The selected record is a reversible L3
pre-registration only; it neither supplies owner-failure semantics nor selects
a mutation rule, attribution criterion, Core, Config, SaveObject, terminal
branch algebra, transition, theory/11 entry, scenario, Gate, Phase, runtime,
contract, or public behavior.

## Current cut and question

WRK-0040 retained five supplied-fixture collapse detectors, including the
owner-service/requester-receipt distinction. WRK-0041 retained an independent
four-fixture detector for the simultaneous owner-terminal-positive and
owner-terminal-negative pair. Neither detector includes the separate P017 and
Plan 220 `X-BRANCH` clause that an owner failure has no owner mutation.

The question is not whether an owner failure or owner mutation is represented
correctly. It is whether one further independently falsifiable supplied-fixture
negative oracle can expose their simultaneous label without constructing the
missing failure, mutation, identity, attribution, or transition semantics.

## Candidate screen

| Candidate | Independent consumer and falsifier | Disposition |
| --- | --- | --- |
| finite owner-terminal-negative / owner-mutation overlap | P017 item 2 and Plan 220 `X-BRANCH` say that owner failure has no owner mutation; supplied neutral, negative-only, mutation-only, and simultaneous fixtures distinguish the forbidden pair | **selected**: one fixture-only countermodel remains in the existing `plan/` Lean lane without a selected failure or mutation representation |
| immediate scoped `no-candidate` | avoids another table but leaves P017's explicit no-mutation condition without a directly attributable negative oracle | not selected: the direct condition has an independent falsifier and can remain fixture-only |
| positive owner failure, mutation, or attribution model | requires a failure member/row, mutation identity, ownership/attribution rule, branch algebra, transition, or storage | reserved: any meaningful test would choose a missing Canon surface |
| pending binding, receipt/rejection, typing, causality, linearity, load, authority, or observation | needs a relation carrier, source of facts, transitions, endpoints, effect/failure row, save placement, or projection | reserved or duplicate: these rows remain ordinary Canon design; WRK-0040/0041 already retain their stated finite distinctions only |
| permutations, minimality, or more Boolean conjunctions over retained marks | unchanged cut and no new independently stated P017/Plan 220 falsifier | duplicate: do not extend the table merely because further Boolean combinations exist |

The local history scan found the no-mutation condition in P017 and Plan 220 but
no earlier owner-negative/mutation countermodel. A temporary Oracle review
agreed that this is the sole remaining fixture-only candidate at the current
cut. Its advice is advisory; selection follows the cited source conditions and
the local duplication/read-only-boundary check.

## Selected package

`working/WRK-0042-p017-x1-owner-negative-mutation-countermodel.md` was
committed and pushed before any outcome source existed. Its sole retained source
uses one supplied anchor and four supplied fixture labels: neutral,
owner-terminal-negative only, owner-mutation only, and their seeded simultaneous
overlap. The source passed Lean `--trust=0`; all four retained theorem reports
have no axioms. The labels are not a Mir failure, mutation, owner, identity,
value, occurrence, storage field, transport key, source form, or runtime event.

The expected result is deliberately narrow: a passing table would show only
that the supplied labels distinguish the three controls from the seeded pair.
It does not show that a Mir execution produces either label, that a negative
terminal branch has a type, that a mutation is attributable to that branch, or
that P017's no-mutation condition is satisfied by an implementation.

## Execution order and termination rule

1. WRK-0042 was registered and pushed before outcome evidence.
2. Its one Markdown-held Lean block was materialized in the declared `plan/`
   lane only.
3. The registered digest, extraction, `lean --trust=0`, axiom, matrix,
   reserved-vocabulary, allowlist, and diff checks passed. The RED test first
   rejected an intentionally all-false `both` detector.
4. Its evidence is linked. Run a fresh screen; do not automatically add
   another Boolean detector: a successor must identify a new explicit source
   condition, independent consumer, and typed falsifier that survive the
   reserved-boundary review. Otherwise retain scoped `no-candidate`.

Stop and escalate instead of repairing the artifact if it needs a failure row,
mutation rule, attribution or identity criterion, branch carrier, semantic
transition, storage representation, receipt, source syntax, runtime, transport,
contract, OBL/THM, SCN, Gate, Phase, helper, schema, CI/Make surface, or public
interface.

## Non-effects

This plan does not turn P017's no-owner-mutation clause into a theorem, OBL,
requirement-satisfaction claim, implementation criterion, or selected design.
It does not endorse an actual mutation model or a failure representation. It
selects one bounded LAB experiment only.
