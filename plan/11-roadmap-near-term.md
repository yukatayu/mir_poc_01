# plan/11 — 近接ロードマップ

## 目的

この文書は、今から数 task 先までの near-term roadmap を示す。
step 数や task 数は厳密な約束ではなく、**rough estimate** である。

## current reading

- legacy checkpoint としては、Phase 1〜5 closeout / freeze は fixed 済みである。
- legacy Phase 6 front-half compile-ready checkpoint も fixed 済みである。
- current macro phase mainline は `Macro 5` theorem / verifier bridge と `Macro 4` executable fixed-subset sample expansion であり、`Macro 6` shared-space follow-up は docs-first reserve line として並走する。

## immediate execution order

### 1. model-check concrete carrier first actualization gate

- macro phase
  - `Macro 5`
- rough weight
  - 中
- rough 所要
  - 1〜3 task / 1〜2週

## next reserve line

### stable malformed broader follow-up

- `Macro 4`
- edge-pair fixed 後の broader malformed follow-up を missing-option / capability family、duplicate cluster、try/rollback malformed-static familyの順でどう reopen するかは later inventory に残す。

### public operational CLI / final public contract later gate

- `Macro 7`
- `run_current_l2_source_sample` を first docs-only candidate に固定したうえで、final public parser / checker / runtime API と public CLI は later gate に残す。

### shared-space admission / compile-time visibility reopen

- `Macro 6`
- role / capability / visibility over-approximation と runtime admission / activation / reconciliation の split を shared-space next reopen として整理する。

## later gates

- shared-space final catalog
- upper-layer application target
- backend / public operational surface success criteria

## current recommendation

- current near-term では、proof/model-check first concrete pilot、second source-sample cluster sequencing、actual `e22` contrast-row source actualization、stable static malformed post-contrast sequencing、public surface inventory、Mirrorea/shared-space docs-first re-entry、model-check/public-checker second reserve inventory、stable-static edge-pair first reopen、public operational surface actualization gate、shared-space identity/auth layering reopenを fixed entry criteria に閉じたうえで、model-check concrete carrier / broader malformed follow-up / public operational later gate / shared-space admission-visibility lineを staged に進める。
- backend や higher-level async-control family を current executable core に早く混ぜない。
