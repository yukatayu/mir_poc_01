# plan/168 - WRK-0009 e5 proof-skeleton identity selection

## Role and authority

This is LAB repository memory. `mirrorea_canon/` remains authoritative for
theory, OBL status, Gates, Phases, contracts, and semantic selection.
WRK-0009 is `L3-open`; this plan adds no theorem, carrier, or lifecycle claim.

## Current selection

At `561c56266419646ed1b14431f47fccb852c391bb`, WRK-0009 is committed and
pushed with `Reliance status: not-promoted`. No evidence command has run.
It asks whether the Lean foundation's concrete e5 review-unit/stub tuple
literally matches the existing static current-L2 e5 route, or is explicitly
documented as an unrelated synthetic tuple.

This is literal-transcription evidence only. It does not interpret fallback,
rollback, cut, canonical normalization, or theorem semantics.

## Candidate disposition

| Candidate | Disposition | Reason |
| --- | --- | --- |
| e5 proof-skeleton identity fidelity | selected as WRK-0009 | A fixed foundation tuple and a fixed existing pipeline tuple give a direct adverse case without a new surface. |
| upstream bundle versus formal-hook projection loss | rejected now | The existing detached bundle lacks occurrence, location, order/frontier, and rollback-effect fields. Exposing them requires forbidden helper/schema/runner work. |
| static formal-hook verdict/reason attribution | reserve | Distinct from WRK-0008 but methodologically similar; retain as information-loss audit only. |
| e21/e22 source-route final-state assertion coverage | reserve | May audit LAB test coverage only; must not infer Canon Place/history/OBL-027 semantics. |
| OBL-001/024/025 variants | rejected | plan/165's duplicate, explicit-scope, and unselected-carrier reasons remain unchanged. |

WRK-0009 is independent of WRK-0008: it does not repeat runtime event
partitioning or ask where same-Place frontier information is lost.

## Registered execution boundary

The committed record's exact command compiles the existing foundation; prints
`obligationName`, `mkLeanStub`, and `e5ReviewUnits`; runs the existing
theorem-stub support test; emits the existing e5 proof-notebook/Lean-stub
pipeline into a unique `/tmp` directory; prints normalized tuples; and runs the
existing 23-command current-L2 regression.

Only the declared LAB roots are retained as inputs/artifacts. Existing scripts,
Cargo tests, and support code run at the pinned Git base; they are not changed
or retained as WRK evidence. Any retained result is a `plan/` artifact with an
append-only evidence commit.

## Outcome handling

- Literal route match, explicit lossless mapping, or explicit synthetic role:
  freeze the record and retain no divergence claim.
- Displayed mismatch with no such statement: retain only LAB identity-fidelity
  evidence; do not repair the foundation or emitter in this record.
- Command failure, required new surface, or semantic interpretation: freeze and
  escalate rather than widen the record.

No branch selects a mapping, equality, carrier, theory/06 meaning, theory/11
status, OBL, Gate, Phase, conformance class, or public behavior.

## Next reopen order

1. Execute and manifest WRK-0009 exactly as registered.
2. Re-run standing triage after that scoped evidence closes.
3. Consider static verdict/reason attribution before source-route state coverage
   unless new evidence changes their eligibility.
4. Escalate any repair or carrier decision.
