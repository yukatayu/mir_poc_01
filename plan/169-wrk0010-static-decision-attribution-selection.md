# WRK-0010 static decision attribution selection

**LAB repository memory.** Canon remains authoritative. WRK-0010 is `L3-open`,
`not-promoted`, and has no evidence result yet.

## Selected question

At `7f42feed38e4c38cc6a3491200f8b0917940bd95`, WRK-0010 was registered to
compare the existing e4/e5/e12/e14 static-gate decision payload with their
existing `fixture_static_cluster` formal hooks. It records literal verdict,
reasons, and `detached_noncore` presence/reason-code information only. No
diagnostic meaning, defect, mapping, carrier, theorem/OBL, or repair follows.

## Candidate disposition

| Candidate | Disposition | Reason |
| --- | --- | --- |
| static gate to formal-hook decision attribution | selected | Four documented static samples have distinct existing decision payloads and fixed artifact paths. |
| e21/e22 final-store assertion directness | reserve | Distinct coverage audit, but weaker source-root proof and no need to combine it. |
| WRK-0009 mapping/synthetic role/repair | rejected | Would decide intent, mapping, defect, or implementation beyond the literal mismatch. |
| upstream projection/frontier repair | rejected | Requires new helper/schema/runner information. |

## Execution and stop line

The registered command alone may run. Retain only a `plan/` matrix after the
four static smokes and the unchanged 23-command regression pass. Literal full,
partial, or absent attribution are classification outcomes only. Any missing
artifact, command failure, or need for a new surface freezes the record.
