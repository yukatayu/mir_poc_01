# Plan 222: P017 X1 Owner-Terminal Exclusivity Candidate Selection

## Role and authority

This is LAB candidate-selection memory for the first post-WRK-0040 P017 X1
screen. `mirrorea_canon/` remains normative. The selected record is a
reversible L3 pre-registration only; it neither selects terminal-branch
semantics nor changes Core, Config, SaveObject, a failure row, a transition,
theory/11, a scenario, Gate, Phase, runtime, contract, or public behavior.

## Current cut and question

WRK-0040 retained a six-fixture predicate-only detector for five coupled
cross-locus collapses. Its `PHASE` column distinguishes owner service from
requester receipt/use. It deliberately does **not** distinguish the permitted
outstanding owner disposition or the mutually exclusive owner-terminal success
and owner-service-failure alternatives required by P017 and Plan 220
`X-BRANCH`.

The question for this screen is not whether a positive exchange model is true.
It is whether one additional, independently falsifiable finite negative oracle
can expose that specific overlap without choosing any missing representation.

## Candidate screen

| Candidate | Independent consumer and falsifier | Disposition |
| --- | --- | --- |
| finite owner-terminal exclusivity | P017 item 2 and Plan 220 `X-BRANCH`; supplied no-terminal, positive-only, negative-only, and simultaneous fixtures distinguish an overlap detector | **selected**: one fixture-only countermodel can remain in the existing `plan/` Lean lane without a selected carrier or dynamic representation |
| pending binding or total association | P017 requires a relation domain and exact association | reserved: any meaningful test selects a pending carrier, relation schema, or identity criterion |
| receipt absence/rejection/failure receipt | P017 leaves rejection treatment and dynamic failure-row effects open | reserved: needs a receipt transition and/or failure-row choice |
| result/failure typing | Plan 220 `X-TYPE` depends on an operation effect/failure representation | reserved: contract and failure semantics are not selected |
| causal order, one-shot strengthening, or restore matrix | Plan 220 `X-CAUSAL`, `X-LINEAR`, and `X-LOAD` need transitions, use representation, endpoints, or save placement | reserved or duplicate: WRK-0040 already retains the bounded `ONE` distinction |
| authority or observation detail | `AUTH` / `OBS` have only opaque fixture distinctions in WRK-0040 | reserved: needs an authority algorithm or typed projection/export semantics |
| permutation, minimality, or independence over WRK-0040 | no changed cut, downstream overclaim, or reproducible defect | duplicate: it would only restate the retained finite table |

An advisory temporary Oracle review independently reached the same result. Its
advice is not authoritative; the selection follows the cited P017 and Plan 220
conditions and the local duplication search.

## Selected package

`working/WRK-0041-p017-x1-owner-terminal-exclusivity-countermodel.md` is the
only selected candidate. Its source, when materialized after the registration
push, may contain only a supplied anchor and four finite fixture labels:
no-terminal, positive-only, negative-only, and simultaneous. It may test a
predicate detecting the simultaneous pair, but may not define an outcome enum,
failure type/row, branch state, transition, owner mutation, receipt,
persistence field, relation, or runtime event.

The expected value is narrow: the test can show that a detector table can keep
the three allowed fixture classes separate from the seeded overlap. A passing
table does not show that Mir supplies either fact, reaches a terminal branch,
has a typed failure, or satisfies `X-BRANCH`.

## Execution order and stop line

1. Commit and push the pre-registration before outcome evidence.
2. Materialize one Markdown-held Lean block in the declared `plan/` lane.
3. Run the registered digest, extraction, `lean --trust=0`, axiom, matrix,
   reserved-vocabulary, allowlist, and diff checks.
4. Freeze immediately if any expected falsifier occurs; otherwise retain only
   the exact finite evidence and link its digest forward.

Stop and escalate instead of repairing the artifact if it needs a terminal
branch carrier, dynamic failure row, semantic transition, outcome generation,
owner mutation rule, source syntax, runtime, transport, contract, OBL/THM,
SCN, Gate, Phase, helper, schema, CI/Make surface, or public interface.

## Non-effects

This plan does not turn `X-BRANCH` into a theorem, OBL, requirement-satisfaction
claim, or implementation criterion. It does not prefer a positive terminal
model, state machine, field layout, storage encoding, failure family, or
receipt policy. It is a selection record for one bounded LAB experiment only.
