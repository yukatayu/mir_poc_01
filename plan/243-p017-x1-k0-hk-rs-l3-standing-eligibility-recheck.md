# Plan 243: P017 X1 K0 H_K-rs L3 Standing-Eligibility Recheck

## Role and authority

This is a LAB pre-registration decision for the candidate selected by Plan 242.
`mirrorea_canon/` remains normative. The conclusion
**`ELIGIBILITY-PASS-FOR-REGISTRATION`** means only that a separate,
source-free L3 registration may be created after this plan is committed and
pushed and after it pins its own fresh exact cut. It does not create
`WRK-0045`, supply a Lean source, run an experiment, or establish any result.

No Canon proposition, relation schema, identifier/equality, occurrence kind,
Core form, rule, causal generator, failure row, Config/SaveObject placement,
source form, runtime behavior, theorem/OBL status, Gate/Phase state,
implementation, or public interface is selected here.

## Candidate and result ceiling

The only candidate is `A-Sigma H_K-rs`: an extensional candidate-local history
containing distinct `q`, `s`, and `r`, a single q-scoped A-Sigma relation
restriction, and the explicitly hypothesized role, result, matching,
acceptance/use, causality, and restore facts from Plan 242. `r` is not an
existing occurrence kind or operational output. A source may ask only for a
conditional lemma, countermodel, or existing-lane experiment over those
premises.

The alternatives are fixed:

| Alternative | Status |
| --- | --- |
| literal C-level requester receipt endpoint at the pinned cut | unavailable unless a later source cut actually supplies it |
| A-Sigma H_K-rs | sole possible L3 candidate |
| DEFER | mandatory result for any ambiguity, vacuity, duplication, or reserved dependency |
| B-Pi | not selected and not an in-record fallback |

The possible result is not P017 satisfaction, a positive MirCore model,
operational reachability of `r`, a semantic receipt transition, or
implementation readiness.

## ADR-0014 standing-predicate recheck

| ADR-0014 condition | Current evidence | Result |
| --- | --- | --- |
| 1. Read-only anchors and pinned authority/evidence base | The future record can pin ADR-0014, working process, P012/P013/P017, theory/01/02/04/05/07, and the cited Plan 229/231/233/239--242 snapshots before reliance. | pass, provided the record recomputes every digest at its actual parent |
| 2. Narrow question, status quo, alternative/falsifier, non-effects, rollback | Plan 242 supplies the narrow A-Sigma question, DEFER alternative, H_K ledger, falsifier table, and non-effects. The future record can reproduce them without adding a semantic surface. | pass |
| 3. Allowed result class | The only planned outcome is an `existing-lane-experiment`, conditional lemma, or countermodel. | pass |
| 4. Existing LAB lane and no shared tooling | `plan/` and `docs/reports/` existed before registration. The proposed source path is one Markdown-held Lean block, extracted to a disposable file; no helper, module, schema, CI/Make target, or evidence lane is needed. | pass |
| 5. Reserved boundary / settled-invariant exclusion | The candidate makes all receipt, matching, type, authority, failure, order, and restore material explicit `H_K`; it stops instead of supplying any Core/history/schema/rule/persistence/runtime/API surface. | pass, only under the stated stop list |

The predicate is satisfied for a **registration only**, not for a result. Any
change of direct Canon or LAB inputs between this screen and registration
requires recomputing the actual record's source cut; no digest from this plan
may be copied as though it already pinned the later commit.

## Independent-consumer and duplicate recheck

Plan 229 bars a successor that repeats WRK-0044's static premise table. Plan
241 supplies the missing closed candidate occurrence/order inventory. Plan 242
then identifies the only independent consumer: two explicit direct
`send -> receive` role mappings, the co-located reply projection, the distinct
extensional endpoint `r`, exact typed result-to-receipt association,
functional matching, and r-sensitive cut/channel/restore closure.

The registration must state that all six are load-bearing. It must freeze
reliance and report `DEFER` if the later source can delete any of
`H_r`, `H_sr2`, `H_result-send`, `H_receipt`, `H_match`, or r-sensitive closure
without changing its conclusion. A bare path, transitive `q prec r`, or
acyclicity result is duplicate and is not an eligible outcome.

## Registration contract

If the immediately following recheck still passes, create exactly one new
`L3-open` record at:

```text
mirrorea_canon/working/WRK-0045-p017-x1-k0-hk-rs-asigma-conditional-trace.md
```

and register it in `mirrorea_canon/MAP.md`. The registration commit may contain
only the record, required working metadata, and one direct numbered report. It
must contain no source at:

```text
plan/wrk-0045-p017-x1-k0-hk-rs-asigma-conditional-trace.md
```

The record must declare `Permitted LAB locations: plan, docs/reports`,
`Reliance status: not-promoted`, `Evidence artifacts: none`, `Evidence commits:
none`, and `Independent review: not-required-for-L3`. Its source/execution
package, if ever opened, is a later commit and may add only that declared
`plan/` artifact plus direct report/control metadata under the standing
procedure.

The preregistration must restate the Plan 242 H_K ledger rather than silently
import OPEN rows from Plans 233--239: positive owner/provenance bases, exact
typed result, pending non-sharing, `H_sr1`, `H_sproj`, `H_r`, `H_sr2`,
`H_result-send`, `H_receipt`, `H_match`, acceptance/use, authority-order, K0,
restore, and non-vacuity all remain explicit candidate hypotheses. It may
derive only role aliases, transitive order, strict distinctness, and views
after positive premises. Matching is not derivable merely from q-scoping.

## Stop and rollback line

Do not register, or freeze the later record on a reproducible outcome, if any
of these is needed: a generic receiver kind/constructor/action, `[E-RECEIVE]`
or other operational rule, a result/reply/receipt type or carrier, identifier
or saved key, fixed tuple/record/family index/common witness, failure member or
row, causal generator, Config/SaveObject placement, serialization/restore
function, observation projection, source/elaboration/G_e form, runtime/wire
behavior, theorem/OBL or lifecycle claim, or an assertion that Theory 04
already assigns the candidate roles.

Likewise stop for raw rejection becoming semantic state/occurrence/failure,
matching from incidental facts, type/authority collapse, receipt/use collapse,
failed-owner success chain, causal edge generated by relation/acceptance/load,
restore merge/duplication/reset/revalidation, missing channel closure, or a
vacuous/contradictory/uninhabited result. `DEFER` is the only in-scope repair;
B-Pi is not substituted after a failure.

## Non-effects

This recheck does not adopt A-Sigma, close OPEN-010/OPEN-011, amend Canon,
change the standing predicate, open a working record, create Lean evidence,
select any R/B/T/U/C/L fact as semantics, or alter samples, implementation, or
public behavior.
