# WRK-0013 retained-reproduction selection

## Role and authority

This is LAB repository memory. `mirrorea_canon/` remains normative for theory,
OBL status, Gates, Phases, contracts, conformance, and semantic selection. This
selection does not create WRK-0013, authorize an outcome command, unfreeze
WRK-0012, change a validator, or make a runtime/public claim.

## Start point

WRK-0012 is frozen. Its two fixed direct-world sidecars produced the registered
observations, but its preregistered numbered `plan/171` result artifact needs
out-of-scope validator/source-hierarchy registration changes. R-2347 is
historical metadata only; it cannot be reused as evidence or repair the frozen
record.

The current task map therefore requires a retention-boundary source screen.
The relevant existing lane is not a new plan convention: `plan/00-index.md`
already indexes unnumbered `plan/wrk-0008` through `plan/wrk-0011` evidence
files. `scripts/validate_docs.py` applies static registration only to filenames
matching the numbered form, while `plan` is already a documented WRK LAB root.
A disposable unnumbered-file probe passed the unchanged documentation and
source-hierarchy validators. The probe is not project evidence or an artifact.

## Candidate comparison

| Candidate | Disposition | Reason |
| --- | --- | --- |
| Re-open WRK-0012 or admit numbered `plan/171` | rejected | It would repair an observed retention falsifier in place and require prohibited validator/source-hierarchy changes. |
| New retained reproduction through unnumbered `plan/wrk-0013-pcomp03-retained-reproduction.md` | selected for registration | It asks a distinct provenance/retention question: can immutable existing sidecars be freshly reproduced after a new registration and retained through an already documented unnumbered plan artifact path? |
| Numbered-plan policy escalation | reserve | Needed only if the selected unnumbered path cannot be admitted without policy or validator change. |
| New carrier/semantic study | reserve | It would either duplicate WRK-0012's carrier question or select a broader interpretation before the retention question is settled. |

## Selected record shape

The next package may create
`mirrorea_canon/working/WRK-0013-pcomp03-retention-reproduction.md` with
`Result class: reproduction`.

- Inputs: the two `direct-world/package.mir.json` leaves at
  `2242901a44d3feb7708f82ff535d91bff4fbe143`, pinned only as LAB inputs.
- Fresh question: after registration, can those unchanged inputs reproduce the
  fixed positive/negative classifications and retain the new output in exactly
  `plan/wrk-0013-pcomp03-retained-reproduction.md` plus its
  `plan/00-index.md` entry under unchanged validators?
- Registration: only WRK-0013 and allowed control metadata; it must not change
  either plan file or run any outcome command.
- Evidence commit: only the unnumbered result memo, its `plan/00-index.md`
  entry, and a direct numbered report. It is a new evidence commit; the
  sidecar-owning commit `2242901...` remains input provenance, not evidence.
- Attribution: a later manifest commits the evidence commit ID and result-memo
  digest into WRK-0013 without changing its first three preregistration
  sections.

## Registered falsifier shape

The future record must stop and freeze if any fresh post-registration result
differs from the fixed positive/negative classifications; either sidecar differs
from its pinned input blob; the exact unnumbered memo/index delta cannot pass
the unmodified validators; the old WRK-0012 output is used as evidence; or any
sidecar, validator, source-hierarchy list, helper, schema, script, CI/Make
surface, Rust crate, runtime, CLI, public interface, or unrelated plan file
needs modification. A runtime pass followed by retention failure is failure of
the new record, not partial success.

## Non-claims and reopen rule

This selection does not claim all P-COMP-03 rows, direct textual `.mir`,
helper/sidecar equivalence, rejection-phase equivalence, language completeness,
runtime correctness, a Canon carrier, semantics, proof/OBL status, conformance,
Gate/Phase movement, or product readiness. It does not unfreeze WRK-0012.

If the new record cannot retain its result through the selected path, freeze it
and escalate the policy question separately. Do not adapt either record in
place.
