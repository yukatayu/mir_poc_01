# Report 2549 — P017 X1 K0 R/L factorization preflight

- Date: 2026-07-30 03:00 JST
- Author / agent: codex
- Scope: Compare only the relation-residence and abstract restore-correspondence
  presentation of the open P017 X1 K0 candidate card. No Canon selection,
  dynamic model, implementation, or public behavior is created.
- Decision levels touched: LAB ordinary-design preparation only; no L0/L1/L2
  decision, `working/` record, theorem/OBL, Gate, Phase, SCN, implementation
  contract, or public claim.

## Objective

Make the first K0 R/L comparison falsifiable without importing a hidden
relation schema, exchange object, identity, field layout, transition, or
restore mechanism. The required result is either one surviving primitive
presentation delta or an explicit collapse to a neutral K0 R/L skeleton.

## Scope and assumptions

P017 X1 remains limited to one V1/R1 cross-locus read. Plan 230 leaves K0
external rejection open and stops K1 at the unselected requester failure row.
The user approved X1 but has not selected a relation presentation. `q` is an
in-history current/restored anchor, never a public, transport, session, or
global cross-load identity. WRK-0044 remains static explicit-premise evidence
only; it establishes no lifecycle, transition, reachability, causality,
one-shot, restore, or runtime result.

## Start state / dirty state

`HEAD` and fetched `origin/main` were equal at
`d036d8d041f5681094622bfe184759bae416f8f9`; the worktree was clean before the
new Plan 231 artifact. Plan 230 had made K0/K1 explicit but had not recorded a
K0 relation-presentation comparison or a definitional-collapse criterion.

## Documents consulted

Canon: `README.md`, `MAP.md`, ADR-0014, P017, theory/01--05, theory/07, and
WRK-0044. LAB: Plans 227--230, the WRK-0044 source and reports, the current
reader/status/task snapshots, `plan/00-index.md`, and the numbered-plan
validator list. The local Oracle operating manual and repository-local Oracle
operations guide were also read.

## Actions taken

1. Checked a temporary Oracle structural review against the P017/Plan 230
   boundary. Its advisory result was used only after comparison with the local
   source hierarchy.
2. Created Plan 231 with one common K0 constraint ledger, candidate-native
   fact/frontier questions, and two deliberately narrow deltas: A-Sigma whole
   q-scoped relation fiber and B-Pi coherent factorized relation families.
3. Defined the exact collapse check: all R/L claims must intertranslate using
   only erasable definitions, without an added existence, uniqueness, choice,
   equality, coherence, or restore premise.
4. Added adversarial review cases and downstream B/T/U/C questions while
   preserving the ordinary-design stop boundary.
5. Synchronized the detailed plan index, source-hierarchy registration, reader
   guide, status view, progress, and task map so they describe an open
   presentation comparison, not a model selection. The focused registration
   test exposed that the hierarchy list had already stopped at Plan 211; the
   missing Plans 212--230 and new Plan 231 are now aligned with its validator.

## Files changed

- `plan/231-k0-rl-factorization-preflight.md`
- `plan/00-index.md`
- `scripts/validate_docs.py`
- `scripts/check_source_hierarchy.py`
- `Documentation.md`
- `docs/project-status.md`
- `progress.md`
- `tasks.md`
- `docs/reports/2549-p017-x1-k0-rl-factorization-preflight.md`

## Commands run

- Canon, P017, Plan 230, current snapshot, and previous-report reads.
- One completed `ask-chatgpt-pro-temp` structural preflight review; its raw
  external transcript is not committed.
- `date` for synchronized status timestamps and branch/dirty-state checks.
- `make docs`: passed Canon index (132 files), source hierarchy (781 paths),
  and document validation after the hierarchy-registration fix.
- `python3 scripts/validate_docs.py --authoritative-working-annex`: passed.
- `python3 -m unittest -v scripts.tests.test_validate_docs`: passed after the
  complete registration fix; its focused numbered-plan test first exposed the
  stale hierarchy inventory.
- Pending at report write: final `git diff --check`, a tracked-plus-working-tree
  webhook-pattern scan, project-status line-cap check, commit, push, and
  remote-head equality verification.

## Evidence / outputs / test results

Plan 231 identifies only one possible substantive distinction. A-Sigma makes a
whole q-scoped relation slice primitive and uses non-exhaustive fact predicates;
B-Pi makes multiple q-anchored relation families primitive and requires joint
coherence-preserving restore correspondence to prohibit component splice. If
that difference can be erased in both directions by definitions alone, the
fork is duplicate packaging and must collapse.

The plan explicitly tests incidental equality, repeated dynamic emission,
multiple principals at one locus, success without receipt, accepted or consumed
restore, incomplete closure, independent loads, B-Pi component splice, and A-Sigma
fiber reuse/equality. These are review obligations, not execution evidence.

The focused registration test also found and repaired a repository-maintenance
defect: `scripts/check_source_hierarchy.py` listed Plans 1--211 while the
documentation validator already required Plans 1--230. No Plan content or
source hierarchy policy changed; the missing existing entries and Plan 231 now
make the two inventories agree.

## What changed in understanding

K0 can advance without selecting a record-shaped exchange state. The correct
next question is not which representation looks convenient, but whether either
primitive placement changes what must be assumed to state non-sharing and
restore closure. A relation presentation is meaningful only if it survives the
non-definitional collapse check without turning `q` or a relation slice into a
stable identity token.

## Open questions

The primitive/derived classification for pending, branch, result, receipt,
acceptance, use, provenance, and restore facts remains open. So do relation
arity and carriers, concrete coherence/correspondence formulae, failure row,
validation, receipt/acceptance transition, Gamma/Delta, occurrence inventory,
causal mapping, dynamic invariants, persistence representation, theorem, and
runtime. K1 remains blocked at its Canon failure-row boundary.

## Suggested next prompt

Run the Plan 231 collapse screen against the Canon/P017 constraints. Retain a
K0 R/L presentation only if its primitive-placement delta cannot be eliminated
without a forbidden hidden premise; otherwise record the neutral skeleton and
begin the B fact-classification review.

## Plan update status

`plan/` 更新済み: Plan 231 and the index distinguish the open A-Sigma/B-Pi
presentation comparison from every schema or lifecycle decision.

## Documentation.md update status

`Documentation.md` 更新済み: the reader guide links Plan 231 and explains its
collapse rule in one concise entry.

## docs/project-status.md update status

更新済み: the control view now names the open whole-fiber/factorized-family
fork and its neutral-skeleton collapse condition without a readiness claim.

## progress.md update status

`progress.md` 更新済み: the logical-specification, research row, and dated log
now state the next R/L collapse screen before B review.

## tasks.md update status

`tasks.md` 更新済み: Macro 1 work now orders the R/L collapse screen before
candidate-native B review and retains K1 as a separate Canon gap.

## samples_progress.md update status

`samples_progress.md` 更新不要: this package changes no runnable sample,
validation command, debug surface, active root, or sample-dashboard row.

## Reviewer findings and follow-up

The temporary Oracle review recommended a single common constraint ledger,
candidate-native A-Sigma/B-Pi deltas, an explicit definitional-collapse criterion, and
joint no-splice review for factorized restore. Its advice is advisory only;
the recorded boundary is grounded in P017, ADR-0014, Plan 227, Plan 230, and
the static limit of WRK-0044. No callable sub-agent execution interface is
available in this environment.

## Skipped validations and reasons

No Lean, parser, runtime, transport, or sample command applies because no
executable artifact is added and the retained Lean source is not modified.
The standard documentation and authoritative-annex validations passed. The
runtime and sample suites are intentionally not rerun because this task changes
only documentation registries and no executable source.

## Commit / push status

Pending at report write. The next operations validate this documentation-only
package, commit it with `--no-gpg-sign`, push it, and verify
`HEAD == origin/main` before continuing to the collapse screen.

## Sub-agent session close status

No callable sub-agent session was opened or remains to close. The temporary
Oracle structural review completed; its transcript remains external and no
raw Oracle output is committed.
