# Report 2375 - local-predicate proposition-decidability selection

- Date: 2026-07-23 00:58 JST
- Author / agent: Codex
- Scope: LAB candidate selection and future pre-registration plan only
- Decision levels touched: none; no Canon theory, ledger, gate, phase, working record, or Lean outcome change

## Objective

Select the smallest non-duplicative successor after WRK-0016 froze on its
data-valued declaration form, without treating an anonymous example or a value
declaration as a repair.

## Scope and assumptions

Canon remains normative. This package does not create a WRK, edit Lean source,
or run any candidate outcome command. The selected route must remain an
existing-lane L3 experiment and retain all OBL-003, checker, Core, API, and
workflow non-claims.

## Start state / dirty state

`main...origin/main` was clean at `e0380515`, matching `origin/main`. The
Discord baseline for the continuing candidate-selection package was recorded.

## Documents consulted

Read Canon README/MAP, ADR-0014, theory/01, theory/02, theory/11, WRK-0014,
WRK-0016, plans 158, 171 through 173, the post-WRK-0014 screens, Reports 2370
through 2374, current snapshots, and the exact Lean foundation. A planner and
reviewer independently read the same evidence; the preceding temporary Oracle
review remained advisory input on the frozen boundary.

## Actions taken

Compared proposition-valued excluded middle, anonymous example/local binding,
retained value declaration, failed-subset witness extraction, and the remaining
OBL families. Selected only the proposition-valued theorem candidate. Recorded
the exact lane, adverse control, semantic declaration boundary, falsifiers,
and non-claims in plan 174; registered that plan in both hierarchy scaffolds.

## Files changed

- `plan/174-local-predicate-proposition-decidability-selection.md`
- `plan/00-index.md`
- `scripts/validate_docs.py`
- `scripts/check_source_hierarchy.py`
- `Documentation.md`
- `docs/project-status.md`
- `progress.md`
- `tasks.md`
- this report

## Commands run

- Canon/LAB source, ledger, plan, report, and foundation inspections
- read-only planner shortlist and reviewer non-duplication audit
- source-hierarchy scaffold inspection for the new numbered plan
- documentation/source-hierarchy validation and focused diff review
- `python3 -m unittest` for the three numbered-plan scaffold/registration tests

## Evidence / outputs / test results

The planner found one strong candidate: a named theorem of
`captureSubset lhs rhs ∨ ¬ captureSubset lhs rhs`; it is proposition-valued and
therefore tests the explicit body rather than a retained data value. The
reviewer agreed that the anonymous example has lower retained evidence value,
local bindings add no distinct signal, retained `def`/`abbrev`/`opaque` has no
bounded consumer, and witness extraction remains reserve until a proof-level
consumer exists. Both reject the remaining ledger routes as duplicate or
owner-reserved.

No successor WRK, Lean source edit, or Lean outcome command exists in this
package. `make docs` passed with 724 / 724 hierarchy paths and 1,529 numbered
reports. The three focused numbered-plan tests passed in 0.068 seconds.

## What changed in understanding

The correct successor criterion is not merely “make the previous proof work.”
The theorem target itself must carry the decision value. A proposition-valued
excluded-middle theorem can distinguish body constructivity from persistence
policy, whereas an anonymous example discards the proof and a retained value
declaration conflates proof with helper/API design.

## Open questions

- Does the selected proposition-valued theorem compile under the declared
  explicit-elimination/no-generic-mechanism boundary?
- Does the opaque-domain theorem attempt fail specifically because the finite
  carrier is absent?
- Is failed-subset witness extraction ever needed by an identified future proof
  consumer?

## Suggested next prompt

Create and push a fresh L3 pre-registration for the exact proposition-valued
`captureSubset` theorem, then run only its registered Lean evidence plan.

## Plan update status

Updated: plan 174 records the selected successor, rejected alternatives,
pre-registration cut, semantic declaration boundary, and non-claims; the plan
index links it.

## Documentation.md update status

Updated: the reader map and current-position narrative link the frozen first
route to the distinct proposition-valued successor.

## docs/project-status.md update status

更新済み: the control view now identifies the only selected successor and
states that registration/outcome do not yet exist.

## progress.md update status

progress.md 更新済み: the logical snapshot and dated log distinguish the
selected theorem candidate from WRK-0016's frozen declaration form.

## tasks.md update status

tasks.md 更新済み: package 44 closes the comparison and makes fresh
pre-registration the next autonomous package.

## samples_progress.md update status

samples_progress.md 更新不要: no Lean source, runnable command, debug surface,
dashboard row, or workflow readiness changed.

## Reviewer findings and follow-up

The planner recommended the proposition-valued theorem because it is a
forward successor with a meaningful body-versus-persistence discriminator. The
reviewer independently rejected treating anonymous examples, local bindings,
or value declarations as one repair and required a semantic declaration-form
boundary. Oracle's prior frozen-boundary review supports those constraints.
Neither advisory result changes Canon or substitutes for a fresh WRK.

## Skipped validations and reasons

No Lean candidate command ran because plan 174 is selection-only and a fresh
immutable registration must precede every outcome command or source edit.
Runtime, distributed, and sample-sync validation do not apply. The full
validator unit suite is not rerun for this mechanical registration-list change:
the immediately relevant three numbered-plan tests and the actual
documentation/source-hierarchy checks passed before commit.

## Commit / push status

Pending at report write. This selection package will be committed with
`--no-gpg-sign`, validated after commit, and pushed immediately when checks
pass.

## Sub-agent session close status

Planner and reviewer completed read-only candidate audits and are closed. The
temporary Oracle review was already closed and distilled in Report 2374; no
external transcript is repository state.
