# Report 2524 — WRK-0041 P017 X1 preregistration

- Date: 2026-07-29
- Author / agent: codex
- Scope: Register one ADR-0014 L3 candidate for the owner-terminal exclusivity
  boundary within the owner's P017 X1 V1/R1 cross-locus-read scope.
- Decision levels touched: L3 only (`working/WRK-0041`). No L0/L1 decision,
  theory ledger, implementation contract, Gate, Phase, or public claim changed.

## Objective

Pre-register a falsifiable, fixture-only test for the `X-BRANCH` simultaneous
owner-terminal-positive/negative collapse without selecting a branch type,
failure row, carrier, transition, or runtime representation.

## Scope and assumptions

The authority/input cut is
`187c3eacf0f45a194072f004443728e9b94f672b`. P017 X1 allows the first and
subsequent independently eligible existing-LAB-lane L3 investigations, not
positive semantics. The declared lane is a single later Markdown-held Lean
artifact under `plan/`, temporary extraction, and direct evidence reports.

## Start state / dirty state

`HEAD` and fetched `origin/main` were equal at
`187c3eacf0f45a194072f004443728e9b94f672b`; the worktree was clean. WRK-0040
was retained as non-promoted finite detector evidence. No WRK-0041 path or
historical `X-BRANCH` countermodel existed.

## Documents consulted

`mirrorea_canon/README.md`, `mirrorea_canon/MAP.md`, ADR-0014,
`working/README.md`, theory/01, P017, WRK-0039, WRK-0040, Plans 214, 220, and
221, the WRK-0040 source and execution report, the current LAB snapshots, the
report template, and the Oracle operating instructions.

## Actions taken

Completed a local all-history keyword/path check and one temporary Oracle
review. Both distinguish this `X-BRANCH` terminal-exclusivity question from
WRK-0039's presentation comparison and WRK-0040's owner-service versus
requester-receipt/use `PHASE` detector. Registered the candidate with a
four-fixture control/singleton/overlap matrix, alternative, falsifiers, strict
non-effects, and rollback before any outcome source or Lean command may run.

## Files changed

- `mirrorea_canon/working/WRK-0041-p017-x1-owner-terminal-exclusivity-countermodel.md`
- `mirrorea_canon/MAP.md`
- `mirrorea_canon/INDEX.json`
- `docs/reports/2524-wrk0041-p017-x1-preregistration.md`

## Commands run

- Read the Canon and LAB inputs in the recorded source hierarchy.
- Checked clean state, `HEAD`, fetched `origin/main`, timestamp, path absence,
  and historical occurrence of `X-BRANCH` / owner-terminal terms.
- Ran `ask-chatgpt-pro-temp` session `p017-x1-post-wrk0040-screen` with the
  relevant Canon and LAB files attached.
- Calculated every pinned source SHA-256 at the authority/input commit.
- Ran Canon index, source hierarchy, documentation, diff, and secret checks
  before the registration commit.

## Evidence / outputs / test results

The local history search found `X-BRANCH` only in Plan 220 and P017, not in a
prior countermodel. The Oracle review recommended this exact finite
owner-terminal exclusivity oracle and rejected pending binding, receipt,
typing, causality, load, authority, observation, source, runtime, and contract
candidate classes as reserved or duplicate. Its advice is advisory only.

No outcome source exists and no Lean command, theorem result, or countermodel
result has run. This is intentional: ADR-0014 and `working/README.md` require
the pre-registration to be committed and pushed before outcome evidence.

## What changed in understanding

P017's `X-BRANCH` condition supplies a distinct negative question that does
not require an answer to the missing carrier question: a finite fixture can
separate allowed no-terminal and singleton facts from a supplied simultaneous
terminal-fact overlap. This does not establish that a Mir execution produces
those facts or enforces their exclusivity.

## Open questions

The positive terminal-branch representation, typed result/failure rows,
pending binding, receipt/rejection treatment, one-shot use, authority, load,
and observation mechanisms remain unresolved. The registered source must stop
instead of importing any of them.

## Suggested next prompt

After this registration is committed and pushed, materialize only its declared
four-fixture Lean source, run the exact registered checks, and retain or freeze
the result without selecting semantics.

## Plan update status

`plan/` 更新不要: `working/README.md` requires this registration commit to
contain only the new WRK and exact operational metadata. The next snapshot
package records the newly registered work in Plan 221 and the LAB readers.

## Documentation.md update status

`Documentation.md` 更新不要: registration-only scope keeps reader snapshots
for the immediately following synchronization package.

## docs/project-status.md update status

更新不要: registration-only scope keeps the compact control view for the
immediately following synchronization package.

## progress.md update status

`progress.md` 更新不要: the next snapshot package records the registration and
current autonomous execution boundary.

## tasks.md update status

`tasks.md` 更新不要: the next snapshot package moves the current task from
candidate screening to the registered execution.

## samples_progress.md update status

`samples_progress.md` 更新不要: no runnable sample, command, debug surface, or
sample-dashboard row changed.

## Reviewer findings and follow-up

Temporary Oracle session `p017-x1-post-wrk0040-screen` completed with GPT-5.6
Sol / Pro effort. Its strongest caveat is that a successful four-row table has
limited value and proves no positive X1 model; WRK-0041 records that caveat as
a non-claim. No callable sub-agent execution interface is available here.

## Skipped validations and reasons

Lean materialization, compilation, axiom reporting, matrix checks, and source
scans are not run before this registration is committed and pushed. No runtime
or sample command applies to a registration-only Canon/LAB metadata package.

## Commit / push status

Registration commit and push follow final validation. Exact commit identity and
remote equality are verified before materializing any WRK-0041 outcome source.

## Sub-agent session close status

No callable sub-agent session was opened or remains to close. The completed
temporary Oracle review has no repository session to close.
