# Report 2571 — WRK-0046 P017 X1 K0 q-fiber U/L registration

- Date: 2026-07-31
- Author / agent: Codex
- Scope: Source-free ADR-0014 preregistration for one finite, linear,
  candidate-local K0 U/L conditional lemma.
- Decision levels touched: a reversible L3 `working/WRK-0046` boundary only.
  No L0/L1/L2 decision, Canon semantic amendment, theorem/OBL, Gate, Phase,
  profile, implementation contract, or public claim changed.

## Title and identifier

2571-wrk0046-p017-x1-k0-qf-ul-lift-registration: register the exact question,
alternative, controls, non-effects, and rollback before any Lean source or
outcome evidence exists.

## Objective

Record the smallest separate K0 U/L experiment left after the frozen WRK-0045
line: test whether a supplied finite linear q-fiber lineage preserves a
candidate-local spent predicate across one restore relation without selecting
P017's actual relation, identity, transition, or persistence design.

## Scope and assumptions

Canon remains normative. P017 and ADR-0014, not this report or a LAB plan,
provide the standing research route. `state`, `restore`, `consume`,
`AcceptedSuccess`, and `Spent` are opaque experiment-local vocabulary. The
record is source-free and unexecuted; no conclusion is claimed.

## Start state / dirty state

`HEAD` and `origin/main` were equal and clean at
`246f2b1cbafe072ed4a3a11d202609c3401eb6ad`. `WRK-0046` and its declared future
source path were absent. The root filesystem had 14 GiB free and 9.2 GiB memory
available at the immediately preceding selection package; this registration
creates documentation only.

## Documents consulted

- Canon: `README.md`, `MAP.md`, `plan/01-phases.md`, ADR-0013, ADR-0014,
  `working/README.md`, frozen WRK-0045, P012, P013, P016, P017, and
  `theory/01`, `theory/02`, `theory/04`, `theory/05`, and `theory/07`.
- LAB: Plans 196, 197, 227, 230, 231, 245; Reports 2570; `AGENTS.md`,
  `Documentation.md`, `docs/project-status.md`, `progress.md`, `tasks.md`, and
  `samples_progress.md`.
- Operations: the Oracle operating notes, current planner rule, and
  `scripts/validate_docs.py` working-annex controls.

## Actions taken

1. Recomputed the exact current Canon/LAB hashes at the clean registration
   parent and verified the intended future source path was absent.
2. Obtained a Canon-first planner pre-edit review. It accepted the candidate
   only after requiring opaque experiment-local carriers, one four-endpoint
   relation witness instead of a map, an explicit finite-linear ceiling, an
   anti-vacuity witness, separate A1 adverse control and A0 falsifier, and a
   strict no-surface stop line.
3. Added source-free `WRK-0046`, registered it in Canon `MAP.md`, and updated
   only the permitted LAB status snapshots to distinguish it from the official
   T0 critical path.
4. Did not create a Lean source, helper, schema, CI/Make surface, sample,
   runtime artifact, or any outcome evidence.

## Files changed

- `mirrorea_canon/working/WRK-0046-p017-x1-k0-qf-ul-lift.md`
- `mirrorea_canon/MAP.md`
- `mirrorea_canon/INDEX.json`
- `docs/project-status.md`
- `progress.md`
- `tasks.md`
- `docs/reports/2571-wrk0046-p017-x1-k0-qf-ul-lift-registration.md`

## Commands run

- Canon/LAB source reads, hash calculation, `git status`, base/remote parity,
  and future-source absence checks.
- One read-only Canon-first planner pre-edit review.
- Canon index regeneration after drafting the working record.
- A pre-commit `make check`: index reported 134 files and source hierarchy
  reported 795/795 before the working-annex validator deliberately rejected the
  uncommitted WRK record.

## Evidence / outputs / test results

The registered question has one finite linear sequence of endpoint pairs and
one supplied restore-relation witness. A0 retains the A0-only `Spent`
preservation premise and its one-consume/one-restore anti-vacuity fixture. A1
retains the relation witness but removes only preservation; it must exhibit the
separate post-restore two-consume adverse fixture. Failure to materialize that
A1 control is `DEFER`/freeze, not the true A0 falsifier. The true falsifier is
stronger: two consumes while every A0 premise still holds.

No Lean declaration, compilation, model, countermodel, parser, runtime,
transport, sample, theorem, OBL result, or implementation evidence exists in
this registration. The only retained artifact is the registration boundary; no
outcome evidence is retained.

## What changed in understanding

The independent consumer is not a decision that consumption is primitive or
derived. It is the narrower open U/L obligation that a future design must not
reset or re-enable consumed use after restore. A finite experimental result can
test that obligation without claiming P017's actual every-continuation closure.

## Open questions

- Does the later Markdown-held Lean source prove the A0 lemma and exhibit the
  A1 adverse trace without assuming at-most-one use, identity, or a restore
  function?
- Does any required evidence surface trigger `DEFER` before it can be retained?
- What ordinary Canon design, if any, later gives a final primitive or uniquely
  derived consumption representation? This record does not decide it.

## Suggested next prompt

After this registration is pushed, materialize only the declared
`plan/wrk-0046-p017-x1-k0-qf-ul-lift.md` Lean evidence, execute its A0/A1
controls, obtain an independent review, and freeze immediately on the registered
falsifier or any reserved dependency.

## Plan update status

`plan/` 更新不要: Plans 227, 230, 231, and 245 already supply the consumer,
comparison boundary, and re-screen. The only future plan path is the separately
declared source artifact after this record is pushed.

## Documentation.md update status

`Documentation.md` 更新不要: it already states that the selected q-fiber
candidate is source-free and does not imply a semantic, lifecycle, or runtime
advance. The current status views now name its registered/unexecuted state.

## docs/project-status.md update status

更新済み: the reader-facing state now distinguishes source-free/unexecuted
WRK-0046 from WRK-0045's frozen line and from the unchanged official path.

## progress.md update status

更新済み: the logical-specification, relation-state, and recent-log rows record
that only post-push WRK-0046 evidence may proceed in the parallel reserve lane.

## tasks.md update status

更新済み: R1 is registered source-free, R2 is the only later reserve package,
and CP-1/CP-2 remain the official critical path.

## samples_progress.md update status

`samples_progress.md` 更新不要: no sample, runner, debug surface, validation
command, or sample workflow changed.

## Reviewer findings and follow-up

The pre-edit planner returned conditional GO. Its required corrections are all
reflected in WRK-0046: q-only predicates, opaque experiment-local state,
four-endpoint relation witness rather than map/function, exclusive edge classes,
one finite linear lineage, separate restore edge/preservation, variant-specific
anti-vacuity, A1 adverse control, separate A0 falsifier, and reserved-surface
`DEFER`. The final corrected-diff review returned `APPROVED`: pins, fields,
allowlist, snapshot wording, A0/A1 split, and non-outcome status were all
consistent.

## Skipped validations and reasons

Lean, parser, runtime, transport, and sample execution are deliberately skipped:
the source-free registration has no permitted executable evidence. The full
documentation unit suite was already run successfully on the immediately prior
documentation package and validator code is unchanged. The ordinary pre-commit
working-annex check correctly stops at "working record must be committed at
HEAD"; its nonzero result is not relied on. Focused authoritative working-annex
checks run only on the clean committed registration HEAD, as required by the
working annex.

## Commit / push status

Pending at report write. This registration will be committed with
`git commit --no-gpg-sign`, pushed to `origin/main`, and checked for remote
parity before any Lean materialization package begins.

## Sub-agent session close status

The planner completed the pre-edit review without repository edits, returned
final `APPROVED` after the correction review, and its session was closed. A
fresh planner review is required for the later evidence package.
